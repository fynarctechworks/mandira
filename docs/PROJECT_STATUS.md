# Mandhira — Project Status

Allowed statuses: `NOT_STARTED · ANALYSIS · PLANNED · IN_PROGRESS · IMPLEMENTED · TESTING · REVIEW · COMPLETE · BLOCKED`

Update rules: one row per feature (granularity = FEATURE_INVENTORY IDs); update in the same commit as the work; a feature is COMPLETE only when its CLAUDE.md Definition of Done holds and its requirements are ticked in this file's Notes.

| Module | Feature | Requirement IDs | Status | Dependencies | Last updated | Notes |
|---|---|---|---|---|---|---|
| Foundation docs | This documentation set | — | COMPLETE | — | 2026-08-24 | Created in project-foundation task; pending founder approval |
| Platform | PLAT-01 Monorepo & tooling | TRD-ARCH-001, TRD-ARCH-006 | COMPLETE | — | 2026-08-24 | B-001 done. pnpm+turbo; apps web/ops; packages ui/db/journey-engine/providers/i18n/config; eslint/prettier/vitest/Playwright; GH Actions lint+typecheck+test+build. Plan: docs/plans/PLAT-01.md |
| Platform | PLAT-02 Design system | PRD-DSGN-001..004, PRD-DSGN-006 | IN_PROGRESS | PLAT-01 | 2026-08-25 | B-002 delivered 13 of 15 PRD §12.5 components in `packages/ui` + motion/focus tokens; 81 unit tests, axe green on both shells with **no exceptions**. OPEN-008 **resolved** (D-025): §12.1 light palette revised, all 47 pairs now AA in both modes. Plan: docs/plans/PLAT-02.md. **Remaining:** Ops data table (B-008) and Ops side-by-side review (B-012) per D-022. **shadcn wired as the source of UI primitives (D-103):** `components.json` in both apps and `packages/ui`, MCP server in `.mcp.json` pinned to the workspace dependency rather than floating on `@latest`, `pnpm ui:add <name>` to pull one in. Its token contract is ALIASED onto PRD §12.1 (`packages/config/tailwind/shadcn-bridge.css`, 59 contract assertions) rather than letting `shadcn init` write a second palette over verified contrast work. Domain components (TierChip, TrustBadge, HealthPill…) stay Mandhira's — they encode PRD rules, not generic UI. `Select` deliberately not taken: a native select opens the OS picker on the phones this is built for. See docs/DESIGN_SYSTEM.md. **App shell states added** — both apps had NO error or not-found boundary, so a thrown exception showed Next's own "Application error: a server-side exception has occurred", three PRD §12.7-forbidden words in one sentence shown to a traveler. **No `loading.tsx` anywhere, on purpose (D-101):** route streaming turns `notFound()` into a soft 404 and `redirect()` into a client navigation — one such file turned 12 passing tests red. `/journeys/*` is gated in middleware instead, so the sign-in redirect is a real 307 (D-102). |
| Platform | PLAT-06 Observability & quality | PRD-DSGN-005/006, TRD-PERF-001, PRD-ANLY-001, TRD §6.1 | IMPLEMENTED | PLAT-05 | 2026-08-26 | **B-024 done.** Security headers on every response in both apps — CSP, HSTS, frame-ancestors, referrer policy, permissions policy — shared from `packages/config` so the two apps cannot drift (D-114 records why `script-src` keeps `'unsafe-inline'` and nonces wait for M2). Analytics is an allowlist of event names AND per-event property names, with values constrained to look like categories so a search term cannot arrive as one (D-115); pgTAP asserts the table has no column that could hold a person and that `anon` is write-only. PRD §12.7 is enforced by a scanner, verified by breaking it (D-116). Error reporting is a seam wired into every route and both boundaries; `@sentry/nextjs` waits for a DSN rather than spending the route budget inert (D-117, ACCT-06). `pnpm check:bundle` makes TRD-PERF-001 real. Plan: docs/plans/PLAT-06.md. **Found and fixed three live problems:** `anon` could TRUNCATE all 68 tables and RLS does not apply to TRUNCATE (`0022`); the four most prominent buttons used an undefined utility class and so had no text colour, failing contrast in dark mode; four routes were over the 180 kB budget, sign-in by 44 kB because `@supabase/ssr` was statically imported into a one-field form. **Deferred:** Lighthouse on a reference device (B-025, needs a deployed URL). |
| Platform | PLAT-07 Production readiness | TRD-DEPL-001, TRD-DEPL-003 | BLOCKED | PLAT-06 | 2026-08-26 | **B-025 built as far as it goes without a deployment.** `scripts/preflight.mjs` refuses a deploy on a localhost Supabase URL, a missing Resend key, a short `CRON_SECRET`, or a service-role key exposed to the browser — each fails QUIETLY in production. `scripts/backup-verify.sh` takes a nightly encrypted dump and **restores the encrypted archive into a scratch database** under `ON_ERROR_STOP=1`, comparing table and RLS-policy counts before keeping it (D-118) — run here: 55 tables, 140 policies. `deploy.yml` applies migrations BEFORE promoting either app, so a rollback never needs a migration rollback (D-119). 8-test smoke suite, run against a local production build. `docs/RUNBOOK.md` marks every procedure verified or untested and closes with what is unproven (D-120). Plan: docs/plans/PLAT-07.md. **BLOCKED on the founder, not on code:** GIT-01 (13 unpushed commits, CI has never run), OPEN-003 (domain), Vercel + Supabase prod projects, ACCT-01 (Resend — without it most travelers cannot sign in), ACCT-02/04/06, OPEN-007 (pilot planners). **Deferred until deployed:** Lighthouse on a reference device, PRD-PLAN-009 off localhost, a restore at production volume. |
| Platform | API-01 Route pipeline | TRD-API-001, TRD-SEC-001, PRD §12.7 | IMPLEMENTED | PLAT-04, JOBS-01 | 2026-08-25 | `withApi` in `@mandhira/db/api` (D-071): validate → authenticate → authorize → rate-limit → handle → `{ok,data}|{ok,error}`, `no-store` on every response, `ApiError` so a handler can refuse in its own words, and `onUnexpected` as the Sentry hook for B-024. Clients are injected, not imported (D-073). `apps/web/lib/api.ts` binds the traveler instance. Plus `GET /api/cron/keepalive` on Vercel Cron — **resolving a TRD §5.4 vs BACKEND_ARCHITECTURE conflict in the TRD's favour (D-072)**. 25 vitest. Plan: docs/plans/API-01.md. **OPEN-011 resolved in B-023:** middleware now issues an opaque `mandhira_device` cookie, so guests no longer share one rate-limit bucket. **Not COMPLETE:** the Ops instance lands when Ops first needs a route handler. |
| Platform | JOBS-01 Scheduled jobs | TRD §5.4, PRD-KNOW-002, PRD-REPT-004, PRD-PRIV-005 | IMPLEMENTED | PLAT-03 | 2026-08-25 | `0013` adds the three pg_cron jobs — `recompute_freshness` (report downgrade, freshness re-derivation, reverify tasks for stale criticals), `roll_journey_statuses` (in the journey's own timezone; `archived` is never undone), `purge_deleted_accounts` (hard DPDP erasure after 30 days) — plus the jobs contract: `job_runs`, single-flight `run_scheduled_job()` (D-070) and `v_job_health`. 55 pgTAP. Plan: docs/plans/JOBS-01.md. **Not COMPLETE:** single-flight is asserted structurally, not behaviourally (advisory locks are re-entrant per session and pgTAP cannot open a second connection); the Edge Function jobs are B-027/B-029/B-031; alerting on `v_job_health` is B-025. |
| Platform | PLAT-03 Schema | TRD-DB-001/002/004/005, TRD-ARCH-004, PRD-KNOW-001/003 | COMPLETE | PLAT-01 | 2026-08-25 | B-003+B-004 done. 7 migrations, 51 tables, 9 `v_published_*` views, RLS enabled everywhere, 119 pgTAP tests, `db lint` clean, CI-enforced. Publish gate is structural: anon has no base-table grant. Plans: docs/plans/PLAT-03.md. B-005 adds generated types (`packages/db/types.ts`, CI-checked for staleness) + Zod schemas for the five TRD Day-3 entities. RLS *policies* are B-006. |
| Platform | PLAT-04 Authorization | TRD-DB-003, PRD-PRIV-002 | COMPLETE | PLAT-03 | 2026-08-25 | B-006 done: `0008_rls_policies.sql` implements the §4.9 matrix; role helpers; 33 pgTAP role/IDOR tests (152 total). Anon surface pinned to 5 policies by an allowlist test. Route-guard layer (`withApi({roles})`) lands with B-007. **OPEN-009** raised: 5 tables carry traveler-relevant data with no view or policy. |
| Platform | PLAT-05 Traveler shell & PWA | TRD-DEPL-002, PRD-OFFL-002, PRD-LANG-002 | COMPLETE | PLAT-02 | 2026-08-25 | B-014 done: /[locale] routing (en + te/hi scaffolds falling back to English, D-056), bottom nav, Serwist worker that never force-reloads (D-058), manifest + install prompt, heartbeat-backed offline banner (D-057). 133 vitest + 35 Playwright. Plan: docs/plans/PLAT-05.md. Dexie offline reads are B-023. |
| Offline | OFFL-01..03 | PRD-OFFL-001/002/003/007, TRD-ARCH-002/005 | IMPLEMENTED | PLAT-05, LIVE-01 | 2026-08-26 | **B-023 done.** Dexie schema verbatim per TRD §4.8; `GET /api/journeys/:id/snapshot` assembles the whole journey in ONE payload (a snapshot built from nine round trips arrives three-quarters written on one bar); `syncJourneyOffline` writes it on open and after every action. **Live Journey works fully in airplane mode** — and always recomputes in the browser from IndexedDB against the real clock rather than replaying a cached projection (D-110), because a stale NOW card is indistinguishable from a live one. `assembleLiveView` was extracted as a PURE function so server and browser run the same code over the same bytes (TRD-ARCH-002). The service worker now caches navigations NetworkFirst — without that, IndexedDB held the whole journey and the browser never got far enough to read it. Reconcile names only what a plan depends on, in one card, and the whole layer fails SOFT: no sync-error dialog exists anywhere (D-111, PRD-OFFL-003). Guest drafts land here as carried from B-019 — offered back after sign-in, never migrated silently, and actually deleted on discard (D-113). `useOfflineFirst` per FRONTEND_ARCHITECTURE §11.2. 16 vitest against a real IndexedDB + 9 Playwright under `setOffline`. Plan: docs/plans/OFFL-01.md. **Deferred:** map tiles (needs MAPS-02), queued writes (M3 `pending_actions`), offline replanning (needs B-026), phrase packs (F12 has no content). **Raised:** DOC-02 (TRD §5.1's KnowledgeBundle list is stale both ways), DEV-01 (TanStack Query not adopted — one cache, not two). |
| Identity | AUTH-01..05 | PRD-ACCT-001/002, TRD-DB-003, PRD-OPS-WF-009 | IMPLEMENTED | PLAT-04 | 2026-08-25 | B-007 done: magic link + Google (D-009), guest-first traveler middleware, `@supabase/ssr` clients in `@mandhira/db/client/*`, profile trigger (D-036), Ops role gate, separation-of-duties trigger (D-037), local admin seed. 161 pgTAP + 5 Playwright incl. a real magic-link E2E (D-038). **Not COMPLETE:** Google OAuth is config-ready but disabled until a Google Cloud client exists; production magic links need a verified Resend domain. |
| Knowledge | FIXTURE-01 Local fixture | TRD §11.2, PRD-TRST-001, PRD-DISC-003 | COMPLETE | KNOW-04 | 2026-08-25 | `supabase/seed/0002` seeds one clearly-invented destination (`fixture-*`, every name carries `(fixture)`, sources `example.invalid`) covering the full RANGE the UI must handle: high/medium/low confidence, a conflicted field, accessibility yes/partial/unrecorded, and a `published` place the gate correctly holds back (D-081). 16 pgTAP pin that range. Three existing assertions were counting globally and now scope to their own fixtures (D-082). Plan: docs/plans/FIXTURE-01.md. **NOT content** — B-013 still needs OPEN-001; this makes B-013 mostly data entry when it unblocks, and lets the B-015 discovery UI be built and tested now. |
| Knowledge | KNOW-01..04 | see inventory | IN_PROGRESS | PLAT-03 | 2026-08-25 | Schema + publish gate landed in B-004 (KNOW-01 entity model, KNOW-03 gate); Ops editors B-009..B-012. **KNOW-04 (this commit):** `0014` repairs a live bug — every published view raised `permission denied for table trust_records` for anon and for any signed-in traveler, so the whole traveler read surface was unusable with content in it (hidden since 0007 because per-row functions only evaluate when there are rows, and all pgTAP ran as `postgres`). Helpers are now SECURITY DEFINER (D-078). `0015` exposes accessibility on places/experiences/routes and stops on routes, resolving the blocking half of **OPEN-009** (D-079, D-080). 51 new pgTAP incl. `0013_traveler_read_surface_test`, the regression guard that reads every view *with content* as a guest and as a traveler. Plan: docs/plans/KNOW-04.md. **Carried:** Dexie version bump for the view shape change → B-023. |
| Ops core | Ops platform (M1 scope) | PRD-OPS-CNT-001, PRD-KNOW-001/002/003/004, PRD-OPS-SRC-001, PRD-OPS-WF-004/009 | COMPLETE | AUTH-05 | 2026-08-25 | B-008..B-012 done: shell, all knowledge editors, sources registry, trust panel, and the full validate/submit/approve/publish path plus the media library. Publishing is enforced in SQL (D-052) and refuses self-approval. Nav O01..O08/O13/O16 live. 125 vitest + 176 pgTAP + 29 Playwright. Remaining Ops screens are M2..M4 per the backlog. |
| Discover | DISC/SRCH-01..02/TRST | see inventory | IN_PROGRESS | KNOW-04 | 2026-08-25 | **B-015 first slice done.** Unblocked by FIXTURE-01: B-015 needed B-013 only for something to display. `lib/knowledge.ts` reads the published views with locale resolution at the boundary; `lib/present.ts` holds PRD F2's plain-language rules; destination page renders F2's sections in F2's order with advisories first; home lists real destinations with an honest empty state. Cards carry the weakest trust state across their fields (D-083) and say so in words when accessibility is unrecorded (D-084). 12 E2E incl. an axe pass at WCAG 2.2 AA. Plan: docs/plans/DISC-01.md. **Second slice done:** place and experience detail pages, each critical field carrying its OWN trust badge, and the trust sheet opening in one tap (PRD F9 acceptance). Availability reads its trust from the availability rule, not the experience (PRD F1). 13 more E2E. Found and fixed: a client component pulling `next/headers` through a type import (D-086), and a `dlitem` accessibility violation axe caught (D-088). **Third slice done (SRCH-01):** `0016` exposes the `search_tsv` columns on the published views; `/[locale]/search` is a plain GET form — a result is a URL, and it works before any JavaScript arrives (D-089). Four of PRD F2's six filters ship; the two needing a journey are absent with a line saying when they arrive (D-091). One Type control over two non-overlapping enums, sourced from the generated `Constants` (D-090). 15 more E2E. **Remaining in B-015:** "See all" past 20 cards, and journey-aware ranking (PRD-DISC-006, needs B-019). **"Add to journey" needs B-019.** |
| Intent | INT-01..03 | see inventory | IN_PROGRESS | KNOW-03 | 2026-08-26 | **PRD F3's structured-form path is done** (`/[locale]/plan`), which needs no AI and is also the TRD §5.5 fallback when the provider is unavailable — so it works on its own terms rather than as a degraded mode. It feeds `buildInitialJourney` and renders the result at `/plan/preview` with Journey Health and its causes: the first screen where the engine reaches a person. Nothing is saved (D-093). `0017` exposes `travel_estimates` so the engine can cost travel at all (D-092). 14 E2E. Plan: docs/plans/PLAN-01.md. **B-018 part 1 done** (the half that needs no content): `0012` adds `ai_calls` (no user identifiers, asserted in pgTAP), `ai_cache` (hash-keyed, 24 h), `rate_limits` + `consume_rate_limit()` (D-062); `packages/providers/src/ai` adds the `AiProvider` abstraction, env-driven model tiers, `runAiTask` (cache → 12 s timeout → one retry → fallback → anonymous log), `assertGrounded`/`keepKnownIds`, `journeyBriefSchema` closed over the published ids (D-064), and the Vercel AI SDK binding; `packages/db` adds `rateLimit()` and the cache/log stores (D-063). 33 pgTAP + 52 vitest. Plan: docs/plans/INT-01.md. **Remaining:** `POST /api/intent/extract`, brief review (A08) and the structured form — blocked on B-015 → B-013 → OPEN-001, and end-to-end extraction also needs ACCT-04 (Gemini key). |
| Plan | PLAN/ENG-01..04,06/HLTH/PREP-01,04/MAPS | see inventory | IN_PROGRESS | ENG-01 | 2026-08-26 | **B-019 Journey Builder done (PLAN-01/02/03/05/06):** traveler sign-in; `POST /api/journeys` persists a `buildInitialJourney` result (the first real `withApi` caller); journeys list; day timeline with tier chips, visible+editable buffers, health and causes; item actions (tier, move, remove, note) each an explicit tap. PRD-PLAN-002's tier rules live in the engine and are enforced at the ROUTE, with E2E hitting the API directly (D-096). Plan: docs/plans/PLAN-02.md. **Fixed two live bugs found here:** the return guard missed items running PAST the anchor — Comfortable while the traveler missed their train, live since B-016 (D-095); and locale middleware was 307'ing every `/api/*` route, so the Vercel Cron keepalive silently never ran and traveler magic links 404'd, live since B-014 (D-097). PRD-PLAN-009 measured, not asserted: p50 119 ms / p95 199 ms, localhost. **Deferred:** drag-and-drop, guest drafts (B-023, docs conflict resolved in PLAN-02 §5a), prep propagation (B-021), transactional creation (OPEN-012). **B-021 Prepare + Journey Summary done (PREP-01/04, SHARE-01 read-only):** the checklist renders PRD F7's groups from `generatePrepareTasks`, deduplicated, each knowledge-derived task carrying a trust badge and its how-to verbatim behind Why?; ticks persist and survive a rebuild. A row stores WHICH task and whether it is ticked, never the words (D-099) — so a dress-code change or an M4 translation reaches journeys planned today. Printable summary at one A4 per day, **measured** with Chromium at A4 (D-100 cut a busy day from 3 sheets to 1). Share links are 32-byte, 30-day, revocable, and read through a SQL projection with two doors — a token or ownership — that structurally cannot return traveler profiles, notes or the owner's identity (D-098); no service-role key touches the one page a stranger can open. Plan: docs/plans/PREP-01.md. **Deferred:** deadline delivery (B-027, `due_at` is persisted for it), booked-slot promotion (B-028), real offline downloads (B-023). **B-020 part done — MAPS-01 + MAPS-03, without waiting for either key (D-105):** `RoutingProvider` in `packages/providers/src/routing` — an OpenRouteService adapter, a straight-line fallback, and a chain that takes the first real answer. Every estimate carries `source: routed | estimated` and an adapter NEVER falls back internally (D-104), so the label cannot lie; the chain always answers, so the engine can always schedule. With no `OPENROUTESERVICE_API_KEY` (the state today) everything is `estimated` and deliberately pessimistic — arriving early costs a sit-down, arriving late costs a darshan. Open-in-Maps hands off to the traveler's own maps app rather than rendering our own (D-106); Android gets a `geo:` URI so OsmAnd users keep OsmAnd. `0021` exposes latitude/longitude on `v_published_places` — `location` reached PostgREST as WKB hex and was unusable client-side. 28 new tests. **Remaining in B-020:** MAPS-02 map tiles only, which is the one part that genuinely needs ACCT-03. **B-022 Live Journey done (LIVE-01..04):** NOW / NEXT / LATER on one scroll, no calendar grid, health pill pinned with causes. NOW carries exactly three actions, and they RECORD what happened rather than rescheduling — the option ladder stays in the Change Card (D-107), asserted by an E2E that compares every planned time before and after tapping Done. Activation is read-time; only "Start today" writes `active` (D-108). Live state widens the APP's item type, never the engine's (D-109), and the clock is read in the page and nowhere below it. Navigate reuses B-020's hand-off. Plan: docs/plans/LIVE-01.md. **Deferred:** the fix-it options (B-026), practical chips (needs MAPS-02), phrase shortcut (F13). | B-016 done: ENG-01 scheduling core — resolveAvailability, computeBuffer, scheduleDay, checkReturnGuard, timezone/DST-correct time helpers. Plan: docs/plans/ENG-01.md. **B-017 done:** ENG-02/03/06 — `computeHealth` (5 checks, 4 states, causes as i18n keys + params, all three PRD-HLTH-005 physical rules), `buildInitialJourney` (deterministic, adds nothing the brief does not name, drops nothing), `generatePrepareTasks` (PRD F7 groups, deduplicated, stable ids), and the comlink worker wrapper above 40 items in `apps/web/lib/engine` (D-060). 154 engine + 7 worker-client tests; coverage 99.9%/93.9% against a 90% gate. Plan: docs/plans/ENG-02.md. Decisions: D-059 pace targets, D-060 worker home, D-061 travel legs stay computed. **Remaining in this module:** worst-case max-duration preview and the health sheet UI (B-019), MAPS (B-020), Prepare UI (B-021) — all downstream of B-018, which is blocked by B-015 → B-013 → OPEN-001. |
| Live & offline core | LIVE-01..04, OFFL-01..03 | see inventory | IN_PROGRESS | PLAN-01 | 2026-08-25 | **ENG-04 done early** (B-022 engine half), for the same reason as ENG-05 — it is pure and needs no content: `getNowNextLater` returns NOW / NEXT / LATER, the day's health state and leave-by, distinguishing an item in progress, a travel leg, free time, before-day and day-complete. Leave-by counts back through travel and the next item's buffer. 17 tests. Plan: docs/plans/ENG-04.md. **Remaining in B-022:** the A16 screen, Done / Running late / Stay longer, the end-of-day card, `start-day` and local reminders — all need B-019. OFFL-01..03 (Dexie) is B-023. |
| Adapt & engage (M2) | ADPT/NOTF/REPT-01,04/SHARE/ENG-05/PREP-02,03/PLAN-07 | see inventory | IN_PROGRESS | M1 | 2026-08-25 | **NOTF-01 done** (B-027's content-free half): `scheduleNotifications` + `applyWeeklyCap` in the engine (D-074, D-075) implement PRD F15's prepare deadlines, journey-tomorrow and leave-by with stable dedupe keys and i18n keys; `PushProvider` + the `web-push` adapter with `gone` separated from `failed` (D-076) and `pnpm push:keys` for VAPID. 31 tests. Plan: docs/plans/NOTF-01.md. **Remaining in B-027:** the subscribe prompt, in-app list and preference switches (need B-019), and the two Edge Functions (need a deployable project, B-025). **ENG-05 done early** (B-026 engine half), because it needs no content while every M1 item after B-017 waits on B-013: `evaluateChange` walks PRD F6's ladder (a)–(f) and stops at the first rung reaching Tight-or-better (D-067); `applyOption` applies absolute-target changes (D-066). FIXED is never moved or removed and PROTECTED never removed — asserted across all 10 trigger kinds. 45 tests. Plan: docs/plans/ENG-05.md. Found and fixed D-065: `scheduleDay` was overwriting edited buffers. **Remaining in B-026:** the Change Card sheet (A13), `journey_change_events`, and trigger wiring — all need the builder UI from B-019.  **B-026 done (ADPT-01..03):** the two halves that existed since ENG-05 are now wired. `POST /changes` evaluates a trigger and records the card; `POST /changes/:id` applies the option the traveler TAPPED — looked up by id in the persisted card, never recomputed, so they get what they were shown (D-121). `decided_at` makes a second tap a no-op instead of double-moving every item. Live's "Running late" and "Stay longer" now raise a trigger and open the Change Card; `no_impact` gets a quiet line and never a card (D-123). Option labels key on the ladder STEP so they cannot drift from the rung they describe (D-122). 9 pgTAP + 10 E2E, the important ones hitting the API directly — "never remove PROTECTED" is asserted across EVERY option, not just the recommended one. **Remaining in M2:** notification delivery (B-027), reports (B-028+).  **B-027 done (NOTF-01/03/04):** subscribe and unsubscribe routes, the seven switches with PRD F15's defaults, an in-app list, a scheduler that queues a journey's reminders on save, and a sender on **Vercel Cron rather than a Supabase Edge Function** — `web-push` is Node and the tested adapter already exists; a Deno reimplementation of VAPID signing fails silently when it is wrong (D-124). **A pgTAP assertion caught a real bug:** the scheduler used the request-scoped client, but `authenticated` has no INSERT on `notifications` by design, so it silently wrote nothing. It now reads as the traveler (RLS decides whose journey) and writes as service-role (only the system schedules) — D-125. The weekly cap is applied at scheduling time so a suppressed draft never becomes a row somebody later 'fixes' by sending (D-126). 12 pgTAP + 8 E2E. **Remaining in M2:** reports (B-028). **Carried:** PRD-NOTF-002's local offline leave-by → B-033. |
| Scale & trust (M3) | OPS-SRC-02..05/QUEUE-01,03,05..07/DYN/SRCH-03/OFFL-04,05/ADPT-04/REPT-02,05 | see inventory | NOT_STARTED | M2 | 2026-08-24 | |
| Launch polish (M4) | LANG/OPS-TRANS/CMPL/PERS/OPS-DASH/PRIV-03,04/REPT-03 | see inventory | NOT_STARTED | M3 | 2026-08-24 | |
| Growth (M5) | COLLAB/CONV/CIRC/PARTNER/NATIVE/OFFL-06 | see inventory | NOT_STARTED | M4 | 2026-08-24 | |
