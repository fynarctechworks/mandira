# Decision Log

Only decisions actually made in the PRD/TRD/founder responses or this foundation process. Format: ID · Date · Decision · Context · Reason · Alternatives · Consequences · Related requirements.

| ID | Date | Decision | Context / Reason | Alternatives considered | Consequences | Related |
|---|---|---|---|---|---|---|
| D-001 | 2026-08 | Next.js PWA for traveler + Next.js Ops; no native app in M1 | One ecosystem, founder's stack, no app-store gate, faster iteration | Expo RN + separate Ops | PWA constraints on iOS push/offline; architecture must not block later Expo shell | TRD-ARCH-001 |
| D-002 | 2026-08 | Supabase (Postgres) over Firebase | Knowledge model is deeply relational; trust records need joins/constraints | Firebase/Firestore | Own the offline layer (Dexie) instead of Firestore sync | TRD-DB-* |
| D-003 | 2026-08 | Monorepo pnpm+turbo with shared packages | Engine/UI/db shared across two apps | Two repos | Single CI; enforced import boundaries | TRD-ARCH-001 |
| D-004 | 2026-08 | Translatable content as `_i18n` jsonb columns | Simpler for AI tooling and queries | translations table | Locale-completeness reporting scans jsonb (Ops tooling accepts) | PRD-LANG-002 |
| D-005 | 2026-08 | Pure Journey Engine; KnowledgeBundle = Dexie snapshot = engine input | One deterministic codepath online/offline; offline replanning becomes wiring | Server-only engine | Invariant must be protected (Dexie versioning, no I/O in engine) | TRD-ARCH-002 |
| D-006 | 2026-08 | Gemini first behind Vercel AI SDK abstraction; grounding enforced in code | Cheapest reliable structured output; provider swap = env var | Claude/OpenAI primary | Must run te/hi eval before public launch (OPEN-004) | TRD-AI-* |
| D-007 | 2026-08 | Rate limiting via Postgres table, not Redis | One less service at this scale | Upstash | Swap path documented if contention appears | TRD-SEC-001 |
| D-008 | 2026-08 | OSM ecosystem (MapLibre+MapTiler tiles, ORS routing, Nominatim) behind provider abstraction; not ideological | Near-zero cost; abstraction allows Google later if Indian routing quality demands | Google Maps Platform | Routing quality risk logged (R-005); travel_estimates cache mandatory | INTEGRATIONS |
| D-009 | 2026-08 | Auth = magic link (Resend SMTP) + Google; no SMS in M1 | Zero SMS cost; model extensible to phone OTP later | Phone OTP primary | Resend domain setup required before auth works | PRD-ACCT-001 |
| D-010 | 2026-08 | Traveler reads only `v_published_*` views with aggregated trust jsonb | Structural enforcement of publish gate + trust visibility | App-level filtering | View shape = client contract; changes need version discipline | TRD-ARCH-004 |
| D-011 | 2026-08 | Search: tsvector first; pgvector hybrid at M3 | Semantic value requires content volume | pgvector day one | embedding columns created early (no later migration churn) | TRD-DB-005 |
| D-012 | 2026-08 | Offline M1 = read-only journey+knowledge+NOW/NEXT/LATER; outbox/replanning M3; tiles M5 | Progressive offline per founder | Full offline day one | Airplane-mode read test is an M1 gate | PRD-OFFL-* |
| D-013 | 2026-08 | Permanent exclusions: payments, reviews/social, ads, popularity ranking, auto-apply, AI-published facts, turn-by-turn | PRD §8 | — | Any future change = founder-level product decision | PRD §8 |
| D-014 | 2026-08 | Launch locales en/te/hi; te/hi content lands M4 | Audience assumption (founder may swap) | en-only launch | Locale-as-data requirement from M0 (strings externalised) | PRD-LANG-003 |
| D-015 | 2026-08 | Forward-only additive migrations; two-release column drops | Safe with instant Vercel rollback | Reversible down-migrations | Slight schema debt tolerated | TRD-DB-006 |
| D-016 | 2026-08 | Reports text-only until M4 photos | Avoid media/privacy surface early | Photos day one | reports bucket + media_id column exist from M0 (schema-complete) | PRD-REPT-005 |
| D-017 | 2026-08 | Supabase region ap-south-1 (Mumbai); pg_cron+Edge for jobs; no AWS/GCP infra | Latency to Indian users; free-tier fit | Multi-cloud | keepalive cron required on free tier | TRD-DEPL |
| D-018 | 2026-08-24 | Foundation docs created before any code; registry granularity = 132 requirements; status tracked per feature in PROJECT_STATUS.md | This project-foundation task | Track per-requirement status inline in registry | Two files must be updated together (rule in CLAUDE.md §6.11) | — |
| D-019 | 2026-08-24 | Staging Supabase project deferred to M3; previews use local/seed fixtures | Cost + solo simplicity | Staging from day one | E2E against prod-like env only from M3; acceptable given pilot size | TRD-DEPL-001 |
| D-020 | 2026-08-24 | Tailwind 4 preset ships as CSS (`@mandhira/config/tailwind/preset.css` with `@theme inline`), not a JS preset; tokens live in `@mandhira/ui/tokens.css` and apps import `tailwindcss` themselves | Tailwind 4 removed JS presets; `@import "tailwindcss"` cannot resolve from inside packages/ui under pnpm strict node_modules | tailwind.config.ts preset (not supported in v4); duplicating tokens per app | Both apps import `tailwindcss` then `@mandhira/ui/styles.css`; new shared UI source dirs must be added to each app's `@source` | TRD-ARCH-006, PRD-DSGN-001 |
| D-021 | 2026-08-25 | `packages/ui` components are presentational only: props are plain data, never DB/engine types. `ChangeCard`, `NowCard`, `ItemCard` ship their PRD-specified contract in B-002; wiring to real data happens in their own backlog items (B-019/B-022/B-026) | Lets the whole PRD §12.5 library land in M0 without inventing schema shapes ahead of B-005, and keeps the design system free of domain coupling | Defer the composite cards to their feature items (design system incomplete for most of M1); define provisional types now (would be invented requirements) | Feature items wire, never redesign; a component's props changing is a design-system decision, not a schema one | PRD-DSGN-003 |
| D-022 | 2026-08-25 | Ops data table and Ops side-by-side review (PRD §12.5 rows 14–15) are built in B-008 / B-012, not B-002 | Both need real column definitions and `@tanstack/react-table` wiring to be more than a shell; the backlog already assigns Ops tables to B-008 | Build empty shells now | PLAT-02 stays IN_PROGRESS until B-012; PRD-DSGN-003 is partially satisfied until then | PRD-DSGN-003 |
| D-023 | 2026-08-25 | Add `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` (dev-only) beyond the TRD §3 list | DoD requires tests for a11y-critical component behaviour (icon+text pairing, ARIA wiring, Change Card section order); asserting on class strings would not catch real regressions | Class-string assertions only; Playwright-only component coverage (far slower) | TRD §3 testing row should be amended to name them; jsdom pinned to ^26 because 30.x requires Node ≥22.22.2 and the dev machine runs 22.19.0 | TRD §3, PRD-DSGN-006 |
| D-024 | 2026-08-25 | Vitest runs serially (`fileParallelism: false`) and turbo runs at `--concurrency=2` | Parallel jsdom workers kill the pool on the dev machine (`ERR_IPC_CHANNEL_CLOSED` with forks; esbuild service death with threads), and 8 parallel `tsc` processes hit a native OOM (`node::Realloc` assertion) | Parallel locally + serial in CI (diverging environments); raising machine memory | Costs a few seconds at current suite size; revisit if the suite grows. Keeps local and CI identical | TRD-ARCH-006 |
| D-025 | 2026-08-25 | **OPEN-008 resolved** (founder delegated: "do the best"). PRD §12.1 light-mode palette revised so every text/background pair meets WCAG AA ≥4.5:1 in both modes. `brand.primary` #FF660E is **unchanged** and remains a fill colour; a new token `brand.primary.text` (#C64800 light / #FF7A30 dark) carries brand-coloured text and icons. `text.on.primary` and the PROTECTED chip text flip from white to brand ink #2B1A10 on the orange fill (5.69:1), which also makes light and dark modes consistent (dark already used ink-on-orange). Darkened: `text.tertiary` #9A908A→#7B706A (light) and #857B74→#908780 (dark), `status.comfortable` #2E7D4F→#2B7449, `status.tight` #B8860B→#856108, `status.at_risk` #D9702B→#A0511D, `status.info` #2F6FA8→#2D6AA1. `status.broken` unchanged. | A full audit of all 47 §12.1 pairings (over bg.surface, bg.canvas, raised surfaces, and each status colour's own 12% fill) found **17** sub-AA pairs in light mode and **2** in dark — far more than the 6 that happened to render. PRD §12.1 both declares the palette normative AND claims ≥4.5:1; the two cannot both hold, and PRD-DSGN-001/§12.8 make AA the binding requirement | Darken brand.primary to #CE4B00 so white text passes (rejected — that is no longer Mandhira Orange); keep white-on-orange and accept 2.93:1 (rejected — violates PRD-DSGN-001); enlarge button text to WCAG "large text" (rejected — PRD §12.2 fixes body at 16/500) | Hues preserved throughout; shifts are minimal (tightest pair now 4.50:1). Primary buttons and PROTECTED chips read dark-on-orange instead of white-on-orange — a visible change the founder should eyeball. `tests/e2e/axe-exceptions.ts` no longer allow-lists anything, so any future contrast failure is a real regression | PRD-DSGN-001, PRD-DSGN-006, PRD §12.1 |
| D-026 | 2026-08-25 | Local Supabase for Mandhira binds the **544xx** port range (API 54421, DB 54422, Studio 54423, Mailpit 54424, shadow 54420, pooler 54429) instead of the CLI defaults | Another project's Supabase stack (`Poultry_management-main`) is running on this machine and holds the default 5432x ports. Stopping someone else's long-running stack to free ports is not an acceptable side effect of building Mandhira | Stop the other stack (rejected — not ours to stop); run stacks alternately (rejected — friction every session) | Every local connection string uses 54421/54422; `supabase/config.toml` carries the values so no per-developer setup is needed. CI is unaffected (clean runner, defaults would also work) | TRD-DEPL-001 |
| D-027 | 2026-08-25 | Schema tests are **pgTAP** files under `supabase/tests/`, run by `supabase test db`, rather than a JS test hitting Postgres from vitest | Keeps schema assertions in the same language as the schema, needs no Postgres client dependency in the JS workspace, and gives transactional rollback per test file for free. The Supabase image already ships pgTAP 1.3.3 | A `pg`/`postgres` client + vitest integration tests (adds a dependency and a second connection story); trusting `db lint` alone (does not check semantics) | Two test commands: `pnpm test` (vitest, no DB) and `supabase test db` (pgTAP, needs Docker). CI runs them in separate jobs so the fast one is not gated on Docker | TRD-DB-001, TESTING_STRATEGY |
| D-028 | 2026-08-25 | `search_tsv` generated columns are built from an IMMUTABLE `i18n_text(jsonb)` helper that concatenates **all** locale values of an `_i18n` column, rather than naming locales explicitly | Hardcoding `name_i18n->>'en'`-style keys would mean a migration on every searchable table each time a locale is added, and te/hi content would be silently unsearchable until someone remembered | Per-locale tsvector columns (n× storage, n× migrations); casting the whole jsonb to text (indexes the locale keys as tokens, polluting search) | Adding a locale is a `locales` row and nothing else. `string_agg` is ordered by key so the function is genuinely deterministic, which IMMUTABLE — and therefore use in a generated column — requires | TRD-DB-005, PRD-LANG-002 |
| D-029 | 2026-08-25 | The `v_published_*` views are **definer's-rights** (no `security_invoker`), and `anon` is granted SELECT on the views only — never on the base tables | D-010 makes the views the structural publish gate. Definer's rights let them read past base-table RLS and return exactly the published subset, so no application code path can bypass the gate by querying a table directly or forgetting a `where` clause | `security_invoker = true` plus per-table traveler SELECT policies (moves the gate into ~10 policy definitions that must each stay correct); app-level filtering (rejected by D-010) | Two independent layers now protect drafts: no grant, plus RLS. **If B-006 ever adds traveler SELECT policies to knowledge base tables, this decision must be revisited together with them** | TRD-ARCH-004, PRD-KNOW-003 |
| D-030 | 2026-08-25 | A critical field with **no** trust record fails the publish gate, exactly like one below `human_reviewed` | PRD F9's acceptance is "100% of published critical fields render a badge". A field with no trust record has no badge to render, so admitting it would break that guarantee silently | Treat a missing record as ungated (simpler, but publishes unbadgeable facts); require records only where present | Ops must attach trust to every critical field before a place/experience becomes visible — deliberate friction that makes the trust model real rather than optional. Note `>= human_reviewed` also admits `disputed`, which is the literal TRD rule and consistent with PRD F9: disputed facts carry `conflict_flag` and surface as "Check locally" rather than vanishing | PRD-KNOW-002, PRD-KNOW-003 |
| D-031 | 2026-08-25 | Zod uses `z.guid()` (permissive 8-4-4-4-12 hex) for id fields, not `z.uuid()` | Zod 4's `z.uuid()` enforces RFC 4122 version/variant bits, but Postgres `uuid` columns accept any 128-bit value. Using the strict form would reject ids the database happily stores — a validation layer that is stricter than the column it mirrors rejects legitimate data | `z.uuid()` (rejects non-RFC ids); a hand-rolled regex (same thing, less legible) | API validation now matches column semantics exactly. Caught by a fixture using `1111…` ids, which `z.uuid()` rejected | TRD-API-001, TRD-DB-001 |
| D-032 | 2026-08-25 | Hand-written Zod enums are pinned to the generated database enums by **compile-time** assertions (`expectTypeOf<Member<...>>().toEqualTypeOf<Enums<...>>()`), not runtime tests | The enum lists are duplicated by necessity — Zod needs literals, the DB owns the truth. A compile-time assertion fails during `pnpm typecheck` the moment a migration changes an enum, rather than at runtime in a route handler. Verified to actually fail by removing a member and confirming typecheck breaks | Runtime test querying `pg_enum` (needs a database for a unit test); trusting review to catch drift | Adding an enum value means updating `primitives.ts` in the same commit as the migration, or the build fails. `packages/db/types.ts` is prettier-ignored so the generator's output stays byte-identical for the CI staleness check | TRD-DB-002, TRD-API-001 |
| D-033 | 2026-08-25 | Three tables the §4.9 matrix does not enumerate are opened deliberately: `media_assets` (public read of non-deleted rows, with the `uploaded_by` column withheld by a column-level grant), `entity_media` (public read), and `feature_flags` (public read, admin write). All other unlisted tables stay deny-by-default | §4.9 lists table *groups*, not every table. Travelers cannot render any published content without resolving media, and flags gate client UI — leaving both closed would break B-014/B-015. `uploaded_by` is the only identity in `media_assets`, and a column grant makes it unreachable rather than merely unselected | Add a `v_published_media` view (invents a tenth view the TRD does not list); leave closed and serve via service role (breaks the views-only architecture for images); expose `uploaded_by` too (leaks which operator uploaded what) | Public read surface is now exactly five policies, pinned by a pgTAP allowlist test so a future migration cannot widen it unnoticed. **Still unresolved:** `accessibility_records`, `route_places`, `circuits`, `destination_links` and `live_feed_readings` carry traveler-relevant data but have neither a policy nor a published view — see OPEN-009 | TRD-DB-003, PRD-KNOW-003 |
| D-034 | 2026-08-25 | `user_reports.user_id` is withheld from `authenticated` by column-level grant rather than exposing reports to Ops through a `reporter_hash` view | §4.9 requires Ops see "reporter_hash only". A view would have worked, but a column grant is stronger and simpler: RLS policy expressions are evaluated by the system and are not subject to column grants, so rows still filter correctly by `user_id` while *nobody* — operator or reporter — can select the column | A `v_ops_user_reports` view plus separate grants (two objects to keep in sync); trusting the Ops query layer not to select it (UI hiding is never a control, CLAUDE.md §4) | Ops can triage and resolve reports without ever seeing who filed them. Any future need for reporter identity must go through a service-role path and a DECISION_LOG entry | PRD-PRIV-002, TRD-DB-003 |
| D-035 | 2026-08-25 | `entity_versions` and `audit_log` have SELECT policies but **no** INSERT/UPDATE/DELETE policy for any role, including admin | Rows arrive only via the SECURITY DEFINER `record_entity_version()` trigger or the service role. An audit trail an operator can edit is not an audit trail, and "admin" is exactly the role whose actions most need to be non-repudiable | Allow admin writes (defeats the purpose); allow inserts so application code can write directly (bypasses the trigger, producing inconsistent history) | Ops tooling that needs to annotate history must add a separate table rather than mutating these. Covered by two pgTAP denial tests | TRD-DB-004, PRD-OPS-WF-009 |
| D-036 | 2026-08-25 | Profiles are created by a database trigger on `auth.users`, not in the sign-in callback | A callback only covers the route it lives in. A trigger covers every path that can ever create a user — magic link, Google, the Supabase dashboard, a seed script, a future provider — so no route can forget. The trigger also validates the requested locale against `locales` and falls back to `en`, because an unknown locale would violate the FK and fail the entire sign-up | Create the profile in the auth callback (misses every other path); create it lazily on first read (every reader must then handle its absence) | AUTH-04 is satisfied by construction. Any future user-creation path gets a profile for free | AUTH-04, PRD-ACCT-002 |
| D-037 | 2026-08-25 | Separation of duties (PRD-OPS-WF-009) is enforced by a `review_tasks` trigger comparing `completed_by` to the entity's last `entity_versions.changed_by`, not by route-handler logic | AUTHORIZATION_MODEL requires workflow constraints at the database layer. A check that lives only in a route handler is one refactor — or one new code path — away from being bypassed, and self-approval is precisely the failure this rule exists to prevent | Check in the approve route handler only; check in the Ops UI (UI is never a control) | Any path that completes an approve task is subject to it, including future Edge Functions and manual SQL. Covered by three pgTAP cases | AUTH-05, PRD-OPS-WF-009 |
| D-038 | 2026-08-25 | E2E tests drive the **real** magic-link flow via the local mail catcher, and Playwright binds dedicated high ports (3986/3987) rather than 3000/3001 | A hand-built session cookie was silently rejected by `@supabase/ssr` while the app was perfectly healthy — proving only that the test could build a cookie. Reading the link out of Mailpit exercises what a user actually does. Separately, a probe against :3001 was answered by an UNRELATED application already running on this machine: a suite that silently tests someone else's app is worse than one that fails | Fabricate cookies (brittle and unconvincing); stub auth in tests (tests the stub); keep the default ports (collides with other local projects) | Local E2E requires the Supabase stack running. Ports are overridable via `MANDHIRA_WEB_PORT`/`MANDHIRA_OPS_PORT`. Local `additional_redirect_urls` uses localhost wildcards so any free port works — production must list exact URLs | AUTH-01, TESTING_STRATEGY |
| D-039 | 2026-08-25 | `brand.primary.text` darkened again, #C64800 → **#C14600**, and its verification set extended to include `brand.primary.soft` | The D-025 audit checked brand-coloured text against `bg.surface` and `bg.canvas`, but not against `brand.primary.soft` — a pairing that did not exist until B-008 used it for the active nav item. Measured 4.33:1 there, below AA. Caught by the axe check on the authenticated Ops shell, not by review | Use a different active-nav treatment (avoids the pairing rather than fixing the token, and the pairing will recur); accept 4.33:1 | Now verified against surface, canvas and soft, tightest 4.52:1. **Lesson recorded:** a token audit must cover the composites a token will sit on, not just the base backgrounds — new fills introduce new pairs, so the axe check on every new screen is doing real work | PRD-DSGN-001, PRD-DSGN-006 |
| D-040 | 2026-08-25 | `@tanstack/react-table` pinned to **^8** although 9.1.2 is `latest` | v9 replaced the v8 API (`useReactTable`/`getCoreRowModel` → a features-based `useTable`) and ships `useLegacyTable` purely as a deprecated compatibility shim. TRD §3 pairs this library with "shadcn Table", whose patterns are all v8, and B-009…B-012 will build many tables — a stable, well-documented API is worth more here than being on the newest major | Adopt v9's new API (little community material, and I would be writing foundational code against an API I had just met); use `useLegacyTable` (writing against a deprecated shim on day one) | Revisit as a deliberate migration once v9 patterns settle. The wrapper is thin, so the blast radius is one file | TRD-ARCH-006, PRD-DSGN-003 |
| D-041 | 2026-08-25 | Ops navigation lists **all 22** screens, with unbuilt ones disabled and labelled by the milestone that delivers them, rather than showing only what exists | The screen map is itself useful to an operator learning the platform, and searching the palette for "conflicts" should find it and say "M3" rather than return nothing as though it did not exist. It also keeps the backlog honest — the nav cannot quietly diverge from what is actually shipped | Show only built screens (nav silently grows, and operators cannot see where things will live); build 22 placeholder pages (empty screens pretending to be features) | A test asserts every unbuilt entry carries a `comingIn` label, and that O01–O22 are covered exactly once. Sidebar and palette render from one model so they cannot disagree | PRD-OPS-CNT-001 |
| D-042 | 2026-08-25 | E2E signs in once in a `ops-setup` project and shares `storageState`; the auth-gate spec opts out and uses a **separate seeded account** (`ops-e2e@mandhira.local`, reviewer) | GoTrue rate-limits magic-link sends per address (local default 2/hour), so a suite where every test requests its own link fails as it grows. Worse, two specs signing in as the same person race for each other's links — the mail catcher returns the newest message, so one test consumes the other's. Separate mailboxes remove the race entirely, and the gate spec proves a non-admin Ops role gets in | Share one account and serialise the specs (slow, and the race returns the moment someone re-parallelises); fabricate cookies (rejected in D-038) | Local `email_sent` raised to 200/hour — mail goes to Mailpit, so there is nothing to abuse; production keeps provider defaults. The seed now creates two Ops accounts | AUTH-01, TESTING_STRATEGY |
| D-043 | 2026-08-25 | Ops writes use **Server Actions** wrapped in `opsAction()`, not API route handlers with `withApi` | The Ops app is entirely form-driven, and TRD §3 permits both. `opsAction()` provides the same three guarantees TRD-API-001 requires of `withApi` — server-resolved session, role check, Zod validation, and the `{ok,data}｜{ok,error}` envelope — with field-level errors mapped for inline display. The role check is repeated inside the action even though the layout already gated the page, because a Server Action is an addressable endpoint, not merely a function the page calls | Route handlers + `withApi` (more moving parts for a form post, and a second serialisation layer); trusting the layout gate alone (a Server Action can be invoked directly) | `withApi` is still owed for genuine JSON APIs (traveler routes, B-018+). Both wrappers must keep the same envelope shape so clients see one contract | TRD-API-001, AUTHORIZATION_MODEL |
| D-044 | 2026-08-25 | PostGIS coordinates are exposed to PostgREST via **computed columns** (`latitude(destinations)`, `longitude(places)`, …) in `0010_geo_accessors.sql` | PostgREST serialises `geography` as EWKB hex, not GeoJSON, so client code reading `centre.coordinates` gets nothing — **silently**. A saved pin simply came back blank, which the B-009 editor round-trip test caught. Computed columns make `select("*, latitude, longitude")` return real numbers with no hex parsing anywhere in the app | Parse EWKB hex client-side (fragile, and repeated per table); add a view per table (duplicates the column list); store redundant lat/lng columns (denormalises, and the geography column is what PostGIS indexes) | Writes still use WKT, which Postgres casts natively. Any future geography column needs its own accessor pair — worth remembering, because the failure mode is silence, not an error | TRD-DB-005 |
| D-045 | 2026-08-25 | The centre/location picker ships **without** the MapLibre map; coordinates are set by geocoding search or direct entry | TRD §11.2 Day 5 specifies a MapLibre picker, but the map needs a tile source and `NEXT_PUBLIC_MAPTILER_KEY` is not provisioned (OPEN_ITEMS). Shipping a map component that cannot be run is a component that cannot be tested, and B-020 is the item that owns mapping work and can verify it against real tiles | Ship an untestable map behind a key check; use keyless OSM raster tiles (their usage policy is not something to assume); block B-009 on the key (blocks all knowledge entry on an unrelated account) | Operators can set pins today via Nominatim search or decimal degrees. The map lands in B-020. `maplibre-gl`/`react-map-gl` were installed then removed rather than left unused | TRD-DB-005, PRD-OPS-CNT-001 |
