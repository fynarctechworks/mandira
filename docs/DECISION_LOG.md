# Decision Log

Only decisions actually made in the PRD/TRD/founder responses or this foundation process. Format: ID · Date · Decision · Context · Reason · Alternatives · Consequences · Related requirements.

| ID | Date | Decision | Context / Reason | Alternatives considered | Consequences | Related |
|---|---|---|---|---|---|---|
| D-001 | 2026-08 | Next.js PWA for traveler + Next.js Ops; no native app in M1 | One ecosystem, founder's stack, no app-store gate, faster iteration | Expo RN + separate Ops | PWA constraints on iOS push/offline; architecture must not block later Expo shell | TRD-ARCH-001 |
| D-002 | 2026-08 | Supabase (Postgres) over Firebase | Knowledge model is deeply relational; trust records need joins/constraints | Firebase/Firestore | Own the offline layer (Dexie) instead of Firestore sync | TRD-DB-* |
| D-003 | 2026-08 | Monorepo pnpm+turbo with shared packages | Engine/UI/db shared across two apps | Two repos | Single CI; enforced import boundaries | TRD-ARCH-001 |
| D-004 | 2026-08 | Translatable content as `_i18n` jsonb columns | Simpler for AI tooling and queries | translations table | Locale-completeness reporting scans jsonb (Ops tooling accepts) | PRD-LANG-002 |
| D-005 | 2026-08 | Pure Journey Engine; KnowledgeBundle = Dexie snapshot = engine input | One deterministic codepath online/offline; offline replanning becomes wiring | Server-only engine | Invariant must be protected (Dexie versioning, no I/O in engine) | TRD-ARCH-002 |
| D-006 | 2026-08 | Gemini first behind Vercel AI SDK abstraction; grounding enforced in code | Cheapest reliable structured output; provider swap = env var | Claude/OpenAI primary | Must run te/hi eval before public launch (OPEN-004) | TRD-AI-* |
| D-007 | 2026-08 | Rate limiting via Postgres table, not Redis | One less service at this scale | Upstash | Swap path documented if contention appears | TRD-SEC-001 |
| D-008 | 2026-08 | OSM ecosystem (MapLibre+MapTiler tiles, ORS routing, Nominatim) behind provider abstraction; not ideological | Near-zero cost; abstraction allows Google later if Indian routing quality demands | Google Maps Platform | Routing quality risk logged (R-005); travel_estimates cache mandatory | INTEGRATIONS |
| D-009 | 2026-08 | Auth = magic link (Resend SMTP) + Google; no SMS in M1 | Zero SMS cost; model extensible to phone OTP later | Phone OTP primary | Resend domain setup required before auth works | PRD-ACCT-001 |
| D-010 | 2026-08 | Traveler reads only `v_published_*` views with aggregated trust jsonb | Structural enforcement of publish gate + trust visibility | App-level filtering | View shape = client contract; changes need version discipline | TRD-ARCH-004 |
| D-011 | 2026-08 | Search: tsvector first; pgvector hybrid at M3 | Semantic value requires content volume | pgvector day one | embedding columns created early (no later migration churn) | TRD-DB-005 |
| D-012 | 2026-08 | Offline M1 = read-only journey+knowledge+NOW/NEXT/LATER; outbox/replanning M3; tiles M5 | Progressive offline per founder | Full offline day one | Airplane-mode read test is an M1 gate | PRD-OFFL-* |
| D-013 | 2026-08 | Permanent exclusions: payments, reviews/social, ads, popularity ranking, auto-apply, AI-published facts, turn-by-turn | PRD §8 | — | Any future change = founder-level product decision | PRD §8 |
| D-014 | 2026-08 | Launch locales en/te/hi; te/hi content lands M4 | Audience assumption (founder may swap) | en-only launch | Locale-as-data requirement from M0 (strings externalised) | PRD-LANG-003 |
| D-015 | 2026-08 | Forward-only additive migrations; two-release column drops | Safe with instant Vercel rollback | Reversible down-migrations | Slight schema debt tolerated | TRD-DB-006 |
| D-016 | 2026-08 | Reports text-only until M4 photos | Avoid media/privacy surface early | Photos day one | reports bucket + media_id column exist from M0 (schema-complete) | PRD-REPT-005 |
| D-017 | 2026-08 | Supabase region ap-south-1 (Mumbai); pg_cron+Edge for jobs; no AWS/GCP infra | Latency to Indian users; free-tier fit | Multi-cloud | keepalive cron required on free tier | TRD-DEPL |
| D-018 | 2026-08-24 | Foundation docs created before any code; registry granularity = 132 requirements; status tracked per feature in PROJECT_STATUS.md | This project-foundation task | Track per-requirement status inline in registry | Two files must be updated together (rule in CLAUDE.md §6.11) | — |
| D-019 | 2026-08-24 | Staging Supabase project deferred to M3; previews use local/seed fixtures | Cost + solo simplicity | Staging from day one | E2E against prod-like env only from M3; acceptable given pilot size | TRD-DEPL-001 |
| D-020 | 2026-08-24 | Tailwind 4 preset ships as CSS (`@mandhira/config/tailwind/preset.css` with `@theme inline`), not a JS preset; tokens live in `@mandhira/ui/tokens.css` and apps import `tailwindcss` themselves | Tailwind 4 removed JS presets; `@import "tailwindcss"` cannot resolve from inside packages/ui under pnpm strict node_modules | tailwind.config.ts preset (not supported in v4); duplicating tokens per app | Both apps import `tailwindcss` then `@mandhira/ui/styles.css`; new shared UI source dirs must be added to each app's `@source` | TRD-ARCH-006, PRD-DSGN-001 |
| D-021 | 2026-08-25 | `packages/ui` components are presentational only: props are plain data, never DB/engine types. `ChangeCard`, `NowCard`, `ItemCard` ship their PRD-specified contract in B-002; wiring to real data happens in their own backlog items (B-019/B-022/B-026) | Lets the whole PRD §12.5 library land in M0 without inventing schema shapes ahead of B-005, and keeps the design system free of domain coupling | Defer the composite cards to their feature items (design system incomplete for most of M1); define provisional types now (would be invented requirements) | Feature items wire, never redesign; a component's props changing is a design-system decision, not a schema one | PRD-DSGN-003 |
| D-022 | 2026-08-25 | Ops data table and Ops side-by-side review (PRD §12.5 rows 14–15) are built in B-008 / B-012, not B-002 | Both need real column definitions and `@tanstack/react-table` wiring to be more than a shell; the backlog already assigns Ops tables to B-008 | Build empty shells now | PLAT-02 stays IN_PROGRESS until B-012; PRD-DSGN-003 is partially satisfied until then | PRD-DSGN-003 |
| D-023 | 2026-08-25 | Add `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` (dev-only) beyond the TRD §3 list | DoD requires tests for a11y-critical component behaviour (icon+text pairing, ARIA wiring, Change Card section order); asserting on class strings would not catch real regressions | Class-string assertions only; Playwright-only component coverage (far slower) | TRD §3 testing row should be amended to name them; jsdom pinned to ^26 because 30.x requires Node ≥22.22.2 and the dev machine runs 22.19.0 | TRD §3, PRD-DSGN-006 |
| D-024 | 2026-08-25 | Vitest runs serially (`fileParallelism: false`) and turbo runs at `--concurrency=2` | Parallel jsdom workers kill the pool on the dev machine (`ERR_IPC_CHANNEL_CLOSED` with forks; esbuild service death with threads), and 8 parallel `tsc` processes hit a native OOM (`node::Realloc` assertion) | Parallel locally + serial in CI (diverging environments); raising machine memory | Costs a few seconds at current suite size; revisit if the suite grows. Keeps local and CI identical | TRD-ARCH-006 |
| D-025 | 2026-08-25 | **OPEN-008 resolved** (founder delegated: "do the best"). PRD §12.1 light-mode palette revised so every text/background pair meets WCAG AA ≥4.5:1 in both modes. `brand.primary` #FF660E is **unchanged** and remains a fill colour; a new token `brand.primary.text` (#C64800 light / #FF7A30 dark) carries brand-coloured text and icons. `text.on.primary` and the PROTECTED chip text flip from white to brand ink #2B1A10 on the orange fill (5.69:1), which also makes light and dark modes consistent (dark already used ink-on-orange). Darkened: `text.tertiary` #9A908A→#7B706A (light) and #857B74→#908780 (dark), `status.comfortable` #2E7D4F→#2B7449, `status.tight` #B8860B→#856108, `status.at_risk` #D9702B→#A0511D, `status.info` #2F6FA8→#2D6AA1. `status.broken` unchanged. | A full audit of all 47 §12.1 pairings (over bg.surface, bg.canvas, raised surfaces, and each status colour's own 12% fill) found **17** sub-AA pairs in light mode and **2** in dark — far more than the 6 that happened to render. PRD §12.1 both declares the palette normative AND claims ≥4.5:1; the two cannot both hold, and PRD-DSGN-001/§12.8 make AA the binding requirement | Darken brand.primary to #CE4B00 so white text passes (rejected — that is no longer Mandhira Orange); keep white-on-orange and accept 2.93:1 (rejected — violates PRD-DSGN-001); enlarge button text to WCAG "large text" (rejected — PRD §12.2 fixes body at 16/500) | Hues preserved throughout; shifts are minimal (tightest pair now 4.50:1). Primary buttons and PROTECTED chips read dark-on-orange instead of white-on-orange — a visible change the founder should eyeball. `tests/e2e/axe-exceptions.ts` no longer allow-lists anything, so any future contrast failure is a real regression | PRD-DSGN-001, PRD-DSGN-006, PRD §12.1 |
| D-026 | 2026-08-25 | Local Supabase for Mandhira binds the **544xx** port range (API 54421, DB 54422, Studio 54423, Mailpit 54424, shadow 54420, pooler 54429) instead of the CLI defaults | Another project's Supabase stack (`Poultry_management-main`) is running on this machine and holds the default 5432x ports. Stopping someone else's long-running stack to free ports is not an acceptable side effect of building Mandhira | Stop the other stack (rejected — not ours to stop); run stacks alternately (rejected — friction every session) | Every local connection string uses 54421/54422; `supabase/config.toml` carries the values so no per-developer setup is needed. CI is unaffected (clean runner, defaults would also work) | TRD-DEPL-001 |
| D-027 | 2026-08-25 | Schema tests are **pgTAP** files under `supabase/tests/`, run by `supabase test db`, rather than a JS test hitting Postgres from vitest | Keeps schema assertions in the same language as the schema, needs no Postgres client dependency in the JS workspace, and gives transactional rollback per test file for free. The Supabase image already ships pgTAP 1.3.3 | A `pg`/`postgres` client + vitest integration tests (adds a dependency and a second connection story); trusting `db lint` alone (does not check semantics) | Two test commands: `pnpm test` (vitest, no DB) and `supabase test db` (pgTAP, needs Docker). CI runs them in separate jobs so the fast one is not gated on Docker | TRD-DB-001, TESTING_STRATEGY |
| D-028 | 2026-08-25 | `search_tsv` generated columns are built from an IMMUTABLE `i18n_text(jsonb)` helper that concatenates **all** locale values of an `_i18n` column, rather than naming locales explicitly | Hardcoding `name_i18n->>'en'`-style keys would mean a migration on every searchable table each time a locale is added, and te/hi content would be silently unsearchable until someone remembered | Per-locale tsvector columns (n× storage, n× migrations); casting the whole jsonb to text (indexes the locale keys as tokens, polluting search) | Adding a locale is a `locales` row and nothing else. `string_agg` is ordered by key so the function is genuinely deterministic, which IMMUTABLE — and therefore use in a generated column — requires | TRD-DB-005, PRD-LANG-002 |
| D-029 | 2026-08-25 | The `v_published_*` views are **definer's-rights** (no `security_invoker`), and `anon` is granted SELECT on the views only — never on the base tables | D-010 makes the views the structural publish gate. Definer's rights let them read past base-table RLS and return exactly the published subset, so no application code path can bypass the gate by querying a table directly or forgetting a `where` clause | `security_invoker = true` plus per-table traveler SELECT policies (moves the gate into ~10 policy definitions that must each stay correct); app-level filtering (rejected by D-010) | Two independent layers now protect drafts: no grant, plus RLS. **If B-006 ever adds traveler SELECT policies to knowledge base tables, this decision must be revisited together with them** | TRD-ARCH-004, PRD-KNOW-003 |
| D-030 | 2026-08-25 | A critical field with **no** trust record fails the publish gate, exactly like one below `human_reviewed` | PRD F9's acceptance is "100% of published critical fields render a badge". A field with no trust record has no badge to render, so admitting it would break that guarantee silently | Treat a missing record as ungated (simpler, but publishes unbadgeable facts); require records only where present | Ops must attach trust to every critical field before a place/experience becomes visible — deliberate friction that makes the trust model real rather than optional. Note `>= human_reviewed` also admits `disputed`, which is the literal TRD rule and consistent with PRD F9: disputed facts carry `conflict_flag` and surface as "Check locally" rather than vanishing | PRD-KNOW-002, PRD-KNOW-003 |
| D-031 | 2026-08-25 | Zod uses `z.guid()` (permissive 8-4-4-4-12 hex) for id fields, not `z.uuid()` | Zod 4's `z.uuid()` enforces RFC 4122 version/variant bits, but Postgres `uuid` columns accept any 128-bit value. Using the strict form would reject ids the database happily stores — a validation layer that is stricter than the column it mirrors rejects legitimate data | `z.uuid()` (rejects non-RFC ids); a hand-rolled regex (same thing, less legible) | API validation now matches column semantics exactly. Caught by a fixture using `1111…` ids, which `z.uuid()` rejected | TRD-API-001, TRD-DB-001 |
| D-032 | 2026-08-25 | Hand-written Zod enums are pinned to the generated database enums by **compile-time** assertions (`expectTypeOf<Member<...>>().toEqualTypeOf<Enums<...>>()`), not runtime tests | The enum lists are duplicated by necessity — Zod needs literals, the DB owns the truth. A compile-time assertion fails during `pnpm typecheck` the moment a migration changes an enum, rather than at runtime in a route handler. Verified to actually fail by removing a member and confirming typecheck breaks | Runtime test querying `pg_enum` (needs a database for a unit test); trusting review to catch drift | Adding an enum value means updating `primitives.ts` in the same commit as the migration, or the build fails. `packages/db/types.ts` is prettier-ignored so the generator's output stays byte-identical for the CI staleness check | TRD-DB-002, TRD-API-001 |
| D-033 | 2026-08-25 | Three tables the §4.9 matrix does not enumerate are opened deliberately: `media_assets` (public read of non-deleted rows, with the `uploaded_by` column withheld by a column-level grant), `entity_media` (public read), and `feature_flags` (public read, admin write). All other unlisted tables stay deny-by-default | §4.9 lists table *groups*, not every table. Travelers cannot render any published content without resolving media, and flags gate client UI — leaving both closed would break B-014/B-015. `uploaded_by` is the only identity in `media_assets`, and a column grant makes it unreachable rather than merely unselected | Add a `v_published_media` view (invents a tenth view the TRD does not list); leave closed and serve via service role (breaks the views-only architecture for images); expose `uploaded_by` too (leaks which operator uploaded what) | Public read surface is now exactly five policies, pinned by a pgTAP allowlist test so a future migration cannot widen it unnoticed. **Still unresolved:** `accessibility_records`, `route_places`, `circuits`, `destination_links` and `live_feed_readings` carry traveler-relevant data but have neither a policy nor a published view — see OPEN-009 | TRD-DB-003, PRD-KNOW-003 |
| D-034 | 2026-08-25 | `user_reports.user_id` is withheld from `authenticated` by column-level grant rather than exposing reports to Ops through a `reporter_hash` view | §4.9 requires Ops see "reporter_hash only". A view would have worked, but a column grant is stronger and simpler: RLS policy expressions are evaluated by the system and are not subject to column grants, so rows still filter correctly by `user_id` while *nobody* — operator or reporter — can select the column | A `v_ops_user_reports` view plus separate grants (two objects to keep in sync); trusting the Ops query layer not to select it (UI hiding is never a control, CLAUDE.md §4) | Ops can triage and resolve reports without ever seeing who filed them. Any future need for reporter identity must go through a service-role path and a DECISION_LOG entry | PRD-PRIV-002, TRD-DB-003 |
| D-035 | 2026-08-25 | `entity_versions` and `audit_log` have SELECT policies but **no** INSERT/UPDATE/DELETE policy for any role, including admin | Rows arrive only via the SECURITY DEFINER `record_entity_version()` trigger or the service role. An audit trail an operator can edit is not an audit trail, and "admin" is exactly the role whose actions most need to be non-repudiable | Allow admin writes (defeats the purpose); allow inserts so application code can write directly (bypasses the trigger, producing inconsistent history) | Ops tooling that needs to annotate history must add a separate table rather than mutating these. Covered by two pgTAP denial tests | TRD-DB-004, PRD-OPS-WF-009 |
| D-036 | 2026-08-25 | Profiles are created by a database trigger on `auth.users`, not in the sign-in callback | A callback only covers the route it lives in. A trigger covers every path that can ever create a user — magic link, Google, the Supabase dashboard, a seed script, a future provider — so no route can forget. The trigger also validates the requested locale against `locales` and falls back to `en`, because an unknown locale would violate the FK and fail the entire sign-up | Create the profile in the auth callback (misses every other path); create it lazily on first read (every reader must then handle its absence) | AUTH-04 is satisfied by construction. Any future user-creation path gets a profile for free | AUTH-04, PRD-ACCT-002 |
| D-037 | 2026-08-25 | Separation of duties (PRD-OPS-WF-009) is enforced by a `review_tasks` trigger comparing `completed_by` to the entity's last `entity_versions.changed_by`, not by route-handler logic | AUTHORIZATION_MODEL requires workflow constraints at the database layer. A check that lives only in a route handler is one refactor — or one new code path — away from being bypassed, and self-approval is precisely the failure this rule exists to prevent | Check in the approve route handler only; check in the Ops UI (UI is never a control) | Any path that completes an approve task is subject to it, including future Edge Functions and manual SQL. Covered by three pgTAP cases | AUTH-05, PRD-OPS-WF-009 |
| D-038 | 2026-08-25 | E2E tests drive the **real** magic-link flow via the local mail catcher, and Playwright binds dedicated high ports (3986/3987) rather than 3000/3001 | A hand-built session cookie was silently rejected by `@supabase/ssr` while the app was perfectly healthy — proving only that the test could build a cookie. Reading the link out of Mailpit exercises what a user actually does. Separately, a probe against :3001 was answered by an UNRELATED application already running on this machine: a suite that silently tests someone else's app is worse than one that fails | Fabricate cookies (brittle and unconvincing); stub auth in tests (tests the stub); keep the default ports (collides with other local projects) | Local E2E requires the Supabase stack running. Ports are overridable via `MANDHIRA_WEB_PORT`/`MANDHIRA_OPS_PORT`. Local `additional_redirect_urls` uses localhost wildcards so any free port works — production must list exact URLs | AUTH-01, TESTING_STRATEGY |
| D-039 | 2026-08-25 | `brand.primary.text` darkened again, #C64800 → **#C14600**, and its verification set extended to include `brand.primary.soft` | The D-025 audit checked brand-coloured text against `bg.surface` and `bg.canvas`, but not against `brand.primary.soft` — a pairing that did not exist until B-008 used it for the active nav item. Measured 4.33:1 there, below AA. Caught by the axe check on the authenticated Ops shell, not by review | Use a different active-nav treatment (avoids the pairing rather than fixing the token, and the pairing will recur); accept 4.33:1 | Now verified against surface, canvas and soft, tightest 4.52:1. **Lesson recorded:** a token audit must cover the composites a token will sit on, not just the base backgrounds — new fills introduce new pairs, so the axe check on every new screen is doing real work | PRD-DSGN-001, PRD-DSGN-006 |
| D-040 | 2026-08-25 | `@tanstack/react-table` pinned to **^8** although 9.1.2 is `latest` | v9 replaced the v8 API (`useReactTable`/`getCoreRowModel` → a features-based `useTable`) and ships `useLegacyTable` purely as a deprecated compatibility shim. TRD §3 pairs this library with "shadcn Table", whose patterns are all v8, and B-009…B-012 will build many tables — a stable, well-documented API is worth more here than being on the newest major | Adopt v9's new API (little community material, and I would be writing foundational code against an API I had just met); use `useLegacyTable` (writing against a deprecated shim on day one) | Revisit as a deliberate migration once v9 patterns settle. The wrapper is thin, so the blast radius is one file | TRD-ARCH-006, PRD-DSGN-003 |
| D-041 | 2026-08-25 | Ops navigation lists **all 22** screens, with unbuilt ones disabled and labelled by the milestone that delivers them, rather than showing only what exists | The screen map is itself useful to an operator learning the platform, and searching the palette for "conflicts" should find it and say "M3" rather than return nothing as though it did not exist. It also keeps the backlog honest — the nav cannot quietly diverge from what is actually shipped | Show only built screens (nav silently grows, and operators cannot see where things will live); build 22 placeholder pages (empty screens pretending to be features) | A test asserts every unbuilt entry carries a `comingIn` label, and that O01–O22 are covered exactly once. Sidebar and palette render from one model so they cannot disagree | PRD-OPS-CNT-001 |
| D-042 | 2026-08-25 | E2E signs in once in a `ops-setup` project and shares `storageState`; the auth-gate spec opts out and uses a **separate seeded account** (`ops-e2e@mandhira.local`, reviewer) | GoTrue rate-limits magic-link sends per address (local default 2/hour), so a suite where every test requests its own link fails as it grows. Worse, two specs signing in as the same person race for each other's links — the mail catcher returns the newest message, so one test consumes the other's. Separate mailboxes remove the race entirely, and the gate spec proves a non-admin Ops role gets in | Share one account and serialise the specs (slow, and the race returns the moment someone re-parallelises); fabricate cookies (rejected in D-038) | Local `email_sent` raised to 200/hour — mail goes to Mailpit, so there is nothing to abuse; production keeps provider defaults. The seed now creates two Ops accounts | AUTH-01, TESTING_STRATEGY |
| D-043 | 2026-08-25 | Ops writes use **Server Actions** wrapped in `opsAction()`, not API route handlers with `withApi` | The Ops app is entirely form-driven, and TRD §3 permits both. `opsAction()` provides the same three guarantees TRD-API-001 requires of `withApi` — server-resolved session, role check, Zod validation, and the `{ok,data}｜{ok,error}` envelope — with field-level errors mapped for inline display. The role check is repeated inside the action even though the layout already gated the page, because a Server Action is an addressable endpoint, not merely a function the page calls | Route handlers + `withApi` (more moving parts for a form post, and a second serialisation layer); trusting the layout gate alone (a Server Action can be invoked directly) | `withApi` is still owed for genuine JSON APIs (traveler routes, B-018+). Both wrappers must keep the same envelope shape so clients see one contract | TRD-API-001, AUTHORIZATION_MODEL |
| D-044 | 2026-08-25 | PostGIS coordinates are exposed to PostgREST via **computed columns** (`latitude(destinations)`, `longitude(places)`, …) in `0010_geo_accessors.sql` | PostgREST serialises `geography` as EWKB hex, not GeoJSON, so client code reading `centre.coordinates` gets nothing — **silently**. A saved pin simply came back blank, which the B-009 editor round-trip test caught. Computed columns make `select("*, latitude, longitude")` return real numbers with no hex parsing anywhere in the app | Parse EWKB hex client-side (fragile, and repeated per table); add a view per table (duplicates the column list); store redundant lat/lng columns (denormalises, and the geography column is what PostGIS indexes) | Writes still use WKT, which Postgres casts natively. Any future geography column needs its own accessor pair — worth remembering, because the failure mode is silence, not an error | TRD-DB-005 |
| D-045 | 2026-08-25 | The centre/location picker ships **without** the MapLibre map; coordinates are set by geocoding search or direct entry | TRD §11.2 Day 5 specifies a MapLibre picker, but the map needs a tile source and `NEXT_PUBLIC_MAPTILER_KEY` is not provisioned (OPEN_ITEMS). Shipping a map component that cannot be run is a component that cannot be tested, and B-020 is the item that owns mapping work and can verify it against real tiles | Ship an untestable map behind a key check; use keyless OSM raster tiles (their usage policy is not something to assume); block B-009 on the key (blocks all knowledge entry on an unrelated account) | Operators can set pins today via Nominatim search or decimal degrees. The map lands in B-020. `maplibre-gl`/`react-map-gl` were installed then removed rather than left unused | TRD-DB-005, PRD-OPS-CNT-001 |
| D-046 | 2026-08-25 | O06 "Facilities & accessibility" is delivered as **panels on the Places and Routes screens**, not as a screen of its own. Its nav entry stays visible, labelled "In Places & Routes" | An `accessibility_records` row has no independent existence — it attaches to exactly one place or route — and a facility IS a place (`place_type = 'facility'`), already editable in O03. A separate screen would be a second way to edit the same rows, which is how two editors drift apart | Build a standalone O06 screen (duplicates Places for facilities, and edits accessibility away from the thing it describes); hide the nav entry (an operator looking for it would conclude it does not exist) | The nav label tells an operator where to find it instead of pretending it is unbuilt. If a bulk accessibility-audit view is ever wanted, it is a new screen, not a move of this one | PRD-OPS-CNT-001, PRD-DISC-003 |
| D-047 | 2026-08-25 | Zod schemas in a `"use server"` module are declared as their own consts, never inline inside an `opsAction({...})` argument | Next scans exported declarations in a `"use server"` file and treats every arrow function it finds as a Server Action candidate. A Zod `.refine()` callback is a synchronous arrow, so an inline schema fails the build with "Server Actions must be async functions" — an error that points at the refine, not at the real cause | Wrap refinements in async functions (absurd); move all schemas to a separate module (fine, but the schema belongs next to the action it validates) | Every actions file follows the same shape. Worth knowing because the error message is genuinely misleading about its own cause | TRD-API-001 |
| D-048 | 2026-08-25 | The Supabase Insert/Update type widening is centralised in one documented `asRow()` helper rather than an `as any` plus an eslint-disable at each call site | Supabase's generated types widen every jsonb column to `Json`, which our concrete validated shapes do not structurally satisfy despite being correct at runtime. Scattered disables are also fragile: Prettier reformatting moved several onto the wrong line, silently disabling nothing while the real `any` stayed unflagged | Cast at each call site (fragile, as observed); hand-write narrower row types (violates TRD §1.4 — table types are generated, never hand-written) | One bounded type hole, documented, with every call site validated by Zod immediately above it | TRD-DB-001, TRD-API-001 |
| D-049 | 2026-08-25 | The critical-field list lives in `@mandhira/db/critical-fields.ts` and is asserted against `0007_published_views.sql` by a test that parses the migration | The list necessarily exists twice: in SQL, where it gates the published views, and in TypeScript, where the trust panel renders it. Drift is silent and dangerous in both directions — adding a field to the views would leave the UI with no way to verify it, and removing one would un-gate a field while the UI still claimed it was protected | Keep the list only in SQL and query it at runtime (a round trip to render a static list); accept drift and rely on review | Adding a critical field means updating both, or the build fails. The test reads the migration text rather than the database, so it runs without Docker | PRD-KNOW-002, PRD-KNOW-003 |
| D-050 | 2026-08-25 | Moving a trust record to `verified` requires the **verifier**, **approver** or **admin** role — narrower than the action's general write permission | PRD F18 separates review from verification: a reviewer accepts a draft (`human_reviewed`), a verifier confirms it against a T1/T2 source (`verified`). `verified` is precisely the status that yields a high-confidence badge, so letting every Ops role set it would collapse the distinction the trust model rests on. A schema refinement additionally refuses `verified` with no source — a claim nobody can check | One permission for all statuses (simpler, but makes the reviewer/verifier split decorative) | Enforced in the action, on top of RLS. The full Verify **queue** arrives with B-012/M3; this is the inline path | PRD-OPS-WF-009, PRD-KNOW-002 |
| D-051 | 2026-08-25 | The Ops route segment is `force-dynamic` with `revalidate = 0` | Operators act on what they just did — register a source, then verify a field against it on the next screen. A cached read makes the just-created source simply absent, with no error to explain it. Ops is a low-traffic authenticated tool where correctness is worth far more than a cache hit | Per-page cache opt-outs (easy to forget on a new screen); tag-based revalidation (more machinery than a small internal tool needs) | Every Ops screen renders fresh. Revisit only if the tool grows enough for this to matter | PRD-OPS-CNT-001 |
| D-052 | 2026-08-25 | Publishing goes through `publish_entity()` in SQL — the only writer of `status = 'published'` — which enforces validation, the approver role and separation of duties itself | ARCHITECTURE §6 says no feature may bypass the publish gate. A route handler cannot make that promise: the next handler, Edge Function or manual SQL statement is not bound by it. Keeping it in the database means every path is bound. The function is SECURITY INVOKER, so RLS still applies to the UPDATE and it cannot be used to escalate | Enforce in the Ops action only (one code path guarded, others not); a trigger on status (harder to return a useful problem list from) | The `v_published_*` views remain an independent second layer, so even a row wrongly marked published stays invisible without trust. Any future publish surface must call this function | PRD-KNOW-003, PRD-OPS-WF-004 |
| D-053 | 2026-08-25 | `validate_for_publish()` returns a list of `{field, message}` rather than a boolean | PRD F18's acceptance is explicit: a blocked publish names the field at fault. A boolean would force the UI to re-derive the reasons, which is exactly how an Approve screen ends up disagreeing with the gate about what is missing | Return boolean and compute reasons in TypeScript (two implementations of one rule) | The Approve queue and the per-entity panel both render the same list the gate enforces. Adding a rule means adding it once | PRD-OPS-WF-004 |
| D-054 | 2026-08-25 | `record_audit()` is a SECURITY DEFINER function and the only write path into `audit_log`; the actor is taken from the session, never from an argument | D-035 left `audit_log` with no INSERT policy for anyone, which is what makes it append-only — but it also meant `publish_entity()` could not record its own action. A narrow definer function restores the ability to append without granting the ability to edit or delete, and taking the actor from the session means an entry cannot be attributed to someone else | Grant INSERT to authenticated (re-opens the trail to tampering); make `publish_entity` itself SECURITY DEFINER (loses RLS on the UPDATE) | Ops mutations from B-037 onward have a writer to use. Nothing gains the ability to rewrite history | TRD-DB-004, PRD-OPS-WF-008 |
| D-055 | 2026-08-25 | Media stores one original; responsive variants come from Supabase image transformations on delivery, not from `sharp` at upload | TRD §11.2 Day 7 suggests generating 1600/800/400 with sharp. Supabase Storage already serves `?width=` transformations (TRD §3 lists exactly that for `next/image`), so pre-generating would create three derivative files per asset to keep in step with the original, for no benefit | Generate variants with sharp on upload (three files to track, and a re-upload silently leaves stale derivatives) | One asset row, one object. If transformation limits become a cost problem, pre-generation can be added without changing the schema | TRD §3, PRD-OPS-CNT-001 |
| D-056 | 2026-08-25 | English messages are merged UNDERNEATH every locale, so an untranslated key renders in English rather than as the key itself | te/hi ship as scaffolds until M4 (B-034). A traveler seeing `nav.journey` on screen is a broken app; seeing "Journey" is merely an untranslated one. This is the same principle PRD-KNOW-005 applies to content, extended to UI strings | Ship English strings copied into every locale file (they would then be indistinguishable from real translations, and the locale-completeness report in M4 would read 100%); let keys render raw | te/hi message files hold blank values, so completeness reporting stays honest while travelers still see readable text. Filling one in is enough to override | PRD-LANG-002, PRD-KNOW-005 |
| D-057 | 2026-08-25 | Offline detection uses a heartbeat request, not `navigator.onLine` alone | `navigator.onLine` reports whether a network interface exists, not whether anything is reachable — on a train, in a temple basement, or behind a captive portal it is routinely `true` while nothing answers. Those are precisely the conditions this product is built for, so trusting the flag would make the offline banner wrong exactly when it matters | Trust `navigator.onLine` (wrong in the common case); no indicator at all (PRD-OFFL-002 requires one) | A `/api/heartbeat` 204 endpoint and a 30s poll with a 4s abort. The banner also states WHEN the saved information is from, because "offline" alone does not tell a traveler whether it is an hour or a week old | PRD-OFFL-002 |
| D-058 | 2026-08-25 | The service worker sets `skipWaiting: false`; a new version takes over only when the traveler taps the update toast | TRD-DEPL-002 is explicit that the app must never force-reload during Live mode. Someone standing in a darshan queue may be relying on what is on screen, and an automatic reload would interrupt the exact moment the app exists to help with | `skipWaiting: true` (standard, and wrong here); reload on next navigation (still unpredictable mid-journey) | Updates are visible but never disruptive. An E2E asserts the worker ships the SKIP_WAITING handshake rather than claiming control itself | TRD-DEPL-002, PRD-LIVE-* |
| D-059 | 2026-08-25 | Pace maps to a share of the day window for PLACEMENT only: relaxed 0.6, balanced 0.8, full 1.0 | PRD §2.4 names relaxed/balanced/full but attaches no numbers, and buildInitialJourney has to decide when a day is full enough to move on. These are the engine’s reading of the words — relaxed has to leave visible room, full fills the day the traveler declared | Ask the founder and block B-017 (the whole traveler chain already waits on OPEN-001); pack every day to 100% and let health complain (relaxed would then mean nothing) | Targets govern placement only. Nothing is dropped for exceeding one, and computeHealth judges the result on PRD F5’s own thresholds, so a wrong number here costs day distribution, never correctness | PRD-PLAN-008, PRD-HLTH-002 |
| D-060 | 2026-08-25 | The comlink Web Worker wrapper lives in apps/web/lib/engine, not in packages/journey-engine | comlink’s Endpoint type pulls in DOM lib types. Putting it in the engine would mean adding DOM to a package that must also run on the server, and would put `document` within reach of code that is supposed to be pure (ARCHITECTURE §3) | A `@mandhira/journey-engine/worker` subpath export (adds DOM lib to the engine’s tsconfig); structurally re-declaring comlink’s Endpoint to avoid the lib (tests the re-declaration, not the protocol) | The engine stays DOM-free and importable anywhere. The wrapper is app infrastructure and is tested over a real Comlink MessageChannel. If apps/ops ever needs it, it moves to a shared package then | TRD §9, TRD-ENG-002 |
| D-061 | 2026-08-25 | Travel between two items is computed, never materialised as a `travel_leg` journey item | scheduleDay already charges travel time between consecutive items. Inserting a leg item as well would double-count it in both the schedule and the time load, and every existing test would still pass because the numbers stay internally consistent | Materialise legs and stop charging travel in scheduleDay (every caller then depends on the builder having run); materialise and special-case them everywhere they are counted | The `travel_leg` item type stays for legs the traveler adds themselves (a named train). The day view renders the computed leg between items, per PRD F4 | PRD-PLAN-002, TRD-ENG-001 |
| D-062 | 2026-08-25 | The rate-limit counter is incremented and checked inside one SQL statement (`consume_rate_limit`), never read-then-written from TypeScript | A JS-side read-modify-write lets two concurrent requests both read 9 of 10, both decide they are under the limit, and both proceed — which is exactly the case a rate limiter exists for. TRD §6.2 leaves the mechanism open; the correctness requirement does not | Read the count and write it back from the helper (races under the only load that matters); an advisory lock per key (same result, more moving parts); Upstash (an account and a cost for something Postgres does in one upsert) | One `insert … on conflict do update … returning` does the counting. The helper only chooses which windows apply and turns the answer into a `Retry-After`. pgTAP proves it refuses exactly at the limit | TRD-SEC-001, TRD §6.2 |
| D-063 | 2026-08-25 | The AI runtime takes its cache and its call log as PORTS; the Supabase implementations live in `@mandhira/db` | `packages/providers` is the only place vendor SDKs may be imported, but that does not make it the place that owns database access. Importing `@mandhira/db` there would put the schema behind the provider abstraction and make every AI test need a database | Import the Supabase client into the providers package (couples the two and makes cache behaviour untestable without a database); put the whole AI runtime in `packages/db` (puts an SDK in the data package) | `runAiTask` is exercised against a Map in tests and against `ai_cache`/`ai_calls` in the app, through the same code path. The wiring point is `@mandhira/db/ai-store` | TRD-AI-004, ARCHITECTURE §1 |
| D-064 | 2026-08-25 | `journeyBriefSchema` stays UNSATISFIABLE when the candidate list is empty, rather than falling back to a free-text experience id | The tempting fix — allow any string when there is nothing published — turns the one hard constraint in the schema off exactly when there is no published knowledge to check the answer against. PRD-INT-005 requires 100% of hallucinated experiences blocked, and that is the moment it would fail | Allow a free string with no candidates (fails open); skip the AI call entirely when there are no candidates (correct, and the caller can still do that — but the schema must not depend on the caller remembering) | With no candidates the model can produce a brief with no experiences, and nothing else. `keepKnownIds` rejects on the same basis, so both layers agree | PRD-INT-005, TRD-AI-002 |
| D-065 | 2026-08-25 | A `buffer_minutes` value already on an item wins over the computed default in `scheduleDay`; the default applies only where none is set | PRD F4 says buffers are visible and EDITABLE, and the original code overwrote every one of them on each schedule — so an edit survived until the next recompute and then silently vanished. It also made PRD F6's ladder step (a), "absorb the delay into your buffers", impossible to actually carry out: the absorbed minutes reappeared on the next schedule. Found by a B-026 test, not by review | Keep recomputing and add a separate `buffer_override` column (two fields meaning one thing, and every reader has to remember both); apply the absorb option as a duration change instead (lies about what was shortened) | One field, and it means what it says. All 25 existing scheduling tests still pass, because they never set a buffer and so still get the computed one | PRD-PLAN-005, PRD-ADPT-003 |
| D-066 | 2026-08-25 | Every `ItemChange` is an ABSOLUTE target (`shorten to 45`, `set window 14:00`), never a delta from wherever the item happens to be | The card the traveler sees was evaluated against a specific set of operations, and `applyOption` has to reproduce exactly that. The first draft expressed a PROTECTED move as a delta and evaluated it against pre-modified items — so applying it to the real items would have done nothing at all, and the traveler would have tapped an option that silently did not happen | Deltas plus a note of what they were computed against (correct only while nothing else changes in between); re-evaluating at apply time (the card would then be describing a different plan from the one applied) | Applying an option twice lands in the same place, asserted by a test, and another asserts the applied result matches what the option promised | PRD-ADPT-002, TRD-ENG-002 |
| D-067 | 2026-08-25 | Option generation STOPS at the first ladder rung that reaches Tight or better, and any option that does not improve on "keep as is" is dropped | PRD F6 step 3 says the ladder stops; the reason is that offering a removal beside an option that already fixed things implies the removal was necessary, and the traveler has no way to check whether it was. An option that changes something and improves nothing is a cost with no benefit | Generate every rung and rank (produces cards offering to delete an OPTIONAL item next to one where nothing moves); keep non-improving options for completeness (they read as choices, not as noise) | A day whose own buffers absorb the delay gets exactly one option and no removals. A day where nothing can help gets an empty option list and a "keep as is" that says Broken — honest, and the traveler is not offered a false choice | PRD-ADPT-003, PRD-PLAN-007 |
| D-068 | 2026-08-25 | `critical_fields()` restates the TRD §4.4 lists that `0007_published_views.sql` writes inline, and a pgTAP test parses the live view definitions to prove the two agree | The publish gate belongs inside the views, where it is visible in the view definition and cannot be changed by editing a function somewhere else. But the reverify job also needs the list, and a second inline copy would be free to drift — a field that stopped being reverified while still gating publication would make the job go quiet with nothing looking wrong | Refactor the views to call the function (changes the gate, which is the one thing that must not move, and breaks the TS test that reads 0007); duplicate the arrays in the job with a comment (drift with no guard); derive the list from trust_records at runtime (would treat any field anyone happened to record as critical) | Two statements of the same list, and a test that fails if they diverge. The TypeScript copy in `packages/db/src/critical-fields.ts` is already guarded the same way against 0007, so all three are pinned to one source | TRD §4.4, PRD-KNOW-003 |
| D-069 | 2026-08-25 | A failing scheduled job records the failure in `job_runs` and RETURNS; it does not re-raise | pg_cron runs each command in its own transaction, so re-raising rolls the whole thing back — including the row saying it failed. The choice is between an error in the Postgres log with no row here, or a row here and nothing in the Postgres log. `v_job_health` and the Ops dashboard read this table, so the row is worth more | Re-raise and lose the record (the failure is then only visible to someone reading Postgres logs); an autonomous transaction via dblink (a new extension and a second connection per job, for a log line) | `last_status` is how a failure surfaces, and `v_job_health.needs_attention` is true for a job that runs on time and fails every time — an overdue check alone would call that healthy. Only SQLSTATE is stored, never SQLERRM, which can quote row values back | BACKEND_ARCHITECTURE jobs contract, TRD §5.4 |
| D-070 | 2026-08-25 | Every pg_cron schedule calls `run_scheduled_job('name')`, never the job function directly | Idempotence each job can own, but single-flight and "did this run" cannot be left to each one to remember — the job that forgets is the one that fails silently, and silence is the failure mode this whole table exists to make visible | Repeat the lock and the logging inside each job (five copies of the same guard, and a sixth job that forgets); rely on pg_cron's own run history (it records that the command ran, not what it did) | One wrapper takes an advisory lock keyed on the job name, writes the `job_runs` row, and dispatches through an allowlist. A run that cannot take the lock records itself as `skipped` rather than as an error | BACKEND_ARCHITECTURE jobs contract |
| D-071 | 2026-08-25 | `withApi` lives in `@mandhira/db/api`, not in a new `packages/api` | Everything it composes is already in `packages/db` — the Supabase clients, `rateLimit()`, `getOpsRoles`, the Zod schemas. A new package would be a deviation from the README tree, which CLAUDE.md §1 makes a founder decision, and blocking the wrapper on that would leave the first route to be written without one | A new `packages/api` (needs tree approval, and would depend on `packages/db` for all of its parts anyway); duplicate the wrapper in each app (CLAUDE.md §4 forbids duplicate functionality, and the copies would drift on exactly the security checks that must not) | Exported from the `/api` subpath so nothing reaching a browser bundle can import it by accident. If it grows past a request pipeline, `packages/api` becomes worth asking for — noted rather than assumed | TRD-API-001, CLAUDE.md §1 |
| D-072 | 2026-08-25 | `keepalive` is a Vercel Cron route (`/api/cron/keepalive`), NOT a Supabase Edge Function — **resolving a documented conflict between TRD §5.4 and BACKEND_ARCHITECTURE** | TRD §5.4 assigns keepalive to Vercel Cron; BACKEND_ARCHITECTURE lists it among the Edge Functions. The TRD outranks the architecture docs (CLAUDE.md §2), and it is independently right: an Edge Function that pings Supabase runs ON Supabase, so a paused project cannot wake itself. The pinger has to be somewhere else | Follow BACKEND_ARCHITECTURE (a keepalive that stops working exactly when it is needed); implement both (two things to keep alive, one of which cannot work) | A route handler guarded by `CRON_SECRET`, scheduled from `apps/web/vercel.json`. A missing secret closes the route rather than opening it. **BACKEND_ARCHITECTURE.md should have keepalive struck from its Edge Function list** — flagged rather than silently edited | TRD §5.4, TRD-DEPL-001 |
| D-073 | 2026-08-25 | `createWithApi` takes its Supabase clients, role reader and guest-key function as INJECTED dependencies rather than importing them | Importing `next/headers` into `packages/db` would make the pipeline unusable from a test, an Edge Function, or a plain Node script — all of which need the same validate/auth/limit sequence. It would also bind the package to one app's cookie and guest-session conventions, which belong to the app | Import the app clients directly (couples the package to Next and makes every pipeline test need a request context); a global registry set at startup (the same coupling, later and less visibly) | Each app builds its own instance once (`apps/web/lib/api.ts`). The pipeline is tested against stubs over the same code path the app runs | TRD-API-001, ARCHITECTURE §3 |
| D-074 | 2026-08-25 | Notification SCHEDULING lives in `packages/journey-engine`, not in the Edge Function that runs it | What to remind someone about, and when, is journey logic — it reads items, buffers and prepare deadlines, and CLAUDE.md §4 puts journey logic in the engine and nowhere else. It also has to run in two places: on the server every fifteen minutes, and on the phone for the local leave-by reminders PRD F15 requires to work offline. Two implementations of "when should we say something" would disagree exactly when the phone is offline and cannot be corrected | Put the rules in the Edge Function (untestable without a scheduler, and unavailable to the offline client); duplicate them client-side (two copies of the same rule, drifting) | `scheduleNotifications` is pure: journey, items, prepare tasks, prefs and `now` in; drafts out. The Edge Function persists what it returns and nothing more | PRD-NOTF-001, TRD §5.4, CLAUDE.md §4 |
| D-075 | 2026-08-25 | PRD F15's weekly cap on non-journey notifications is applied when drafts are SCHEDULED, not when they are sent | A suggestion the traveler never sees because the sender silently dropped it is still a suggestion the product decided to make. Capping at the point of queueing keeps the decision visible in `notifications`, where it can be counted, rather than hidden in a sender's control flow | Filter at send time (the queue then lies about what was intended); no cap and rely on there being few suggestions (PRD F15 states the cap as a rule, not an expectation) | `applyWeeklyCap` takes the last non-journey send time and drops anything inside seven days of it. Journey notifications are never capped — the cap exists to stop the product talking at people, not to stop it saying a train moved | PRD-NOTF-004 |
| D-076 | 2026-08-25 | A push result distinguishes `gone` (404/410) from `failed`, and a `gone` subscription is dropped immediately rather than after five failures | 404 and 410 are the push service stating a fact — the browser has thrown the subscription away. TRD §5.4's five-failure rule is for transient trouble; applying it to a definite answer means four more pointless sends and a table filling with endpoints that will never respond | One `failed` status for everything (correct but wasteful, and the failure count stops meaning anything); delete on any failure (a push service having a bad afternoon would unsubscribe everybody) | `shouldDisable` returns true immediately for `gone` and at the fifth consecutive failure otherwise. 429 and 5xx are retryable; a 4xx is us being wrong, and sending it again will be wrong the same way | TRD §5.4, INTEGRATIONS |
| D-078 | 2026-08-25 | The published views' helper functions (`critical_fields_gated`, `entity_trust`, and 0015's two) are SECURITY DEFINER with a pinned `search_path`; EXECUTE is granted explicitly to anon/authenticated rather than inherited from PUBLIC | **Fixes a live bug.** A view reads its referenced TABLES with the view owner's rights, but a FUNCTION called inside it runs with the INVOKER's. Both helpers read `trust_records`, which 0008 restricts to Ops — so every published view raised `permission denied` for anon and for any signed-in traveler. Invisible since 0007 because per-row functions are only evaluated when there are rows, and every pgTAP file ran as `postgres` | Inline both helpers into all nine views so the owner's table rights apply (duplicates the publish gate nine times — a gate restated nine times will eventually disagree with itself); grant anon/authenticated SELECT on `trust_records` (opens the whole table to read arbitrary rows) | Travelers can read the views and see trust badges, so PRD F9 is possible at all. The gate is unchanged. **Accepted exposure:** EXECUTE cannot be revoked — verified, not assumed: a function in a view is checked against the invoker — so a caller who already knows an UNPUBLISHED entity's UUID can call `entity_trust` directly and learn its source name and verification dates. Judged smaller than the risk of nine copies of the gate. `0013_traveler_read_surface_test` is the regression guard | PRD-TRST-001, PRD-KNOW-003, TRD-DB-005 |
| D-079 | 2026-08-25 | An experience exposes the accessibility of the PLACE it happens at, resolved through `v_published_places` | PRD-DISC-003 puts accessibility icons on the EXPERIENCE card, and a traveler choosing between two darshans will not open two place pages to find out which one they can get into. Resolving through the place's own published view means an experience can never expose the accessibility of a place that has not itself cleared the gate | A second query from the client (two round trips for one card, and every caller has to remember the join); denormalise onto experiences (two copies of one fact, drifting) | One column on the experience view. An experience whose place is ungated shows NULL rather than the place's data — asserted | PRD-DISC-003, PRD-KNOW-003 |
| D-080 | 2026-08-25 | Accessibility is NULL when no record exists, never an empty object | "We have no accessibility information for this place" and "we checked and it has none of these features" are completely different things to a wheelchair user, and a `{}` that renders as a row of grey icons says the second while meaning the first. Same reasoning as `step_free`'s tri-state and PRD-KNOW-005 | Return `{}` and let the UI guess (the UI cannot tell the difference); default every field to false (asserts facts nobody checked) | The client can distinguish absent from negative and say so. `route_stops_for` returns `[]` rather than NULL for the opposite reason — a route with no stops IS a known-empty list, not missing information | PRD-DISC-003, PRD-KNOW-005 |
| D-081 | 2026-08-25 | A clearly-invented local fixture destination is seeded by default, and it deliberately covers the RANGE of trust and accessibility states rather than a happy path | The traveler app had nothing to render and the publish gate had nothing to gate, because B-013 waits on OPEN-001. More importantly: a fixture where everything is Verified and step-free would be worse than none — a UI built against it would ship having never rendered "Check locally", never rendered a conflict, and never rendered "we do not know". The fixture's value is the states it forces you to handle | Wait for B-013 (leaves the whole traveler surface unbuildable and untestable while OPEN-001 is outstanding); a minimal happy-path fixture (would let the two-thirds of the trust UI that matters most ship untested); make it opt-in behind a flag (every developer and CI run has to remember it) | One destination, four places, three experiences, a route with stops. High/medium/low confidence, a conflicted field, an accessibility record that is yes, one that is partial, and one absent entirely, plus a place that is `status = 'published'` yet held back by the gate. `0014` pins that range so a later edit cannot flatten it. Every slug is `fixture-`, every name carries `(fixture)`, sources are `example.invalid` | TRD §11.2, PRD-TRST-001, PRD-DISC-003 |
| D-082 | 2026-08-25 | pgTAP assertions must be scoped to their own fixtures; a global count is not an assertion | Seeding the fixture broke three existing tests that passed only because the database happened to be empty — `0007` asserted a published view was entirely empty, `0011` read the reverify job's database-wide return, `0012` counted every place with `step_free = 'yes'`. None of them was wrong about the behaviour; all three were coupled to an empty world | Keep the fixture out of the default seed so the old assertions keep passing (preserves the coupling and hides it until the first real content lands, which is B-013 — the same trap, later); assert ranges instead of exact counts (weaker, and still drifts) | All three now scope to the entity the test itself created. A test that breaks when unrelated data appears is a test that will break during B-013 for a reason that has nothing to do with what it checks | TESTING_STRATEGY |
| D-083 | 2026-08-25 | A discovery card shows the WEAKEST trust state across its fields, not the strongest or an average | A card summarises several independently-verified fields under one badge. Showing the best of them lets a card read "Verified" while an unshown timing says "Check locally" — technically true, practically a lie, and undetectable by the person it misleads. The fixture's Dawn Darshan is exactly this case: T1-verified, with a conflict on its booking instructions | Show the entity-level badge only (ignores the field-level records that are the whole point of the trust model); show several badges per card (six badges on a card is noise, and noise is how a real warning gets skipped); average them (invents a state PRD F9 does not define) | One badge per card, at the weakest state. The detail page and trust sheet break it down field by field — next slice. An E2E asserts the conflicted card reads "Check locally" | PRD-TRST-001, PRD-DISC-002 |
| D-084 | 2026-08-25 | An unrecorded accessibility field produces NO icon, and the absence is stated in words on the card | Rendering a crossed-out wheelchair for a place nobody has checked tells a wheelchair user something false about the world. Rendering nothing at all is not enough either — silence on a card that shows icons elsewhere reads as "it has none of these". Both failures land on the traveler least able to absorb them | Default unrecorded to `no` (asserts a fact nobody checked); render a grey "unknown" icon for every unrecorded field (a row of grey icons on every under-researched place, which trains people to ignore the row) | Icons appear only for recorded values, `partial` keeps its own word rather than rounding, and a place with no record at all says "We don't have accessibility information for this place yet." Follows D-080's NULL-not-empty-object through to the UI | PRD-DISC-003, PRD-KNOW-005 |
| D-085 | 2026-08-25 | Supabase `.select()` strings are always single string literals, never built by concatenation | The typed client infers the row shape from the LITERAL type of the select string. A `+` concatenation widens it to `string` and the row type silently degrades to `GenericStringError`, so every field access becomes a type error with a misleading message — it cost time here before the cause was obvious | Cast the result (throws away the type safety the generated types exist to provide); define selects as `as const` fragments and join (same widening at the join) | Long selects stay on one line even though it is ugly. Prettier leaves string literals alone, so the formatter will not undo it | TRD-DB-001 |
| D-086 | 2026-08-26 | Pure trust helpers live in `apps/web/lib/trust.ts`, separate from `lib/knowledge.ts` | A "use client" component that reaches even a TYPE out of a module which imports `next/headers` drags the whole server-only chain into the browser bundle. The build then fails naming `next/headers` rather than the import that caused it, and TypeScript cannot see the boundary at all — `tsc` passed while every detail route returned 500 | Keep one module and cast in the client component (the import still exists, so the chain still resolves); make the badge a server component (it needs state for the sheet, so it cannot be) | `lib/trust.ts` holds the vocabulary and the two rules and touches nothing request-scoped; `knowledge.ts` re-exports them so server callers still have one import. The same class of mistake as B-009's `emptyPlace()` | TRD-API-001, PRD-TRST-001 |
| D-087 | 2026-08-26 | Both apps read their build directory from `NEXT_DIST_DIR`, and the E2E suite builds into `.next-e2e` | `next dev` and `next build` both write to `.next`, so running the suite while a dev server is open corrupts that server's chunks — every route then answers "Cannot find module ./NNN.js" until the directory is deleted. It cost time twice in one session, and the second time looked like a code failure rather than a collision | Tell developers not to build while `dev` is running (a rule nobody remembers, and CI will not enforce); always kill the dev server first (turns every test run into a restart) | `pnpm test:e2e` builds and serves from its own directory. The Playwright config already uses unusual ports for the same class of reason: a test run must not collide with whatever the developer has open | TESTING_STRATEGY |
| D-088 | 2026-08-26 | A `<dt>`/`<dd>` pair sits under at most ONE wrapping `<div>` inside a `<dl>`, with any badge placed inside the `<dt>` | HTML5 permits a single `div` grouping inside a definition list; the first version of `FactRow` nested the pair two divs deep for layout, which axe flagged as `dlitem`. It read correctly and was wrong with a screen reader, which is exactly the failure a sighted review will not catch | Drop the `<dl>` for plain divs (loses the term/definition semantics that make these pages navigable); suppress the axe rule (the rule is right) | The badge lives inside the `<dt>` beside its label, and the locale-fallback note inside the `<dd>`. Layout is unchanged | PRD-DSGN-005, WCAG 2.2 AA |
| D-089 | 2026-08-26 | Search is a plain GET form, server-rendered, with no client JavaScript | A result is then a URL: shareable, back-button-able, in browser history, and readable before any JavaScript arrives. PRD F11/PRD-OFFL exist because this product is used on hills and in basements — a search box that needs a bundle to run is a spinner exactly where a page was needed | A client-side search component with debounced fetch (nicer typing, useless on one bar, and a shared link becomes a bare /search); an API route plus client fetch (the same, with more moving parts) | Filters survive a reload and a share, the box keeps what was typed so refining does not mean retyping, and the result count is announced through role=status for a screen reader | PRD-DISC-001, PRD-OFFL-002, SRCH-01 |
| D-090 | 2026-08-26 | The type filter is ONE control over two non-overlapping enums; choosing a place type returns no experiences and vice versa | PRD F2 asks for a single "Type" filter, but `place_type_enum` and `experience_type_enum` share no values. Something has to give, and returning the other group unfiltered would make the control look broken to the person who just used it | Two separate Type controls (spends two of the six filter slots on one idea); match everything in the other group (the filter appears to do nothing to half the page); hide the other group entirely (hides that a whole kind of result exists) | One select, grouped under "Experiences" and "Places" headings so the two vocabularies are visible before choosing. The option lists come from the generated `Constants`, so they cannot drift from what the database accepts | PRD-DISC-005 |
| D-091 | 2026-08-26 | Only four of PRD F2's six filters ship now; the two that need a journey are ABSENT, with a line saying when they arrive | "Availability on my dates" and "Near a place I've added" both require an active journey (B-019). PRD F2 caps visible filters at six rather than requiring six, so four is within spec — and a control that is always present and never works teaches a traveler that the controls do not work, after which they stop trying the ones that do | Ship all six with two disabled (dead controls, as above); ship six with the two silently matching everything (a filter that changes nothing is worse than an absent one) | Four working filters and one sentence naming what is still to come. The registry row PRD-DISC-005 says "exactly 6" where PRD F2 says "max 6 visible"; the PRD outranks the registry (CLAUDE.md §2) and is followed here | PRD-DISC-005, PRD F2 |
| D-092 | 2026-08-26 | `travel_estimates` gains a published view (`0017`), gated on BOTH endpoints being published places | The engine reads it to put travel between two items on the clock. Ops-only meant every journey scheduled with ZERO travel time between places — silently, and looking completely normal on screen, which is the exact class of wrongness the engine exists to prevent. Same shape of gap as OPEN-009: something the engine needs, with no path for the person it serves | Leave it Ops-only and rely on `transport_connections` (only curated legs get a time; anything the routing cache filled is invisible); compute distance client-side from coordinates (inventing a travel time, which B-016 refuses to do on principle) | Distance, duration and mode between two published places. `provider` and `computed_at` are deliberately not exposed — a traveler needs the number, not which vendor produced it, and TRD §5.5 has the engine label a fallback rather than the data doing it | TRD-ENG-001, TRD §4.4 |
| D-093 | 2026-08-26 | The brief preview builds a journey and SAVES NOTHING, saying so on the screen | `journeys` has no anon policy and AUTHORIZATION_MODEL is explicit that a guest gets device-local drafts only — which is Dexie, and Dexie is B-023. Persisting a guest journey server-side keyed on a cookie would contradict that model and make anyone holding the cookie able to read it | Persist via service-role keyed on a device cookie (bypasses RLS as a pattern, which CLAUDE.md §5 forbids, and shares a journey with whoever has the cookie); require sign-in (contradicts guest-first, PRD-ACCT-001, on the screen most worth trying before committing) | The brief lives in the URL, like search (D-089), so it survives a reload and can be sent to whoever is coming along. The screen states plainly that nothing is kept — implying a save that is not happening is how someone loses an hour of work | PRD-ACCT-001, AUTHORIZATION_MODEL, PRD-OFFL-001 |
| D-094 | 2026-08-26 | One traveler stands for the whole party in the brief, carrying the most constrained mobility | The engine takes the strictest limit in the group anyway (PRD-PLAN-005, PRD-HLTH-005), so a full roster would collect more about people than the answer needs. PRD §10 and PRD-PRIV-002 make traveler attributes the most sensitive thing this product holds | Ask for each traveler individually (more personal data for an answer that does not change); infer from a pace choice (guessing about somebody's mobility, which is the one thing PRD Principle 1 reserves for the traveler) | One question, four answers, and the value never reaches `traveler_profiles` or anything Ops can read. A full roster arrives with B-019, where a saved journey genuinely needs one | PRD-PLAN-010, PRD-PRIV-002 |
| D-095 | 2026-08-26 | `checkReturnGuard` selects the preceding item by its START, not its end | **Fixes a live bug from B-016.** The old filter took the last item with `planned_end_at <= fixed_start_at`, which EXCLUDES an item running past the anchor — the clearest breach there is. A 90-minute darshan from 06:00 against a 06:30 train had no preceding item at all, so the guard returned ok and the day read Comfortable while the traveler missed their train. It survived because nothing in the product could produce a FIXED item until B-019 made one reachable | Compare against the anchor's end instead (same blind spot, moved); special-case overlapping items (two rules where one will do, and the second will drift) | Selected on start, falling back to the end instant when an item has no recorded start so a partially scheduled one is still weighed. Two regression tests pin both directions, and an E2E asserts the sentence reaches the screen | PRD-PLAN-006, TRD-ENG-001 |
| D-096 | 2026-08-26 | PRD-PLAN-002's tier rules live in the engine (`checkItemAction`) and are enforced at the ROUTE; the UI asks the same function only so it does not offer what will be refused | The tier is the traveler's own statement of what matters, and the rules over it govern the option ladder, the item editor and any future offline edit. One statement of the rule, so a screen, a route and an offline edit cannot disagree. Hiding a button has never been a control (CLAUDE.md §4) | Enforce in the route only (the UI then offers actions that fail, which reads as breakage); enforce in the UI only (a crafted PATCH walks straight through); a database CHECK (cannot express "requires confirmation") | 24 unit tests over the whole tier × action matrix, and E2E that send DELETE and PATCH directly to the API rather than clicking. `requiresConfirmation` is enforced server-side too: a client that omits it is refused rather than assumed to have asked (PRD Principle 6) | PRD-PLAN-002, PRD-PLAN-003 |
| D-097 | 2026-08-26 | `api/` and `auth/` are excluded from the traveler app's locale middleware | **Fixes a live bug from B-014.** next-intl was prefixing every non-page route: `/api/heartbeat` 307'd to `/en/api/heartbeat`, which meant the Vercel Cron keepalive silently never ran (cron does not follow redirects) and a traveler following a magic link landed on a 404. Nothing caught it because no browser had called an API route until B-019 | Handle the locale prefix inside each route (every future route has to remember); match only page paths explicitly (a longer list to keep in step with the app) | Two segments added to the matcher's negative lookahead. `/api/heartbeat` answers 204 again and traveler sign-in works | TRD-DEPL-002, TRD-API-001 |
| D-098 | 2026-08-26 | The Journey Summary projection lives in SQL (`journey_summary_payload`), reached through two doors that each check something: a share token, or ownership | `journey_shares` carries one policy — `owns_journey` — so a token holder is `anon` and cannot read the journey through RLS at all. A public page therefore needs a path past RLS, which makes the projection itself the control rather than something RLS backstops. TRD-SEC-004's promise (excludes profiles and notes) should not rest on every future editor of a TypeScript file remembering it: `select('*')` is one keystroke from being the leak, and a reviewer can check one function instead of trusting a convention. The owner's preview reads the SAME projection, so nobody can send a link that shows more than what they were shown | A service-role client with a hand-written select (works today, and the exclusion list becomes a convention); a `security_invoker` view (cannot see a token holder, who is anon); assembling the owner's preview separately in TypeScript (two assemblies of the same page drift, and the direction is a shared link showing more than the owner saw) | Granted to `anon`, so no service-role key touches the one route a stranger can reach. `journey_summary_payload` is granted to NO client role — it checks nothing itself, so a grant would make any journey readable by uuid, and pgTAP asserts that missing grant. The payload is Zod-parsed on the way back in: the RPC types as `Json`, and a cast would be the one place the database's shape is taken on faith | PRD-PREP-004, TRD-SEC-004 |
| D-099 | 2026-08-26 | A `prepare_tasks` row stores WHICH task it is and whether it is ticked — never the words | The same argument as Journey Health, which is recomputed on every read and never stored. A task's wording comes from three moving things: the message catalogue (hi/te are scaffolds until M4), the knowledge behind it (a temple changes its dress code), and the journey. Freezing a sentence at generation time means a journey planned today keeps today's English and today's dress code forever — and the traveler reads it at the gate, which is the worst possible place to find out it went stale | Render `title_i18n`/`body_i18n` for every locale at generation time (freezes both the copy and the knowledge); store the engine key inside `trust_ref` (works, and lies about what that column is); a deterministic uuid v5 from the key (no schema change, but `downloads:offline` is not unique across journeys so it would need namespacing, and the identity stops being readable in the table) | New nullable `engine_key`, unique per journey, so regeneration upserts instead of duplicating and a ticked box survives an edit elsewhere. Null means a traveler-authored task the engine must never touch. Regeneration never deletes: a task whose item was removed stops rendering, and if the item returns so does the tick. The two `_i18n` columns stay for traveler-authored tasks, whose text has no other home | PRD-PREP-001, PRD-PLAN-004 |
| D-100 | 2026-08-26 | A day's entry requirements and dress code are stated once per DAY on the printed summary, not under every item | PRD F7's acceptance is one A4 page per day. Several stops at the same temple all carry "carry photo ID" and the same dress code, and repeating them under every entry took a day with 8 items to THREE sheets — breaking the criterion for exactly the busy days that most need to fit. Measured, not estimated: 3 sheets before, 1 after, same journey | Shrink the type (costs legibility for the people most likely to be handed paper); drop requirements from the summary (they are the reason the page is carried at the gate); state them once for the whole journey (a sheet is read on its own, so day 2 would carry nothing) | Still repeated ACROSS days, deliberately — each sheet has to stand alone, and "see yesterday" is no use to whoever is holding today. Deduplicated by the sentence rather than by place id, so two temples with the same rule say it once. Verified with Chromium's print renderer at A4: 1 day/2 items, 1 day/8 items and 3 days/4 items each all put exactly one day on a sheet | PRD-PREP-004 |
| D-101 | 2026-08-26 | No `loading.tsx` anywhere in either app; route-level streaming is traded away for correct HTTP status codes | A `loading.tsx` wraps its segment in Suspense, which makes Next stream the route — the status is sent with the shell, before the page's own code runs. `notFound()` then cannot produce a 404 and `redirect()` cannot produce a 307. Observed, not theorised: adding one `[locale]/loading.tsx` turned 12 passing tests red at once, including every "is a 404, not someone else's page" assertion. Those are privacy-adjacent — a soft 404 still returns 200 to a crawler or a cache, and the whole reason those routes call notFound() rather than a 403 is to avoid confirming that someone else's journey exists | Keep the skeletons and relax the tests (gives up a real HTTP contract for a visual nicety); loading only on leaf routes with no notFound below them (almost none qualify — `[locale]/` and `journeys/` both have 404-ing descendants); a client-side loading flag per page (the same thing with more code) | Documented in docs/DESIGN_SYSTEM.md so the next person does not helpfully add one back. A skeleton, if ever wanted, goes in a CLIENT component behind its own Suspense on a leaf route. The Skeleton primitive was removed rather than left unused (CLAUDE.md §4) | TRD-API-001, PRD-PREP-004 |
| D-102 | 2026-08-26 | `/journeys/*` is gated in `middleware.ts`, before rendering, rather than only by `redirect()` inside each page | The in-page redirect is correct but late: it runs during render, so anything that makes the route stream downgrades it to a client-side navigation, and even without streaming the protected page's server component (a journey read, a knowledge bundle, a health recompute) runs for a visitor who was never going to see it. Deciding in middleware means a real 307 and no wasted work. Everything else stays guest-first per AUTH-03 — browsing, searching and building a draft still need no account, and `/s/<token>` is opened by people who will never have one | Gate every traveler route (breaks AUTH-03's guest-first promise outright); rely on the page redirect alone (late, and streaming-fragile); a route group for authenticated pages (moves files without fixing when the decision happens) | UX only, never a control — RLS is what stops one traveler reading another's journey, and the in-page `redirect()` is kept as defence in depth (CLAUDE.md §4: UI hiding is never a control). Refreshed auth cookies are copied onto the redirect response; dropping them would sign the traveler out on the very request meant to send them to sign-in | AUTH-03, PRD-PRIV-002 |
| D-103 | 2026-08-26 | shadcn is wired as the source of UI PRIMITIVES; its token contract is aliased onto PRD §12.1 rather than `shadcn init` writing a second palette | `packages/ui` was already shadcn's idiom written by hand — Radix, cva, cn, Tailwind — but with no `components.json`, so every primitive had to be built from scratch. Wiring the CLI up costs nothing and removes that. What it must not cost is colour: PRD §12.1's palette carries verified contrast ratios and documented decisions (D-025, D-039, OPEN-008), and shadcn's defaults have never been checked against this product's 4.5:1 requirement. Two palettes means two sources of truth, and only one of them flips in dark mode | `shadcn init` and accept its palette (throws away verified contrast work); copy registry source in by hand (what was already happening, and the reason primitives were scarce); keep shadcn out entirely (leaves the CLI and MCP server unusable for no gain) | `packages/config/tailwind/shadcn-bridge.css` aliases every contract name onto a Mandhira token; `shadcn-bridge.test.ts` (59 assertions) holds that each one maps, resolves to `var(--token)` and never a literal, and names a token that exists — including the trap that `--brand-primary-text` is text-on-light, not ink-on-fill, which as `primary-foreground` would put ~2:1 under every primary button. Registry components import `cn` via the `#lib/cn` subpath, NOT `@/`, because both apps transpile this package with the APP's resolver. `Select` is deliberately not taken: a native select opens the OS picker on the phones this is built for. MCP server pinned to the workspace dependency, not `@latest` | PRD §12.1, PRD §12.5 |
| D-104 | 2026-08-26 | `RoutingProvider` returns null when it cannot answer, and never falls back inside an adapter; every estimate carries `source: routed | estimated` | The engine plans around travel time, so what matters as much as accuracy is knowing which kind of number it is. A straight line is wrong in exactly the places that matter — a river with one bridge, a hill with a road around it, a temple street closed to vehicles. If an adapter quietly substituted its own guess on failure, a `routed` label could end up on a number nobody routed, and PRD F1's rule that an unverified fact is never shown as verified applies to derived numbers too | Throw on failure (every caller then needs a try/catch to reach the fallback); fall back inside the ORS adapter (makes `source` a lie); return an estimate with a confidence score (a number nobody can act on, where two words do) | The chain ends in the straight-line router, which never declines — so the engine can ALWAYS schedule, and what varies is whether the number is routed or estimated, never whether there is one. The estimate is deliberately pessimistic (detour factor per mode, minimum 60s per transition): arriving early costs a traveler a sit-down, arriving late costs them a darshan that happens once that day. ORS also declines for `train` and `ferry` rather than routing them by road, which would be a plausible wrong number | MAPS-01, TRD-ARCH-003 |
| D-105 | 2026-08-26 | MAPS-01 and MAPS-03 ship WITHOUT the OpenRouteService or MapTiler keys; MAPS-02 (map tiles) is the only part that waits | B-020 was blocked as a unit on ACCT-03, but only the tiles actually need a key. Routing degrades honestly — with no `OPENROUTESERVICE_API_KEY` the adapter declines immediately and every estimate comes back `estimated`, which is a working product with slightly pessimistic travel times rather than a broken one. Open-in-Maps needs no key at all: it is a URL | Wait for the keys and ship all three together (leaves the traveler with no way to reach a temple gate, for a credential that is not a technical dependency); build a Mandhira map view instead (a worse copy of something the traveler's own maps app already does well, with their saved places and offline downloads) | The whole of B-020 except tiles is now live. ACCT-03 remains open, and turning it on is an env var — no code change. `0021` exposes latitude/longitude on `v_published_places` because `location` reaches PostgREST as WKB hex and was unusable client-side; the publish gate still applies, so an unverified pin is not readable | MAPS-01, MAPS-03, ACCT-03 |
| D-106 | 2026-08-26 | Open-in-Maps hands off to the platform's own maps application rather than rendering a Mandhira map | Getting the last two hundred metres to a temple gate is something Google Maps, Apple Maps and OsmAnd already do well — with the traveler's own saved places, their offline downloads and their language. A worse copy would be actively harmful at the moment someone is lost. Android gets a `geo:` URI specifically so the traveler's DEFAULT app answers: someone who installed OsmAnd for offline pilgrimage routes should not be dragged into Google Maps by us | Embed a map on the place page (needs MapTiler, and still cannot navigate); always link to Google Maps (ignores what the traveler chose, and fails offline where OsmAnd would not); `maps://` for Apple (refused by Android and desktop browsers, so a shared link breaks) | Apple uses `maps.apple.com`, which opens the app on an Apple device and the web map elsewhere, so a forwarded link still works. Android repeats the coordinates inside `q` — without that some apps treat the label as a free-text SEARCH and wander off to a similarly named temple in another state. 8 unit tests pin the URL shapes, including the lat/lng order: ST_X is longitude and ST_Y is latitude, and a swap renders a confident pin in western China that looks entirely normal | MAPS-03, PRD-OFFL-006 |
| D-107 | 2026-08-26 | Done / Running late / Stay longer RECORD what happened and never reschedule; the option ladder stays in the Change Card (B-026) | Both "running late" and "stay longer" mean the plan no longer matches reality, and the obvious implementation is to reschedule the rest of the day. That is forbidden twice over: PRD Principle 6 allows no state-changing journey action without an explicit tap on THAT change, and PRD F6 routes every plan change through a Change Card the traveler accepts. A day that quietly rearranged itself because someone said they were running late is precisely the behaviour this product exists to not have | Auto-reschedule the rest of the day (violates Principle 6 and F6); make the actions inert until B-026 (a button that does nothing teaches travelers not to press it); ask for confirmation then reschedule (a Change Card without the Change Card's reasons or its "keep as is") | The actions write `status` and the `actual_*` timestamps only. The day then re-projects from the truth, so the health pill drops and the leave-by moves — a real, useful answer. `evaluateChange` is already built (ENG-05) and waits for B-026 to offer the fix. An E2E asserts every `planned_start_at` is byte-identical after tapping Done, so a future "helpful" reschedule fails loudly | PRD-LIVE-002, PRD F6 |
| D-108 | 2026-08-26 | Live activation is a READ-time decision; "Start today" is the only thing that writes `journeys.status = active` | PRD-LIVE-001 activates Live automatically at day-start OR on an explicit tap, and it is tempting to treat those as one write triggered by a clock. But a journey that silently became `active` because a phone woke at 6am is a state change nobody asked for, and `status` is read by notifications and the journeys list. Separating them means the screen works for someone standing at a temple gate who has tapped nothing, while the persisted state still only changes on purpose | Write `active` on first view (a side effect of looking at a screen); gate the screen behind the tap (useless to the person who just opened the app mid-journey); a scheduled job flipping status at day-start (a clock making a decision about someone else's day) | The Live screen renders whenever the journey has days. "Start today" is shown until the journey is active and then disappears. Ending is the mirror — `completed` when the traveler says so, never inferred from midnight passing | PRD-LIVE-001, PRD Principle 6 |
| D-109 | 2026-08-26 | `StoredItem = JourneyItem & { status, actual_start_at, actual_end_at }` — live state widens the APP's item type, never the engine's | The engine's `JourneyItem` is a scheduling INPUT: what is planned. `status` and the `actual_*` timestamps say what HAPPENED. Adding them to the engine's contract would put fields in front of every scheduling function that none of them should read, and the first thing to quietly schedule from an actual time would be a bug nobody could see | Add the columns to the engine's `JourneyItem` (invites scheduling from actuals); a parallel Map keyed by item id (correct, and every call site has to remember to join it); read status separately in each consumer (three queries where one will do) | Structurally assignable, so a `StoredItem[]` passes straight into the engine with no cast and the engine still cannot see the extra fields in its own type. The clock is read in the PAGE and nowhere below it, keeping D-005's no-clock rule intact all the way down | TRD-ENG-001, PRD-LIVE-002 |
| D-110 | 2026-08-26 | The Live screen ALWAYS recomputes in the browser from IndexedDB against the real clock; server-rendered props are the first paint, never the answer | A cached page showing a stale projection is indistinguishable from a live one — the NOW card looks completely normal while pointing at an item that finished forty minutes ago, and a traveler acts on it. That is the single most dangerous failure this screen can produce, and it is the DEFAULT behaviour of any service-worker-cached page. Recomputing locally decouples the two things that go stale at different rates: DATA can be hours old and still useful, a CLOCK cannot be wrong by a minute | Trust the cached RSC payload offline (the stale-NOW bug, shipped); refuse to render Live offline (abandons PRD-OFFL-002's headline requirement); revalidate on a timer only (still wrong between ticks, and offline the timer never succeeds) | `assembleLiveView` was extracted as a PURE function so the server and the browser call the same code over the same bytes — TRD-ARCH-002 requires them to agree, and two assemblers would drift. The service worker caches navigations NetworkFirst so the document survives offline; the projection over it is always recomputed, so a stale document never means a stale answer. E2E asserts it under `context.setOffline(true)` | PRD-OFFL-002, PRD-OFFL-007, TRD-ARCH-002 |
| D-111 | 2026-08-26 | The offline layer fails SOFT everywhere: an unavailable store is a no-op, never an exception and never a message | PRD-OFFL-003 forbids a sync-error dialog outright. IndexedDB is unavailable for reasons that have nothing to do with this app — private browsing, a full disk, a browser set to block site data — and none of them are a traveler's problem to solve while standing at a temple gate. A failed background sync is also not news to someone who knows they are on a train | Surface a warning when storage is unavailable (the dialog the PRD bans); throw and let callers decide (every call site then needs a try/catch, and the first one forgotten becomes a white screen); disable the feature loudly (tells the traveler about our problem) | Stated plainly as the cost: if storage is unavailable, offline does not work. What must not happen is offline not working AND an error interrupting the journey. A failed sync also leaves the PREVIOUS snapshot intact — losing the cache because the network went away would defeat the point entirely, and there is a test for it | PRD-OFFL-003, PRD §12.7 |
| D-112 | 2026-08-26 | `lib/journey-types.ts` — the journey types and `toEngineJourney` split out from `journeys.ts` | Second occurrence of the D-086 failure and worth naming as a pattern: `journeys.ts` imports `getKnowledgeBundle` → the Supabase server client → `next/headers`. Any browser-side module reaching for `toEngineJourney` dragged that whole chain into the client bundle and failed the build with a message naming none of the files responsible. B-023 hit it exactly, because the offline Live view runs in the browser and needs both the types and the conversion | Duplicate `toEngineJourney` in the offline module (two copies of the fallback-to-today rule, which will diverge); a type-only import plus an inline conversion (the same duplication, less visibly); mark `journeys.ts` client-safe (it genuinely is not — it queries) | Nothing in `journey-types.ts` may import anything that touches a request. `journeys.ts` re-exports so no call site changed. The rule generalises: a value shared between server and browser belongs in a module with no request-scoped imports at all | TRD-ARCH-002, D-086 |
| D-113 | 2026-08-26 | A guest's draft lives in IndexedDB on their own device, is OFFERED back after sign-in, and is deleted the moment it is migrated | Carried from B-019, which resolved a documented TRD-vs-backlog conflict in favour of this: `journeys.status = draft` implied a server-side draft for someone with no account, which means writing a stranger's plan to our database before they have agreed to anything. PRD-ACCT-001 is guest-first and PRD §10 treats journey contents as personal | A server-side draft row (stores personal data for someone who has not signed up); URL-only (already done in B-019 and sufficient across a sign-in round trip, but lost when the tab closes); migrate automatically on sign-in (a journey appearing in an account because someone once looked at a preview — and on a shared phone, possibly not their journey) | Offered, never automatic (PRD Principle 6). Saved on success and only then deleted — the reverse order loses the journey someone just signed up to keep. Expires after 30 days on READ, since there is no job on a device that has been closed. Deletion is a FEATURE, not tidying: this is a shared-phone product in a shared-phone market | PRD-ACCT-001, PRD-PRIV-002 |
| D-114 | 2026-08-26 | CSP ships with `script-src 'unsafe-inline'`; nonces are deferred to M2 with the reason recorded | Next inlines its bootstrap and streams RSC payloads through inline scripts, so a strict `script-src 'self'` does not boot the app at all. The honest fix is per-request nonces, which Next supports only via middleware — and this app's middleware already does locale routing plus a Supabase session refresh on every request. Adding nonce generation makes every response uncacheable, which is a real cost paid on every page view for a hardening that matters most against an XSS this app has no known vector for | Ship no CSP at all (loses frame-ancestors, form-action, object-src and connect-src, which are the directives that actually constrain this app's shape); ship nonces now (every response uncacheable, on a product built for slow connections); `strict-dynamic` (same nonce requirement) | Everything OTHER than script-src is tight: `frame-ancestors 'none'`, `object-src 'none'`, `form-action 'self'`, and `connect-src` naming exactly Supabase, ORS and Open-Meteo. E2E asserts the headers on a real response AND that no screen logs a CSP violation — a policy that breaks the app is worse than none, because the first person to hit it loosens it in a hurry and loosens it too far | TRD §6.1, TRD-PERF-001 |
| D-115 | 2026-08-26 | Analytics is an ALLOWLIST of event names and, per event, of property names; unknown properties are dropped and unknown names refused | PRD-ANLY-001's acceptance is zero user identifiers. A ban list only stops what someone thought to forbid, and the field that leaks is always the one nobody thought about. Property VALUES are additionally constrained to look like categories — lowercase, no spaces, 64 chars — which is what stops a search term arriving as a property and with it the name of a temple somebody was quietly looking for at 2am | A ban list of known-bad keys (fails open on anything new); free-form properties with review (review does not run at 2am); no analytics until M2 (PRD §7's funnel is unmeasurable and the launch decision becomes a guess) | Unknown NAMES are refused because that is a bug or a probe; unknown PROPERTIES are dropped rather than rejecting the event, because a client shipped last month should not lose a legitimate measurement over a field this build removed. The session id is the device cookie truncated to 16 chars — enough to group one visit, not enough to follow someone across weeks. 12 vitest, plus pgTAP asserting the table has no column that could hold a person | PRD-ANLY-001, TRD §4.7 |
| D-116 | 2026-08-26 | PRD §12.7's forbidden vocabulary is enforced by a test over the traveler app's strings, not by a copy review | B-024's checklist asks for a "copy review against §12.7". A read-through holds until the next person writes a string. The scanner strips comments first (neighbouring code discusses "an error" constantly while rendering nothing of the sort) and skips code-shaped occurrences — `console.error`, `role="alert"`, and `ApiError("invalid")`, whose argument is a machine-readable status code and not something a person reads | A manual review each release (does not survive the release after); lint rule on all strings (no way to tell copy from code); ban the words everywhere including Ops (§12.7 governs the TRAVELER's voice — an operator at a desk is better served by "this failed, here is the reference") | Verified by deliberately breaking it: inserting "An error occurred… !" into a component fails both the word check and the exclamation-mark check, and passes again when reverted. A scanner that cannot be shown to fail is a scanner that proves nothing | PRD-DSGN-005, PRD §12.7 |
| D-117 | 2026-08-26 | The error reporter is a SEAM with a console transport; `@sentry/nextjs` is not installed until there is a DSN | TRD §11.2 asks for "Sentry in both apps". What both apps need is somewhere for an unexpected failure to GO, wired in once. The package is heavy, this milestone's own budget is ≤180 kB of route JS, and the DSN is founder-owned (ACCT-06) — so installing it today spends the budget on something inert. Swapping it in later is writing one adapter, not threading a new call through thirty handlers | Install Sentry now and leave it unconfigured (bundle weight for nothing, and an unconfigured SDK that looks configured); keep `console.error` per site (nothing to swap when the DSN arrives); no reporting until B-025 (the first anyone hears of a failure is a traveler saying the app did not work) | Wired into `withApi.onUnexpected` and into both apps' error boundaries. The report carries route, message and a truncated stack — never the request body, headers, cookies or a user id: an error report that carries a traveler's journey to a third party is a privacy incident with good intentions. `reportError` swallows a throwing transport, so reporting can never break what it reports on | PLAT-06, TRD-PERF-001 |
| D-118 | 2026-08-26 | The nightly backup RESTORES what it just took, into a scratch database, and compares table and RLS-policy counts before keeping the artifact | A backup nobody has restored is a file, and the moment you discover it was only ever a file is the moment you needed it. Three failures are silent until then: a dump truncated by a broken pipe, a restore that printed errors and exited zero, and an encrypted archive whose passphrase was never actually tried. The RLS-policy count is the check most likely to be skipped by hand and the one that matters most — a restore that returns the data without the policies returns it to everybody | Dump and upload (the usual arrangement, and the one that fails this way); restore weekly by hand (nobody does, and least of all in a quiet month); trust Supabase's own backups (the free tier has no PITR, which is why TRD-DEPL-003 asks for this at all) | The restore reads the ENCRYPTED archive, never the plaintext dump, so encryption round-trips are proven too. `ON_ERROR_STOP=1` is what makes the restore's exit code mean anything. The scratch database is dropped on every exit path — a half-restored copy of traveler data left lying around is its own incident. Verified locally: 55 tables and 140 policies. Unverified at production volume, and said so in the runbook | TRD-DEPL-003, PRD §10 |
| D-119 | 2026-08-26 | Migrations are applied BEFORE either app is promoted, in a pipeline where a failed migration stops the deploy with the old version still serving | Migrations are forward-only (D-015), so the schema can always run the previous release's code — the reverse is not true. Promote first and a traveler's request reaches code expecting a column that does not exist yet, which presents as a bug rather than as a sequencing mistake. It also means a rollback needs no migration rollback: after `vercel rollback` the database is one additive migration ahead, which is the safe direction | Deploy and migrate together (a window where they disagree); migrate after promoting (the window, guaranteed); reversible migrations (a DROP COLUMN undoing an ADD COLUMN destroys whatever was written in between) | `deploy.yml`: verify → migrate → promote → smoke, with a concurrency group so two deploys cannot race one another's migration. Preflight runs first and refuses a localhost Supabase URL, a missing Resend key or a service-role key exposed to the browser — each of which fails quietly in production rather than loudly | TRD-DEPL-001, D-015 |
| D-120 | 2026-08-26 | The runbook marks every procedure ✅ verified or ⚠️ untested, and lists what is still unproven | A runbook is read at 3am by someone who cannot tell which half is which. Mixing "I have run this" with "this should work" makes the whole document untrustworthy at exactly the moment it is needed, and the honest version is more useful than the longer one | Write it as though everything works (the usual, and it fails the first time someone follows a step that was never tried); document only what has been run (leaves the most likely production failures unwritten, since nothing has been deployed) | Nine procedures. Verified: the restore path, the 429 diagnosis (which cost an afternoon in B-019), the AI degradation, travel-time labelling. Untested: rollback, the paused project, key rotation, stuck queues — all blocked on there being a deployment. A closing section names what is unproven, including that a restore has never been done at production volume | TRD-DEPL-003 |
| D-121 | 2026-08-26 | Evaluating a change and applying one are two routes with a tap in between; the card is PERSISTED before it is answered | PRD-ADPT-005 allows nothing to be applied without a tap, and PRD Principle 6 makes that the product's spine. A single endpoint that evaluated and applied would make "show me my options" indistinguishable from "do it". Persisting the card first matters for a subtler reason: the traveler is shown options computed at one instant and answers a few seconds later, so the option they tap is looked up BY ID in the stored card rather than recomputed — a recomputation against a journey that moved underneath them would apply something they never saw | One endpoint returning the new plan (removes the tap the whole product rests on); recompute on apply (applies an option the traveler was not shown); hold the card client-side only (a refresh loses it, and nothing records what was offered) | `decided_at` makes a second POST a no-op rather than double-moving every item — a double-tap on a slow connection is the normal case, not the exotic one. "Keep as is" is recorded as a decision with a null option index, because a log that only recorded changes would show a traveler who declined three times as one who was never asked. E2E hits both routes directly: an option id that was never on the card is a 404 | PRD-ADPT-004, PRD-ADPT-005, PRD Principle 6 |
| D-122 | 2026-08-26 | Option labels are keyed on the LADDER STEP, not on the option id or its contents | PRD F6's ladder has a fixed meaning per rung — (a) is always "use the slack", (f) is always "drop something" — so a label derived from the step cannot drift out of step with the rung it describes. Deriving it from the changes instead would mean re-inferring intent from operations, and the first option whose shape was ambiguous would get a label that quietly misdescribed it | Label from the option's operations (re-infers intent that the step already states); i18n keys from the engine (the engine deliberately emits keys, and this IS the layer that renders them); a generic "adjust the day" (true, useless, and identical for every rung) | The engine returns keys and params, never sentences — the same rule as health causes — so `change-sheet.tsx` is where PRD F6's contract becomes language. Every option carries its own "because" (PRD F6 requires one), and "keep as is" states the health that results from changing nothing: an option to decline that does not say the cost is not really an option | PRD-ADPT-002, PRD-ADPT-004 |
| D-123 | 2026-08-26 | `no_impact` gets a quiet line and never a card; only running late or staying longer raises a trigger from Live | PRD-ADPT-005 says a no-impact outcome is a quiet toast. Interrupting someone to tell them nothing needs to change is precisely how they learn to dismiss the card that does matter — and the card that does matter will be a closure or a missed return. "Done" deliberately raises nothing: finishing something roughly on time is not a change worth interrupting anyone over | Show a card for every trigger (trains dismissal); show nothing for no_impact (the traveler tapped something and got silence, which reads as broken); raise a trigger on Done too (a card after every completed item) | The row is still written for a no-impact evaluation — `journey_change_events` is the record of what the product NOTICED, and a table only written when options existed would make the log claim the system never saw the quiet days | PRD-ADPT-005, PRD-LIVE-002 |
| D-124 | 2026-08-26 | The notification sender is a Vercel Cron route, not a Supabase Edge Function | TRD §5.4 lists it among the Edge Functions, and this is the second time that assignment has not survived contact with the runtime (see D-072 for keepalive). `web-push` is a Node library and `packages/providers` already has a tested VAPID adapter built on it; an Edge Function runs Deno and would need a second implementation of the one thing here that must not be subtly wrong — a mis-signed payload is rejected by the push service with no error anywhere the app or the traveler can see | Port the adapter to Deno (two implementations of VAPID signing, and the wrong one fails silently); call a Vercel route FROM an Edge Function (a hop that adds a failure mode and no capability); use Supabase's queue (nothing sends web push from it either) | Runs every ten minutes, guarded by `CRON_SECRET` like the keepalive — an open endpoint that sends to every traveler is worth more to an attacker than to us. Missing VAPID keys make it report that it did nothing rather than 500 every ten minutes: push being unconfigured is a degradation, and a cron that alarms constantly is an alert nobody acts on. Flagged for the TRD alongside DOC-01 | TRD §5.4, PRD-NOTF-001 |
| D-125 | 2026-08-26 | The scheduler READS as the traveler and WRITES as service-role | **Found by a pgTAP assertion during B-027, after the first implementation silently wrote nothing.** `notifications` grants `authenticated` only SELECT and UPDATE — a traveler may read their reminders and mark them read, and may not create one, because scheduling is the product's decision and a traveler who could insert rows could schedule anything for themselves. The request-scoped client therefore could not insert, and PostgREST reports that as an ordinary error the caller was swallowing | Grant `authenticated` INSERT (lets a traveler schedule arbitrary notifications for themselves); run the whole scheduler as service-role (bypasses RLS on the journey READ, so nothing would stop scheduling reminders for a journey you do not own); a SECURITY DEFINER function (works, and hides the split that is the actual point) | The journey is read through the request-scoped client, so RLS is what stops someone scheduling for someone else's journey; only the INSERT is privileged, and it writes for exactly the user id the route authenticated — never one taken from a request body. The pgTAP now asserts the refusal explicitly, so the next person to try the obvious thing finds out immediately | PRD-PRIV-002, TRD-DB-003 |
| D-126 | 2026-08-26 | PRD-NOTF-003's weekly cap is applied at SCHEDULING time, so a capped draft never becomes a row | The cap could equally be applied when sending. Applying it at scheduling means a suppressed notification leaves no trace in the queue — and a queue full of rows marked "suppressed" is a queue somebody eventually tidies up by sending them. Only `suggestion` and `advisory` count against it: a leave-by reminder is not a message from us, it is the journey the traveler asked for arriving on time, and counting it would let the cap silence the one thing nobody would want silenced | Cap at send time (leaves suppressed rows to be misread later); cap per type (seven separate weekly budgets is not restraint); no cap (PRD-NOTF-003 exists because an app that buzzes is an app people silence entirely) | Copy is stored as i18n KEYS with params, never a rendered sentence, so a traveler who switches to Telugu before a reminder fires gets Telugu rather than the language they were using last Tuesday. The switches say WHEN each type fires rather than only what it is called — "leave-by reminder" tells someone nothing; "15 minutes before you need to set off" tells them whether they want it | PRD-NOTF-001, PRD-NOTF-003 |
| D-127 | 2026-08-26 | Resolving a report records what an operator FOUND; it never edits knowledge | PRD-REPT-002 is unambiguous that a report is a signal at tier T5 and is never auto-published. The tempting shortcut is to let "Updated" also write the corrected fact, since the operator has just checked it. That would put a path into the knowledge tables that bypasses the publish gate — and three people repeating the same wrong thing would eventually become the truth, with the gate having a hole shaped like a feedback form | Let "Updated" edit the entity inline (bypasses the gate); auto-apply after N corroborating reports (the same hole, on a timer); close reports without recording an outcome (the reporter never hears back, and PRD-REPT-003 exists because a report that vanishes is the last one that person files) | Three outcomes and no fourth, named exactly as PRD-REPT-003 does — Updated, Confirmed as correct, Couldn't verify. "Couldn't verify" is as easy to record as the other two, because a queue that only offers success is a queue where the hard ones sit forever. Correcting the underlying fact stays a separate edit through the normal gate. pgTAP asserts a reporter can neither edit the place they reported nor promote their own report into a verified fact | PRD-REPT-002, PRD-OPS-WF-005 |
| D-128 | 2026-08-26 | Booked-slot promotion is its own route, carrying the time and the tier together | PRD-PREP-003 promotes an item to FIXED when a traveler enters the slot they were given. Doing that as a tier change on the existing item route would let the two facts arrive separately — leaving a window where an item is FIXED at whatever time it happened to be scheduled for, which is a fixed commitment to the wrong moment and worse than none. B-019 learned the same lesson from the other side (D-095): a FIXED item without `fixed_start_at` leaves the return guard with no anchor | Reuse the item PATCH with a tier field (the window above); infer FIXED from a booking reference (nothing in the product knows what a booking reference means); promote automatically when a prepare task is ticked (ticking "book this" is not the same as having booked a specific slot) | `fixed_start_at`, `fixed_end_at` and the planned times are written in one statement, and `confirmed: true` is required rather than assumed — PRD Principle 6 applies to promotion as much as to removal, because a FIXED item is one the option ladder will refuse to move. Returns fresh health, since promoting something can turn a comfortable day tight and that is what the traveler needs to see next | PRD-PREP-003, PRD-PLAN-006 |
| D-129 | 2026-08-26 | The offline outbox replays in the order actions were taken, stops at the first network failure, and drops rather than retrying forever | Three rules, each answering a different way a queue fails. ORDER: someone who marked an item done and then answered a Change Card about the rest of their day did those in that sequence; replaying the other way round evaluates the card against a day that has not happened yet. STOP-ON-FAILURE: if one request could not reach the server the next will not either, and working through the rest would empty the queue by EXHAUSTING it rather than delivering it — the traveler's actions quietly disappearing on a bad afternoon. DROP: a 4xx will not become a 2xx on the tenth try, and the commonest case is an action aimed at something already answered from another device | Fire-and-forget on reconnect (loses order); parallel replay (same); retry forever (a queue that never drains, and a battery that never rests); surface failures to the traveler (PRD-OFFL-003 forbids the dialog outright) | 429 is the one 4xx kept, because it means "later" rather than "never". Five attempts then dropped — enough to survive a bad afternoon, not enough to outlive the journey. 13 vitest against a real IndexedDB, including that a network failure leaves the queue INTACT rather than burning it | PRD-OFFL-004, PRD-OFFL-003 |
| D-130 | 2026-08-26 | The outbox drains on the REVALIDATION path, not only after an action — found by an E2E that timed out | The first wiring flushed inside `reproject`, which runs after a traveler taps something. It looked right and was almost useless: someone who marked an item done offline and then closed the app would return with a signal, see everything look normal, and never send it. The queue only moved if they happened to tap something ELSE. `useOfflineFirst` already runs on mount, on a timer, and on the browser's `online` event — which is exactly when a queue should drain | Flush in a dedicated effect (a third place that has to remember); flush in the service worker (cannot reach the session cookie); leave it in `reproject` (the bug) | Caught because the E2E asserted the queue reaches zero within PRD-OFFL-007's 30 seconds rather than asserting it was enqueued and stopping there. An assertion that only checks the near half would have passed | PRD-OFFL-004, PRD-OFFL-007 |
| D-131 | 2026-08-26 | Offline replanning runs WITHOUT traveler profiles, and the card says so | PRD-ADPT-007 requires user-initiated replanning to work offline, and the engine's purity makes that free — the same `evaluateChange`, over the snapshot the server produced. But traveler profiles are deliberately not in that snapshot: they are the most sensitive rows in the schema (PRD-PRIV-002), and caching mobility data on a device to slightly refine a buffer multiplier is a trade this product does not make | Cache mobility in the snapshot (trades a privacy guarantee for a refinement); refuse to replan offline (abandons the requirement in the exact situation it exists for); replan silently without the caveat (an option that looks more comfortable than it is, with nothing saying why) | So an offline option can read slightly more comfortable than it would online, and the card states that plainly — "worked out from what's saved on this device… we'll check it again". External triggers (knowledge, transport, weather) do NOT replan offline, because the information they are about has not reached the device either. The next online evaluation is authoritative | PRD-ADPT-007, PRD-PRIV-002 |
| D-132 | 2026-08-26 | A failed feed poll WRITES an `unavailable` reading rather than skipping the feed | PRD-DYN-003 requires the UI to say "Live update unavailable — showing last known (as of …)" within 60 seconds of an outage, and it can only say that if the failure is recorded. A poll that quietly skips a broken feed leaves the last good reading looking current indefinitely — which is the exact opposite of the promise, and the traveler is reassured by silence. The same reasoning as writing a change event for a `no_impact` evaluation (D-123): the log records what the product NOTICED, not only what it acted on | Skip a failing feed (the last good reading looks current forever); mark the config unhealthy instead (loses the timeline — you cannot tell one bad poll from a week of them); serve a cached value silently (PRD F10 forbids illustrative data in a live slot) | The view exposes `unavailable` readings alongside `ok` ones so the app can distinguish them, and a degraded feed renders its LABEL and no values — showing yesterday's forecast beside "unavailable" would be showing stale data as current, and a blank invites the assumption that conditions are fine. `is_stale` is computed in SQL against the feed's own `refresh_minutes`, so a client cannot drift from an interval an operator changed | PRD-DYN-003, PRD F10 |
| D-133 | 2026-08-26 | Weather is not cached in the offline snapshot, and a live slot shows nothing offline | PRD F10 says never show illustrative or placeholder data in a live slot, and a forecast cached yesterday is exactly that — it looks live, it is labelled live, and it is wrong in the way that matters. Everything else about a journey is meaningfully cacheable: opening hours change slowly, a plan does not change at all. A forecast is the one thing whose whole value is that it is current | Cache with an age label (a "live" value that is a day old is not live, whatever the label says); cache and hide when stale (the same, with a threshold nobody can defend); no weather at all (loses PRD-DYN-005) | So the Conditions section is simply absent offline, and the rest of the Live screen works exactly as before. This is the one place where the offline story deliberately does LESS than the online one, and it is the right way round: a traveler with no signal keeps their plan and loses the forecast, rather than keeping a forecast they should not trust | PRD F10, PRD-OFFL-002 |
| D-134 | 2026-08-26 | Only conditions that change whether being outdoors is a good idea count as disruptive | PRD-DYN-004 raises a Change Card trigger from a live change, and the temptation is to trigger on any forecast movement. A card about a 20% chance of light rain is a card travelers learn to dismiss — and then the thunderstorm during the outdoor evening aarti gets dismissed with it. The same restraint argument as PRD-NOTF-003's weekly cap (D-126) and `no_impact`'s quiet line (D-123) | Trigger on any change (trains dismissal); a configurable threshold per destination (a number nobody can choose well, and one that will be set once and never revisited); no weather triggers (loses PRD-DYN-004 entirely) | Thunderstorm and heavy rain always; rain only at ≥70% chance; and ≥40°C, because pilgrimage routes are walked, often barefoot, often by people who are not young. Applied only to OUTDOOR items — the same forecast is irrelevant to a darshan inside a temple, and saying it anyway is how a traveler learns to skim | PRD-DYN-004, PRD-ADPT-001 |
| D-135 | 2026-08-26 | The Journey Record shows NO score, no percentage, no ratio and no badge — a plain sentence about what the traveler said mattered most | PRD F16 asks for "protected experiences completed — plain statement, no score", and the same reasoning that keeps a number off Journey Health (PRD F5) applies harder here. A traveler who missed the evening aarti because their mother needed to sit down has not scored 71%. An item that did not happen renders with no cross, nothing red, and none of the words "missed", "failed", "incomplete" or "skipped" | Show completed-of-planned as a fraction (turns a pilgrimage into a task list somebody underperformed at); show a progress ring (the same thing wearing a shape) | E2E asserts the RENDERED page contains no `%`, no bare `n/m`, and none of the grading vocabulary — a ratio can appear in markup without ever appearing in `lib/record.ts` | PRD-CMPL-001 |
| D-136 | 2026-08-26 | `POST /api/journeys/:id/complete` is an explicit tap that FREEZES a snapshot; the Record is a live projection before it | The TRD names the route (§5) and the `summary jsonb` column (§4.6), so it exists verbatim. PRD Principle 6 decides its trigger: a journey does not become complete because its last date slid into the past — somebody says it is done. Freezing matters because a finished journey should not revise its own account of itself when a place is later renamed or an item tidied away; a live projection before the tap is what makes the Record readable mid-journey, which is when a traveler most wants to see what they have done | Roll to `completed` in `roll_journey_statuses` (auto-applies a state change, PRD Principle 6); compute the Record live forever (a journey silently rewrites its own history) | Reflection stays editable after completing, nothing is locked, and the offer only appears once the last day is behind them | PRD-CMPL-001, TRD §4.6/§5 |
| D-137 | 2026-08-26 | "Plan a similar journey" carries TIERS, not completions, into the pre-filled brief form — and leaves the dates blank | Someone who missed an experience they had marked must-do still considers it must-do; rebuilding from what was completed would quietly demote the thing they were most disappointed to miss. FIXED collapses into must-do because a booked slot in a NEW journey is not booked yet. It opens `/plan` pre-filled rather than creating anything: a similar journey is a DIFFERENT journey, and one tap producing a second saved journey is the auto-applied change PRD Principle 6 forbids. Dates are absent because they are the one thing a previous journey cannot tell us and the one thing the traveler certainly has a view on | Create the journey and open it (auto-applied); jump straight to `/plan/preview` (needs a start date, so it would have to invent one) | Mobility is the strictest in the party, matching how the original brief collected it (D-094). The brief travels in the URL as every other brief does (D-089/D-093) | PRD-CMPL-003 |
| D-138 | 2026-08-26 | The traveler E2E suite signs in as TWO accounts, and the Record spec runs as the second | `journeys_write` is 120/hour PER USER (TRD §6.2). One account standing in for the whole suite spent a real traveler’s entire hourly budget in ninety seconds — the run went red with 429s the day the Record spec landed, and the counter showed 130. The limit is correct; the fixture was the artefact. CLAUDE.md §5 forbids weakening a limit even in a test, so nothing about the limit changed and `0010_ai_rate_limits_test.sql` still asserts it | Raise the limit for tests (a bypass by another name); reset counters mid-run (the same bypass, spread out); make every test share one journey (order-dependent tests that pass for the wrong reason) | The remaining single account now sits at 116/120 after a full run — **TEST-01** raised: the next write-heavy spec must take a third account or the suite goes red again | TRD §6.2 |
| D-139 | 2026-08-26 | Change detection is deterministic: a candidate opens when the EVIDENCE for a published field disappears from its own source — never on any page diff, and never with a proposed value | PRD F17 asks for a candidate "when a monitored source's capture diff touches an existing published field". Knowing which field a page diff touched normally needs AI extraction (ACCT-04, which does not exist) and would then be an inference the trust model has to trust. `trust_records.evidence_excerpt` already holds the exact words an operator read when they verified the field, next to the source they read them in — so the excerpt vanishing IS the signal, with no model involved. `new_value` stays null: detection knows what disappeared, not what replaced it, and inventing one would be AI writing facts (CLAUDE.md §5) | Raise a candidate on ANY diff (a page with a rotating banner interrupts somebody every morning, and a queue that cries wolf daily is abandoned inside a week — after which the real timing change sits unread among fifty false ones); wait for ACCT-04 (leaves monitoring entirely unbuilt for a key that is not a technical dependency) | Also skips a field whose excerpt was already absent from the PREVIOUS capture — otherwise a one-off reorganisation raises the same candidate every cycle forever. Case and line breaks are ignored; "Evening Aarti" → "Evening aarti" is a style edit. Extraction fills `new_value` on top of the same row when a key lands | PRD-OPS-SRC-004 |
| D-140 | 2026-08-26 | The Review queue records decisions and structurally CANNOT publish; "edit & accept" is a link to the entity editor, not a field on the card | If a review queue could publish, a source quietly rewriting its own page would rewrite what we tell someone standing at a temple gate — which is the one thing the whole trust model exists to prevent. `decide_change_candidate()` writes `change_candidates` and `review_tasks` and nothing else: no knowledge table, no `trust_records`, no entity status. Correcting the fact stays a separate edit through `publish_entity()`, with its validation and separation of duties intact | Let accept write the new value (fastest path, and the exact failure above); let accept set `verification_status` (softer version of the same thing — the trust record would then say verified against evidence nobody read) | Asserted in pgTAP by accepting a candidate and then checking the entity, its trust record and its publish status are all untouched. Said on the screen too, because an operator who believes accept corrects the fact stops making the separate edit | PRD-OPS-WF-001 |
| D-141 | 2026-08-26 | The ingestion cron lives in the OPS app with its own `vercel.json`, not beside the traveler app's crons | Ingestion reads Ops tables, writes Ops evidence and fills an Ops queue; no part of it concerns a traveler. The two apps already deploy as separate Vercel projects (`deploy.yml`), so each carries its own `crons`. The traveler app's route budget is also the one under a 180 kB ceiling (TRD-PERF-001) | Put it in `apps/web` with the others (a route the traveler app has no business owning, and a cross-app import of the Ops runner); use a Supabase Edge Function (Deno, and the runner depends on the Node capture provider — the same argument as D-124) | `apps/ops/middleware.ts` had to exempt `/api/cron`, or the session gate would 307 it to sign-in and Vercel would record a success while the job never ran — the exact failure that was live for a week in the traveler app (D-097) | PRD-OPS-SRC-002, TRD §5.4 |
| D-142 | 2026-08-26 | **`service_role` had no table privileges anywhere in `public`, so every privileged write in the product silently did nothing.** Fixed in `0025`; the callers now surface their failures | Found while building B-029, when the ingestion runner failed with "permission denied for table ingestion_jobs" as service-role. It was not the new table: `has_table_privilege('service_role', …)` was false for **all 55 tables**. So journey reminders were never scheduled (B-027), live feed readings never stored (B-031), and report-resolution notifications never queued (B-028) — each caller discarded the error and reported success. RLS bypass and GRANTs are different things: `service_role` is BYPASSRLS, so every policy question about it answered "policies do not apply", which is true and irrelevant when the GRANT is checked first and there is none | Grant per-table as each feature needs it (the same omission again, one table at a time); leave it and route every privileged write through SECURITY DEFINER functions (a large rewrite of working code to work around a missing grant) | `0026_service_role_test.sql` asserts privileges FOR the role and performs a real `set role service_role` write — pgTAP runs as `postgres`, which owns every table, so no existing assertion could see this. `alter default privileges` covers future tables. TRUNCATE is untouched (0022's stance). The notification scheduler now THROWS and the feed cron returns `ok:false` when nothing could be written | TRD §6.1, D-125 |
| D-143 | 2026-08-26 | Publishing records a `knowledge_updates` row; the TRAVELER'S own session turns it into a Change Card. Ops never evaluates on anybody's behalf | Building a card needs the travelers — mobility drives buffers and the physical-load check — and `traveler_profiles` has no Ops policy at all, permanently (AUTHORIZATION_MODEL, CLAUDE.md §5). So Ops writes a fact about PUBLISHED KNOWLEDGE and nothing else; the traveler's next load notices it and evaluates under their own RLS with their own profiles. That is not a workaround, it is the only place the two halves can legally be in one query. And the result is a CARD: an operator correcting a temple's evening timing does not move somebody's evening — PRD Principle 6 has no exception for changes that are obviously right | Have Ops raise the card directly (needs traveler profiles in an Ops path — forbidden); auto-apply a knowledge correction (the change the whole product exists not to make); leave `knowledge_update` unraised (the state before this: knowledge right, journeys wrong, which is worse than either alone) | `journeys.knowledge_checked_at` is nullable and an existing journey is treated as having seen everything so far — switching this on must not greet every traveler with a backlog of every publish since March. One trigger per check, not one per changed entity: four cards in a row is how somebody learns to dismiss them unread | PRD-OPS-WF-007, PRD-ADPT-001 |
| D-144 | 2026-08-26 | An approver sees a COUNT of affected journeys and the wording travellers will read — never an identity — through one `security definer` function | PRD F18 asks for "the list of affected upcoming journeys (count)", because somebody about to interrupt two hundred people mid-journey should know that while they can still decide not to. But Ops has no read on `journeys` or `journey_items` and must not gain one. `affected_journey_count` is the single crossing, it crosses in one direction, and what comes back is four aggregate keys | Grant Ops read on journeys (the boundary gone, for a number); show nothing (an approver publishing blind); compute it in the app (needs the same grant) | pgTAP asserts the returned object has exactly `active`/`upcoming`/`total`/`first_start_date` and that an approver still cannot read journeys, items or profiles; an E2E asserts no uuid and no email appears anywhere in the rendered panel. Finished journeys are excluded — a change to last month is not news, and counting it would make every impact number grow forever | PRD-OPS-WF-007 |
| D-145 | 2026-08-26 | Automatic conflict detection (PRD-OPS-SRC-005) is genuinely BLOCKED, not merely unbuilt — raised as OPEN-014 rather than worked around | `conflicts.values` is `{source_id, tier, value, captured_at}` per TRD §4.3, so detection needs per-source claimed VALUES. `trust_records` is `unique (entity_table, entity_id, field_name)`: it records the one source a field is currently verified against and has nowhere to hold a second source's competing claim. Nothing in the schema can represent "source B says 18:00" while source A's record says 18:30. The only producer of per-source claims is `ai_extractions.proposed_entities`, which needs ACCT-04 | Add a claims table on my own initiative (a schema decision the TRD does not describe, made silently); infer conflicts from change candidates (B-029 leaves `new_value` null by design, so there is nothing to compare); build the queue and pretend detection works (worst — an empty Conflicts queue reads as "no conflicts") | **Recommendation:** build the Conflicts queue with its PRD-OPS-WF-003 resolution and a MANUAL raise — which is what a verifier does today when they read two sources — and wire auto-detection to extraction when ACCT-04 lands | PRD-OPS-SRC-005, TRD §4.3 |
| D-146 | 2026-08-26 | The Freshness monitor is a SQL function, not a query in the page, and covers published critical fields only | Three rules it depends on already live in the database and must not be restated: `critical_fields()` (the TRD §4.4 list, kept in step with the publish views by a pgTAP assertion that parses the view definitions), `is_entity_published()`, and `derive_freshness()`. A second copy in TypeScript is a second thing that can drift, and drifting means a field quietly stops being watched. Scope is deliberate too: a draft's stale field is nobody's problem yet, and a non-critical field in the queue buries the ones the publish gate depends on | Query `trust_records` from the page and filter in TypeScript (duplicates the critical-field list); a view (permissions harder to reason about than one explicit role check); include every field (the queue becomes unreadable, which is the same as not having one) | Sorted worst-first, then oldest — any other order puts the field nobody has checked since March on page four. `expiring30` keys on `valid_until` rather than age, because a source that vouches until 30 September stops being right on 1 October however recently it was read. Assigning is narrower than looking: a researcher sees what is rotting, a verifier puts it on a desk | PRD-OPS-WF-006 |
| D-147 | 2026-08-26 | Conflicts are raised BY HAND for now, and escalating deliberately leaves the field reading as uncertain | The manual raise is OPEN-014's recommendation, taken: auto-detection needs per-source claimed values and the schema has nowhere to hold them (D-145). Raising is not bookkeeping — `conflict_flag` drops the field to LOW through `derive_confidence` and the published views carry it, so opening one is a decision to stop telling travelers that field is certain. PRD-OPS-WF-003's three outcomes are all real: winner, both-valid-with-context (seasonal timings, where forcing a winner would publish something wrong half the year), and ESCALATE, which keeps the flag because somebody standing in front of the place deserves "check locally" for as long as we genuinely do not know | Auto-detect from change candidates (B-029 leaves `new_value` null by design); clear the flag on every outcome (tidies an unresolved disagreement off a queue while still showing it as certain); let resolving edit the value (whichever source shouted loudest becomes the truth) | Resolving writes `conflicts` and `review_tasks` and nothing else — pgTAP resolves one and then asserts the entity's value and publish status are untouched. Every outcome needs a reason, enforced in SQL: a settled conflict with no reasoning is a field that looks decided and cannot be re-examined. The raise lives on the field's trust panel, where somebody actually notices | PRD-OPS-SRC-005, PRD-OPS-WF-003 |
| D-148 | 2026-09-04 | Adopt the shadcn preset `b2xb9m6ayu` (style `base-lyra`, base colour `mist`, Base UI) as Mandhira's design system, taken verbatim in shape and sizing, with the brand orange set to #FF660E | Founder decision, made against a stated conflict. The preset's controls are dense — 32px buttons, `rounded-none` — where CLAUDE.md §4 and PRD §12.3/§12.8 require 44px tap targets and per-class radii. The founder chose the preset as shipped, knowing it regresses the 44px rule; that rule is a WCAG 2.2 AA target-size requirement, so this is a deliberate accessibility trade, not an oversight, and it should be revisited before the traveler PWA ships to phones | Adapt the preset to 44px and keep PRD radii (rejected: founder wanted the preset's own rhythm); take the preset only on Ops where input is a mouse (rejected: two visual languages in one product); stay on the hand-built Radix set (rejected: the point was to adopt the new system) | The 62 preset components live in `packages/ui/src/components/ui/` on Base UI, beside the 21 legacy Radix components, which keep the 45 existing routes building until each is migrated. `design-system.css` is loaded last in the chain and therefore owns the shadcn contract, so the new palette reaches old and new screens together. `shadcn-bridge.css` is out of the chain but kept, unimported, until the migration finishes. Target-size conformance is now a known open item | CLAUDE.md §4, PRD §12.1, §12.3, §12.8, D-149 |
| D-150 | 2026-09-13 | Founder authorised full implementation of every gap in `docs/PROJECT_AUDIT.md`, with UI built on the D-148 preset rather than PRD §12's visual specification | Founder instruction in conversation ("use the design system preset for UI, don't consider the PRD"), which CLAUDE.md §2 ranks above the PRD. Read as: visual design follows the preset; the product's safety rules stay, because the audit's findings are violations of exactly those rules | Treat the PRD as void (would drop the publish gate, tier invariants and explicit-tap rule the audit found broken); keep PRD §12 visuals (contradicts the instruction) | Umbrella plan `docs/plans/AUDIT-REMEDIATION.md`, workstreams R0–R9 on branch `audit-remediation` | CLAUDE.md §2, D-148 |
| D-151 | 2026-09-13 | New rate-limit scope `ops_action` (60/min and 1,500/h per operator) applied inside `opsAction` | The audit found every Ops Server Action unlimited, including `runIngestion`, which triggers outbound fetches. TRD §6.2 lists no scope for ordinary Ops writes | No limit (a stolen session could rewrite the knowledge base in a loop); reuse `journeys_write` (a traveler budget, wrong owner) | Fails closed like every other scope; generous enough that nobody researching a destination meets it | TRD §6.2, TRD-SEC-001 |
| D-152 | 2026-09-13 | Publish status is guarded by a trigger on the ROLE running the write; `publish_entity` becomes SECURITY DEFINER over one body, `publish_entity_as`, shared with the scheduler | RLS granted column-wide UPDATE to four Ops roles and nothing guarded `status`, so a PATCH through PostgREST published without validation, four-eyes, audit or `knowledge_updates` (audit S-1). A trigger that refuses `anon`/`authenticated` setting `published` passes only definer code and server identities | A column-level grant excluding `status` (breaks submit/return-to-draft, which legitimately write it); a GUC flag set by the function (settable in the same session); RLS `with check` on status (cannot see the old value) | Editing an already-published row and unpublishing stay allowed and are now audited. `publish_entity_as` is granted to no client role — a caller who could name the actor could publish as somebody else | PRD-OPS-WF-004/009, S-1, S-10 |
| D-153 | 2026-09-13 | Client roles hold exactly the grants declared in `0030`; `postgres` default privileges in `public` grant them nothing | On the current Supabase image, every table and function a migration creates was granted to `anon`/`authenticated` by default. Measured: anon held full DML on all 56 tables and EXECUTE on 30 SECURITY DEFINER functions, including `journey_summary_payload(journey_id)` with no ownership check. RLS held the rows, so nothing visibly leaked, but the two-layer design (no grant, then a policy) had one layer | Re-revoke per table as each is found (the next table reopens it); trust RLS alone (one permissive policy later exposes a table to the internet) | `0030` is now the single reviewable statement of the client surface; pgTAP `0031` asserts anon's table privileges are exactly four and its definer functions exactly seven, and that a table created afterwards starts closed. Every future migration must grant explicitly | D-029, D-033, 0022, S-2 |
| D-154 | 2026-09-13 | SECURITY DEFINER helpers reachable by clients answer only for published entities; `entity_media`/`media_assets` reads follow the parent's publish status | anon could enumerate draft UUIDs through `entity_media` and read their trust metadata, accessibility notes, route stops and image paths (audit S-2). `request_is_client()` + `entity_is_published()` let one function body serve the published views (clients) and Ops screens (full access) | Revoke the helpers from anon (breaks every published view, which call them as the invoker); separate Ops copies of each helper (duplicated logic that drifts) | 0013's backend-only `is_entity_published()` now delegates to `entity_is_published()` (0032), so the freshness job, the views and the client helpers share one definition of "published" | S-2, D-078 |
| D-155 | 2026-09-13 | Every Ops-actor mutation is audited by one trigger, with a daily-salted `ip_hash`; capture and extraction rows are audited without bodies | `audit_log` had two write sites, both inside publishing (audit S-3); trust changes, role grants and source edits left no trail. Recording only Ops actors keeps traveler writes and cron jobs out of an Ops-readable trail. The salt is unreadable by any role and deleted as the day turns, so hashes link actions within a day but never across days or back to an address | Record every mutation (buries the trail, exposes traveler identities to Ops); hash with a fixed secret (reversible by anyone holding it forever) | 31 tables carry `<table>_audit`; `media_assets` and `live_feed_configs` gain version history | TRD §6.1, CLAUDE.md §4, S-3 |
| D-156 | 2026-09-13 | Journey Health: time load is judged per stretch up to each FIXED item; a visit must fit inside one availability window; the return guard is reported on its anchor's day; every journey day is reported; state is decided on the unrounded load | Four audit defects: a morning crammed before a midday train read Comfortable; a visit starting five minutes before closing passed; a late return on day 3 marked every day Broken; an empty day vanished from the report. 80.4% also rounded into Comfortable | Keep start-minute availability (the traveler arrives as it closes); report the guard journey-wide (sends the traveler to fix the wrong day) | New causes `health.cause.fixed_unreachable` (FIXED unreachable → Broken, except the return anchor, which the guard reports) and `health.cause.runs_past_availability`. Regression suite `audit-fixes.test.ts` | PRD F5, PRD-HLTH-001..005 |
| D-157 | 2026-09-13 | Change options are judged on the trigger's day plus any day they move something onto, and every option lists affected items with before/after instants | Options were judged on the whole journey's state, so an unrelated Broken day suppressed every option and the traveler got no card (audit defect). Judging only the trigger day would hide a move that breaks tomorrow | Judge the whole journey (the defect); judge only the trigger day (hides the cost of moving) | `ChangeOption.affected` added (additive; persisted cards without it still render). Rung (b) now also moves an OPTIONAL item to another feasible day; rung (d) considers the latest start that still fits a single window; every rung consults `item-rules.ts` | PRD F6, PRD-ADPT-002/004 |
| D-158 | 2026-09-13 | Engine keys become language only through the message catalogs; the Change Card renders the engine's own `labelKey`/`becauseKey` | The Change Card re-derived labels from the ladder step through a hand-written map that had drifted: a removal read "Swap the order of two things" and removing an IMPORTANT item said "the one you marked as optional". The trust disclosure tested for keys the engine never emits, and "why it matters" looked up cause keys that no longer existed | Fix the hand map (drifts again); render keys in English inline (blocks te/hi) | `engine-text.test.ts` scans the engine source and fails if any emitted key lacks English. Health text moves to the catalogs too | PRD F6, PRD-ADPT-004/006, Principle 6 |
| D-159 | 2026-09-13 | Travel estimates are routed and cached when a plan is built, for the consecutive legs the plan actually walks | Nothing wrote `travel_estimates` and the routing provider had no caller, so the engine counted zero minutes between places without an authored connection (audit finding). Routing only consecutive legs keeps cost proportional to plan length rather than to the square of the places in a brief | Route every pair of the brief's places (quadratic); compute travel inside the engine (breaks purity, D-005) | `planWithTravel` plans, routes, and plans again (at most two passes); `provider` stores `adapter:routed|estimated`; a routing failure is logged and never blocks saving | TRD §5.5, INTEGRATIONS, MAPS-01, D-104 |
| D-160 | 2026-09-13 | A journey is saved by one SQL function, `create_journey`, in a single transaction with the owner taken from the session | `POST /api/journeys` wrote five tables and checked one error, so a failure mid-way saved half a journey and answered `{ok:true}` (OPEN-012) | Client-side compensation (can itself fail); service-role writes (bypasses the traveler's RLS) | Resolves OPEN-012. SECURITY INVOKER, so every insert is still checked by the traveler's own policies | OPEN-012, TRD §5.2 |
| D-161 | 2026-09-13 | Traveler data rights as three functions: `export_my_data` (DEFINER, every subquery filtered on the session user), `request_account_deletion`/`cancel_account_deletion` (the existing 30-day purge carries it out), `reorder_journey_items` (atomic) | The purge job existed with no way for a traveler to start it; no export existed (PRD-PRIV-003/004, DPDP). The export filters on the account rather than RLS so an Ops user exporting their own data does not receive other people's reports | INVOKER export under RLS (leaks Ops-readable rows into "your data"); immediate hard delete (no recovery from a mistaken tap) | Push keys and share tokens are omitted as credentials. Reordering refuses any list that does not name every item of the day exactly once | PRD-PRIV-003/004, PRD-PLAN-003 |
| D-162 | 2026-09-13 | Ops administration in SQL: scheduled publishing (revalidated when it runs), version restore that returns published content to review, a team list of role holders only, and aggregate-only dashboards | PRD F18 asks for scheduled or immediate publish; O20 for restore; O21 for role management; O01/O22 for dashboards. Each check belongs in the database where the gate already lives | Restore straight to published (unapproved content goes live); list all accounts for role granting (Ops browsing travelers); return analytics rows (one visit's path is a person) | `publish_scheduled_entities` runs every five minutes through `run_scheduled_job` and appears in `v_job_health`; a schedule that no longer validates is marked `blocked` with its reason. `ops_find_account` answers one exact email | PRD F18, F20, PRD-OPS-WF-004/008, PRD-OPS-MON-001..003 |
| D-164 | 2026-09-13 | Extracted knowledge claims are stored in `ai_extractions`, routed to the Review queue as `change_candidates.new_value`, and open conflicts automatically when a T3-or-better source disagrees with a differently-sourced reviewed value — resolving OPEN-014 without a claims table | OPEN-014 found nowhere to hold "source B claims 18:00" beside source A's trust record. The TRD already provides both halves: the extraction row keeps every claim verbatim, and `conflicts.values` holds both sides. A claim is admitted only if its excerpt appears in the capture and its times/numbers appear in that excerpt (`groundKnowledgeClaims`) | A new claims table (a schema the TRD does not describe); comparing change candidates (detection left `new_value` null by design); trusting the model's excerpt without checking it against the capture | `record_extraction` is service-role only and writes no knowledge table — pgTAP asserts the place's value is untouched. Agreement with the held value is skipped; a T5 source reaches review but can never flag a verified field. `open_conflict` became a role-checked door onto `open_conflict_internal`, the same body the system uses. Activates when ACCT-04 supplies a model key | PRD F17, PRD-OPS-SRC-003/005, OPEN-014, D-145, CLAUDE.md §5 |
| D-165 | 2026-09-13 | The preset's three contrast failures are fixed in tokens, and controls meet 44 px on coarse pointers only | axe found `--muted-foreground` on `--muted` at 4.13:1, the brand orange used as text at 2.93:1, and destructive text on its tint at 3.98:1 (D-148's open items). A new `--primary-text` token (a deeper orange on light, #FF660E itself on dark) serves links; `muted-foreground` and `destructive` darken slightly within their hue. Target size grows on `(pointer: coarse)`, so the founder's dense preset stays as chosen at a desk and WCAG 2.2's target rule holds on a phone | Retheme the preset (overrides the founder's choice in D-148); keep the failures documented (they would follow every migrated route); 44 px everywhere (the density the founder picked would be gone on desktop too) | `design-system-contrast.test.ts` computes WCAG ratios from the oklch tokens for five pairings in both themes, calibrated against #FF660E's hex. It also found D-149's recorded 5.69:1 for ink on orange to be about 6.0:1 — the decision stands, the figure was slightly low | WCAG 2.2 AA, D-148, D-149, CLAUDE.md §4 |
| D-166 | 2026-09-13 | The vendor-SDK import ban lives in the shared ESLint base as SDK families, with `packages/providers` the only exemption | The audit proved two holes by probe: an SDK absent from the fixed five-name list passed, and the rule existed only in the Next apps' config, leaving `db`, `ui`, `journey-engine` and `i18n` unguarded | Keep per-app rules and extend the list (the next unlisted SDK passes again); a lint plugin (a dependency for one rule) | Re-probed after the change: `@google/generative-ai` imported into `packages/db` is refused; the same import inside `packages/providers` is allowed | CLAUDE.md §4, ARCHITECTURE §1 |
| D-167 | 2026-09-13 | EmailProvider over Resend's REST API with no SDK, returning `not_configured` rather than throwing when no key or sender is set | TRD §2.1 names an EmailProvider and none existed. A REST call keeps the dependency surface to `fetch`; distinguishing `rejected` (never retry) from `unavailable` (retry may work) lets a caller respond correctly. Magic-link sign-in mail stays with Supabase Auth's SMTP | The Resend SDK (a dependency for one endpoint); throwing when unconfigured (breaks every courtesy mail in development and before ACCT-01) | New env `EMAIL_FROM`; `.env.example` also gains `GEOCODING_USER_AGENT`, which Nominatim's policy requires and the code already read | TRD §2.1, INTEGRATIONS, ACCT-01 |
| D-170 | 2026-09-13 | Traveler features completed on the preset, with four recorded deviations from TRD §5.2 | Built: `/prepare` hub and `/profile` (A23: travelers, language, saved places, notifications link, export, deletion request and cancel); saved places; add-to-journey with tier and day (PRD-DISC-004); `PATCH /api/journeys/:id` and reorder; the health detail sheet (A12) with "Simplify this day"; "See all" and the two missing discovery filters; `/api/search`, `/api/travel-estimate`, `/api/live/:destinationId`; skip link and a per-minute live region; the Prepare `documents` group; an on-screen leave-by reminder. Deviations: (1) `POST /api/account/export` returns the JSON as an attachment instead of `{ downloadUrl }` — no storage object or signed URL to expire or leak; (2) "Simplify this day" asks the engine with trigger `preferences_changed` because `change_trigger_enum` has no simplify value, and the card's "what changed" line is replaced on screen; (3) an added item does not copy `prep_requirements` — Prepare derives tasks from published knowledge on every read, so a copy would go stale; (4) `/api/search` returns places and experiences without `destinations[]` | Add a `simplify` enum value (a migration and Dexie bump for a label); store export files for a download URL (a copy of personal data at rest) | The leave-by reminder fires only while the Live screen is open — the code says so. Account, traveler and saved-place routes share the `journeys_write` limit rather than new scopes. The date and "near a journey" filters apply after the 20-result cap (open) | PRD F2, F4, F5, F13, F15, TRD §5.2 |
| D-169 | 2026-09-13 | Every Ops screen in the PRD map is built on the preset (O01 dashboard, O11 Verify, O13 all eight types + scheduling, O17–O22, O20 audit and restore, O21 team and flags, O08 captures, O16 usage); O06 stays delivered as panels (D-046), and AI appears in Ops only as suggestions a person accepts | The audit found six Ops screens absent, O01 an empty shell and two P0 workflow requirements (O11 Verify, O20 audit) unbuilt under a COMPLETE row. Translation suggestions fill the editor rather than writing `ai_draft` rows, so no model output is stored until a translator saves or confirms it; extraction runs inside ingestion and only ever opens review work (D-164) | Store AI suggestions as `ai_draft` automatically (text nobody read reaches the catalog); leave O06 as its own screen (records with no independent existence, D-046) | The Places and Destinations tables were folded into the shared `EntityTable` and four copies of the label helper into one; read failures on media, guidance, publish, transport and source detail now show an error state instead of an empty list; the Ops rate limit is enforced (D-151). Phrase-pack editor and media crop presets are not built. Four new Playwright specs, not yet run | PRD F17–F20, PRD-OPS-WF-002/008, PRD-OPS-MON-001..003, D-046, D-151, D-164 |
| D-168 | 2026-09-13 | CI gates formatting (`pnpm format:check`), with the vendored shadcn preset excluded from Prettier | The audit found `format:check` ungated. Measured before gating: the 62 preset files in `packages/ui/src/components/ui` had never been Prettier-formatted, nor had 10 hand-written files. Reformatting the preset would make every future `pnpm ui:add` update an unreadable diff against upstream | Format the preset too (a wholesale reformat CLAUDE.md §5 forbids, and lost upstream diffs); leave the gate off (formatting drift returns) | Hand-written files are formatted; the preset keeps upstream's formatting and is listed in `.prettierignore` | CLAUDE.md §5, D-148 |
| D-163 | 2026-09-13 | `/design-system` gets a 540 kB bundle allowance | It sits outside the traveler route tree, is `noindex`, and renders the entire preset library by design (D-148); it had put `main` over TRD-PERF-001 and blocked deploys | Exclude staff routes from the check (a future traveler route could be mis-filed); split the page (its purpose is seeing everything at once) | `check:bundle` now also runs on every PR, so a regression cannot reach `main` unseen again | TRD-PERF-001, D-148 |
| D-149 | 2026-09-04 | `--primary-foreground` is dark ink, not the preset's near-white, wherever #FF660E is the fill | #FF660E measures 2.93:1 against white — not readable text. The preset pairs its near-white foreground with a much darker orange (#ca3500, oklch L 0.553) where that pairing is fine; substituting the lighter brand orange while keeping the foreground fails WCAG on every primary button, badge and sidebar item simultaneously, and fails invisibly, because the page still renders and still looks like the brand | Keep the preset's near-white (fails 4.5:1); darken the brand orange until white works (stops being #FF660E, which the founder specified); use `--brand-primary-text` #c14600 as the foreground (that is the colour for brand-coloured TEXT on a light ground, not ink on a brand fill — about 2:1, the D-025 trap) | The shipped ink is `oklch(0.216 0.032 42.743)`, 5.69:1. `packages/ui/src/design-system.test.ts` asserts the lightness stays below 0.35 for `primary-foreground` and `sidebar-primary-foreground` in both themes, so a future `shadcn init` writing the default back is caught by a test rather than by a user. The reference page renders the failing pairing beside the passing one, labelled | D-025, D-148, PRD §12.1 |
| D-171 | 2026-09-13 | Email is its own opt-in (`notification_prefs.email`, default off), checked when the message is sent; the natural-language planner is A07/A08 on `/api/intent/extract`; filtered search reads a wider window before capping | EmailProvider (D-167) had no caller. Email reaches outside the app, so it needs its own yes rather than riding on the in-app switches, and deciding at send time means an operator resolving a report queues the row without learning whether the traveler agreed or where they are. A07/A08: every inferred value needs its own Keep or Take out before the plan can be built (PRD-INT-003); the traveler picks which fixed commitment is the way home rather than Mandhira guessing; the questions screen is the fallback whenever descriptions cannot be read (TRD §5.5). Search: date, near and step-free filters run in code after the read, so a 20-row read hid matches (D-170's open item) | A notification type per channel (doubles the switches); Ops checks consent (Ops reads traveler preferences and addresses, PRD-PRIV-002); send email whenever the in-app switch is on (mails people who never asked). Take the latest commitment as the return (inference Principle 1 reserves for the traveler) | The cron reads only configured channels, so rows for unconfigured push or email stay scheduled without starving in-app delivery. An email the provider could not take is retried for 24 h; a rejected one fails. The in-app list excludes email rows. Report resolution is the first email; journey notifications can queue `channel='email'` rows later. Filtered searches read up to 200 rows per kind | PRD F3, F15, PRD-INT-003..005, PRD-REPT-003, TRD §2.1, §5.5, D-167, D-170 |
| D-172 | 2026-09-13 | TRD-PERF-001 and the e2e suite are met, not relaxed: barrel imports optimised, interaction-only sheets load on tap, destructive tokens and system dark mode fixed, the fixture window widened | The first full run after the traveler and Ops batches failed 18 tests, and `check:bundle` failed five routes. Bundles: a client component importing `cn` from `@mandhira/ui` shipped the legacy Radix dialog and checkbox (44 kB gzipped) to every route that did. Contrast: a destructive badge on a tinted table row read 4.35:1 in light and 4.19:1 in dark. Dark mode: the preset followed only the `.dark` class while the legacy tokens followed the system, so a dark system setting put light text on a light page. Fixture: Dawn Darshan was a 90-minute visit in a window that closed 30 minutes after the day began, which D-156 correctly reports as Broken, so every plan built on it was Broken | Raise the budget or add allowances (the budget stops being one); drop the dark-mode axe check; rewrite the plan tests to expect Broken (the Comfortable and Tight paths would lose their only end-to-end coverage) | `optimizePackageImports: ["@mandhira/ui"]`; the add-to-journey, day-health and journey-details sheets and the knowledge-change Change Card load when first needed, each behind a real button that announces a dialog. Light `--destructive` L 0.51, dark L 0.72, and the decorative tint on the Ops job row removed; the preset dark palette is mirrored under `prefers-color-scheme`, and the contrast test fails if the copies differ. Selectors were corrected only where the product had deliberately changed (a screen-reader live region repeating the NOW heading, authored Telugu, the two filters now built, labels that share words). Two real defects fixed: the Ops restore confirmation stayed open after restoring, and the signals time windows had button semantics. The fixture window is 05:00–08:00, locally and in the seed | TRD-PERF-001, WCAG 2.2 AA, D-025, D-148, D-156, D-165, D-170 |
| D-173 | 2026-09-13 | Launch hardening: pushes are displayed; reminders follow the plan and the traveler's language; configuration is validated at deploy and at server start; server failures reach Sentry; sign-in links are limited on the server; AI extraction has a daily budget; the frequent jobs run from pg_cron | A code-verified gap review found: the service worker had no `push` handler, so every push was dropped on arrival; leave-by rows were queued once per item and never updated or cancelled when a plan changed; push text was English-only and never named the place; only the three Supabase variables were validated; nothing read `SENTRY_DSN`; the `auth_magic_link` and `ops_ai_extract` limits had no caller; and `vercel.json` scheduled crons that Vercel Hobby rejects | The Sentry SDK (spends the route budget, TRD-PERF-001); Vercel Pro for the crons (a cost the TRD defers, and still nothing records whether a run happened); re-queue every reminder on each save (duplicates, and a moved item still fires at its old time); validate only in the preflight (an app deployed any other way starts misconfigured); an interactive map library in Ops (a dependency where a static image answers the question) | `syncJourneyNotifications` reconciles: a scheduled reminder the plan no longer implies, or implies at another time or for another place, is cancelled and the current one queued; delivered ones never repeat; it runs after every plan change (item edit and removal, reorder, a Change Card decision, Done and running late). Notification copy is in the catalogs and rendered in the profile locale at send time. `packages/config/env-rules.mjs` is shared by the preflight and both apps' `instrumentation.ts`, and production refuses to start on a blocking finding. `getErrorTracker` posts Sentry envelopes with `fetch`, email addresses masked, nothing from the request. Magic links go through `POST /api/auth/magic-link` (web) and a server action (Ops, which answers the same whether or not an account exists), limited per device and per hashed address. `dispatch_app_cron` (0037) calls the routes through pg_net with the URL and secret held in Vault, records `job_runs`, and marks an earlier dispatch failed when its route did not answer 2xx; `vercel.json` keeps only the daily keepalive. Also: a MapTiler static map in the Ops location picker when the key is set; TEST-02 closed by a runner seam test; Dependabot; `docs/LAUNCH_KEYS.md` lists every external value | PRD F15, PRD-NOTF-002, PRD-LANG-001, TRD §6.2, TRD-SEC-001, TRD-OBSV-001, TRD-DEPL-001, MAPS-02, D-072, D-167, D-171 |
| D-174 | 2026-09-13 | A reporter's account id never reaches Ops: the resolution notice is queued by a role-checked database function, and the audit trigger drops `user_id` from report rows | Building report photos found that `resolveReport` asked for `user_reports.user_id` back, a column 0030 withholds from every client role, so resolving a report failed outright; and `audit_ops_change` copied whole report rows, reporter id included, into `audit_log`, which admins and approvers read (PRD §10: Ops never sees who reported) | Grant operators `user_id` (the leak, made official); read it with the service role in the Server Action (the id still passes through Ops code and logs); drop `user_reports` from the audit trail (loses the record of who resolved what) | `notify_report_resolution(uuid)` (0039) checks the reports roles, reads the reporter inside Postgres, queues the in-app and email notice once, marks `notified_user`, and returns only whether it queued anything. `audit_redact` removes `user_id` before any report row is audited, and existing audit rows were scrubbed. pgTAP 0039 | PRD §10, PRD-REPT-003, D-155, D-171 |
| D-175 | 2026-09-13 | Phrase packs: an Ops editor at `/phrases`, the traveler screen A17 at `/[locale]/destinations/[slug]/phrases`, and a phrase sheet beside the Live NOW card that reads the phrases saved on the device | PRD-LANG-004, PRD-OPS-CNT-004 and A17 had a table, policies and a published view but no editor and no screen. The schema records no destination language, so A17 shows each translation except the traveler's own and the authoring language, with transliteration, and the meaning in the traveler's language | Pick a "local language" per destination (a schema change the TRD does not describe); put the phrase entry among the NOW card's three actions (PRD-LIVE-006 caps them at three) | `v_published_phrases` gains `audio_path` (0035), exposing only live, licensed audio linked by `phrases.audio_media_id`. Dexie v3 stores the destination's pack in the offline snapshot; universal phrases sit under `destination_id = "*"`. CSP gains `media-src 'self'` plus the Supabase origin, without which every recording is refused. Audio plays online only (the service worker does not cache it); text works offline. No unpublish path, as for advisories. pgTAP 0036 | PRD-LANG-004, PRD-OPS-CNT-004, PRD-LIVE-002, TRD §4.4 |
| D-176 | 2026-09-13 | Crop presets are one focal point per media asset; report photos are private media rows that only the server writes and only the reports roles see, through 15-minute signed URLs | PRD-OPS-CNT-003 asked for presets and nothing stored a crop; M4 report photos were not built. One focal point frames every ratio, present and future, over the one original (D-055). A per-user storage folder would put reporter identity into media rows and version history that every Ops role reads (PRD §10) | A crop rectangle per preset (re-cropping for every new ratio); a traveler-writable upload path (identity in storage paths); a separate `photo_path` column (departs from TRD's `user_reports.media_id`) | `media_assets.focal_x/focal_y` (0036) set in Ops on a media detail screen by pointer, arrow keys or typed percentages, previewed for hero 3:2 and card 4:3, rendered with `object-position` via `@mandhira/ui` helpers. Report photos: `storage_bucket = 'reports'`, restrictive RLS hides them from every client role, JPEG only, 2 MB; the browser re-encodes to drop location data and the server re-checks and sets a random path. The 0007 anon inventory now counts permissive policies only. No traveler page renders media yet, and no retention job removes report photos | PRD-OPS-CNT-003, PRD F14, PRD §10, D-016, D-055 |
| D-177 | 2026-09-13 | Report photos are deleted 30 days after their report is resolved or closed, or 180 days after filing if it never is; the report itself stays, and a traveler's export says which reports still carry a photo | No retention rule covered report photos (D-176 follow-up), and DPDP's storage limitation asks that personal data be kept no longer than its purpose needs. A photo exists so somebody checking the report can see what the traveler saw; once the report is resolved that purpose is served, and the picture can show people, number plates or the traveler's own family | Keep photos as long as the report (an archive of travelers' pictures nobody needs); delete at resolution (no room for a second look or a follow-up); a fixed age for every photo (removes evidence from a report still being worked) | `report_photos_due` and `forget_report_photo` (0040, service role only) say what is due and forget rows; `/api/cron/report-photos` removes the files through the Storage API first, because Supabase refuses deletes from its storage tables, then forgets the rows, so a run cut off midway converges on the next. Dispatched nightly by `dispatch_app_cron` and watched in `v_job_health`. `export_my_data` gains `has_photo` per report. pgTAP 0040 | DPDP, PRD §10, PRD F14, D-016, D-173, D-176 |
| D-178 | 2026-09-13 | Phrase recordings play offline: each is downloaded whole into Cache Storage when the offline snapshot syncs, and the service worker answers playback, byte ranges included, from that file | D-175 shipped phrase audio as online-only, and a phrase is most needed where there is no signal. Browsers request audio in byte ranges; a partial 206 response cannot be cached and replayed, so caching on first play stores nothing usable | Cache on first play (stores partial responses, or nothing); precache every recording in the service worker build (recordings are content, not app assets, and change without a deploy); leave audio online-only (fails exactly where the feature matters) | `warmPhraseAudio` (lib/offline/phrase-audio.ts) runs after the snapshot transaction commits, never inside it, capped at 150 recordings and skipping ones already held; `sw.ts` serves `/storage/v1/object/public/media/` audio CacheFirst with the range plugin, storing only whole 200 responses, 60-day expiry | PRD-OFFL-001, PRD-LANG-004, D-175 |
| D-180 | 2026-09-13 | Every traveler-facing string in `apps/web` comes from the message catalogs, enforced by a syntax-aware scanner test | A read-through holds until the next string is written, and several screens received `locale` yet rendered fixed English (R6 in AUDIT-REMEDIATION). Moving the strings also exposed that Live's "Why?" list mapped cause names the engine no longer emits, so it was mostly empty | A regex scanner (cannot tell `className` from `title`); an ESLint plugin (a new dependency) | `lib/i18n-coverage.test.ts` parses every `.tsx` under `app/` and `components/` with the TypeScript parser already in the repo: JSX text, text attributes, prose props, setter and toast strings, prose literals; a self-test proves each rule; two exemptions with reasons (the staff-only `/design-system`, and `global-error.tsx`, which renders when next-intl never ran). 357 keys in 39 new and 5 extended namespaces, te/hi machine-authored (OPEN-004). Sentence-building helpers in `lib/` take a translator; weekdays come from `Intl`. Live's causes now render through `engineText`. Still English: 13 API error messages, `packages/ui` status labels, engine warnings on the plan preview, and 156 older keys without te/hi | PRD-LANG-001, PRD §12.7, R6, D-148, D-158, D-163 |
| D-179 | 2026-09-13 | Three P1 behaviours built on facts the app already held: search ranks what fits the traveler's journey first, a journey item notes once when its critical information has gone stale, and an open journey shows another device's edits within 10 seconds; the traveler knowledge bundle now carries trust | A code-verified check found PRD-DISC-006, PRD-TRST-004 and PRD-ACCT-005 unbuilt, and that `getKnowledgeBundle` always sent `trust: {}` — so Journey Health's trust-exposure check (PRD F5 check 5) never saw a real record | Filter search to the journey (hides what a traveler asked for); store the stale note's dismissal in the database (a migration for a one-time hint; per-device memory is enough to keep it from nagging); a Supabase Realtime subscription for sync (about 70 kB on a page near its budget, and a socket held open on a phone) | Fit: at the journey's destination and open or running on one of its days, for the journey under way or else the soonest ahead (`journey-fit.ts`, `journeyToFit`); ranked after every filter and before the cap, never filtered, labelled on the card. Stale note: the oldest stale critical field across an item's experience and place, in months (`stale-note.ts`), dismissed per confirmation on the device, so a field re-verified and stale again is said again. Sync: `GET /api/journeys/:id/version` (latest `updated_at` of the journey and its items, soft deletes included) under its own `journey_sync` read limit, checked every 10 s while visible and online, refreshing the server-rendered page on change; latest edit wins as every write already does | PRD-DISC-006, PRD-TRST-004, PRD-ACCT-005, PRD F5, TRD-PERF-001 |
| D-181 | 2026-09-13 | The traveler API keeps the route names it was built with where they differ from TRD §5.2; the table below is the mapping | TRD-API-002 asks for §5.2's names verbatim, and a code-verified audit found seven unrecorded differences. Each built name either serves the traveler better or matches a decision already made, and renaming now would change working clients, e2e specs and the offline outbox for no behavioural gain | Rename every route to §5.2 (churn across clients, specs and queued offline actions); keep aliases for both names (two URLs per action to keep in step) | `POST /change` → `POST /api/journeys/:id/changes`; `POST /change/:eventId/decide` → `POST /api/journeys/:id/changes/:eventId` (plural resource, the event id is the path); `POST /start-day` → `POST /api/journeys/:id/live` (starting the day is an action on Live, D-104); `POST /clone` → `GET /api/journeys/:id/similar`, which prefills the plan form rather than copying a journey the traveler has not reviewed (PRD Principle 6); `GET /api/share/:token` → the server-rendered page `/[locale]/s/[token]` (no client needs the JSON); `/api/push/subscribe` → `/api/notifications/subscribe`; `POST /api/auth/migrate-draft` → none: the guest draft stays on the device and is saved through `POST /api/journeys` after sign-in (D-113); the item status route is `PATCH`, not `POST`, because it updates an existing item. Routes added beyond §5.2: `/version`, `/knowledge-check`, `/prepare`, `/reflection`, `/items/:itemId/booking`, `/account/profile`, `/travelers`, `/saved-places`, `/auth/magic-link`, the cron routes and `/heartbeat` | TRD §5.2, TRD-API-002, D-104, D-113, D-170 |
| D-182 | 2026-09-13 | TanStack Query and Zustand are not adopted: traveler reads are server components, offline reads go straight to Dexie, and UI state stays in React | TRD §2 and FRONTEND_ARCHITECTURE §11 name both libraries. Every traveler read page is a server component and every offline read is Dexie-first with revalidation (TRD-ARCH-005), so TanStack Query would add a second cache in front of Dexie and require converting the read path to client components; the only shared client state (sheets, the Live session) lives in a handful of components where React state is enough, so a store would be a dependency with nothing to hold (CLAUDE.md §4: no unnecessary dependencies). Recorded here from DEV-01, which had stayed an open item | Adopt TanStack Query with a sync-storage persister (a rewrite of the read path for a duplicate cache); adopt Zustand for Live and builder state (no state shared widely enough to need it); leave it as an open item (the audit correctly flagged an undecided deviation) | Server components for reads, Server Actions and `withApi` routes for writes, `lib/offline/*` for Dexie, `@tanstack/react-table` still used in Ops. Revisit only if a screen needs client-side caching for something other than offline persistence | TRD §2, TRD-ARCH-005, TRD-ARCH-006, FRONTEND_ARCHITECTURE §11, DEV-01 |
| D-183 | 2026-09-13 | Every engine reader projects recorded actual times forward before reading the plan (`projectActuals`): an item that overran ends when it really ended and counts what it took, every unsettled non-FIXED item after it that day moves by the overrun, and nothing is pulled earlier | PRD-LIVE-002's "Running late" and "Done" record `actual_*` and move nothing (Principle 6), but Live, Journey Health and leave-by reminders read only planned times, so running late changed nothing a traveler could see. Projecting keeps the plan untouched and the Change Card the only way to rearrange it; FIXED items stay put so the health checks report them unreachable. The engine's `JourneyItem` gains optional `status`, `actual_start_at`, `actual_end_at` (additive; the offline snapshot already carried them) | Reschedule the day on "Running late" (applies a change without a tap, forbidden by Principle 6); read actuals only in Live (health and reminders would disagree with Live) | `packages/journey-engine/src/actuals.ts`, applied inside `computeHealth`, `getNowNextLater` and `scheduleNotifications`; idempotent | PRD-LIVE-002, PRD F5, PRD F8, PRD F15, Principle 6 |
| D-184 | 2026-09-13 | A Change Card decision made offline is replayed by re-evaluating the same trigger on the server and applying the chosen option only if the server's card still offers that option id; otherwise it is dropped as no longer applicable. Offline "Keep as is" sends nothing. The outbox stops at a 5xx or 429 as it does with no network, and only one flush runs at a time | Offline cards have no server event, so the queued decision was sent with an empty event id and silently discarded. Option ids name the ladder step, so they are stable across evaluations; applying an option the server no longer offers would apply something the traveler was not shown (PRD-ADPT-005). Continuing past a server error sent later actions ahead of earlier ones and spent every queued action's attempts on one bad minute. Queued item-status taps carry `occurredAt`, which the status route accepts within a week and not in the future | Send the offline card's operations directly (writes a plan computed against a snapshot that may be stale); ask the traveler again on reconnect (a second prompt for a decision already made) | `lib/offline/outbox.ts` `deliver`, `flushOutbox`; the connection check flushes on every page; an offline report is queued instead of hanging | PRD-OFFL-004, PRD-OFFL-005, PRD-ADPT-005, PRD-ADPT-007 |
| D-185 | 2026-09-13 | An email the provider could not take is rescheduled with back-off (5, 10, 20, 40, then 60 minutes), keeping the first due time in the notification's payload so the 24-hour retry window still runs from it; the cron reads due rows oldest first | Retrying on every run spent the batch on a provider that was down and, unordered, let newer rows starve older ones. Keeping the state in `payload` avoids a migration for bookkeeping the table's jsonb column already holds | A `next_attempt_at` column (a migration for one value); leave retries on every run (the batch fills with retries during an outage) | `app/api/cron/notifications/route.ts`, `lib/email-delivery.ts` | PRD F15, TRD §5.4, D-173 |
| D-186 | 2026-09-13 | Ops actions and RLS agree (0042): route stops, editor removals and a reviewer's return-to-draft go through audited SECURITY DEFINER functions that check the same roles the screens offer; media roles can read archived media and write attachments; researchers may write only `unverified`/`ai_extracted` trust records; a trigger enforces PRD F18's per-status role table on any status or evidence change by a signed-in user, so only verifiers and admins set `verified` (approver removed from the app's table); creating knowledge requires researcher/editor/admin; reverify tasks are worked in the Verify queue and close only after a new `verified_at`; triaging a report opens a Verify task | RLS refuses an UPDATE or DELETE by matching no rows, so actions offered to a role were refused silently and reported success (stops doubled, rules not removed, rejects ignored). Functions keep RLS the primary control without granting reviewers UPDATE or editors blanket DELETE. PRD F18 names the verifier as the one who verifies; the app also allowed approvers, which RLS refused | Widen table policies to every role an action lists (reviewers could then edit content, editors hard-delete anything); remove the actions from those roles (contradicts the permission map and PRD F18) | `supabase/migrations/0042_ops_permission_alignment.sql`, `apps/ops/lib/action.ts` `requireDrafter`. Still open for a founder decision: edits to already-published content go live without review, because the update policy allows it and the published views read the live row | PRD F18, AUTHORIZATION_MODEL, D-033 |
| D-187 | 2026-09-13 | Design-review blockers and majors fixed: tailwind-merge is taught the type scale so a primary button keeps its on-primary colour (dark mode 2.5:1 → token contrast); a locale catch-all renders unknown traveler URLs inside the locale layout; Ops gains a root not-found and a mobile menu; trust badges and tier chips render catalog labels; every switch carries its own accessible name; Ops dates are formatted with a pinned locale and zone; Live has a heading and offers "Start today" only on the journey's dates; Telugu and Hindi catalogs complete (156 keys each) | The design review (screenshots, axe) found these on real pages. The contrast failure was not the palette: `cn` dropped `text-text-on-primary` whenever `text-body` followed, because tailwind-merge filed both as text colours. The new te/hi strings were written in-house and follow the catalogs' existing wording; they need the same native-speaker review as the rest (Translator role) | Change the dark palette (the tokens already pass; the class was being discarded); hide untranslated keys behind English fallback (PRD-LANG-001 requires the traveler's language) | `packages/ui/src/lib/cn.ts` + test, `apps/web/app/[locale]/[...rest]/page.tsx`, `apps/ops/app/not-found.tsx`, `apps/ops/components/ops-mobile-nav.tsx`, `trustBadge` catalog namespace | PRD §12.7, §12.8, PRD-LANG-001, D-172, D-180 |
| D-188 | 2026-09-13 | A journey links to the traveler's existing profile of the same person — exact match on self flag, name, mobility and age group — and creates one only when none exists; identical duplicates already stored are merged into the oldest (0043) | `create_journey` created new profiles for every journey, so the Travelers page filled with copies of "You". Matching exactly keeps earlier journeys planned around the profile they were planned with: a trip where Amma uses a wheelchair creates a second Amma rather than rewriting the first | Update the matched profile to the new attributes (silently changes the health of past and upcoming journeys); match on name only (merges different people who share a relation name); have the plan form pick saved travelers (the right next step for the form, but the duplicates are made by the database) | `supabase/migrations/0043_one_profile_per_traveler.sql`; `merge_duplicate_traveler_profiles()` is owner-only | PRD-ACCT-003, PRD-PRIV-002, OPEN-012 |
| D-189 | 2026-09-14 | One dark palette: the older `tokens.css` dark values take the preset's cool near-black hexes (canvas #090b0c, surface #161b1d, raised #22292b, border #2e3233, text #f9fbfb / #9ca8ab / #8a9497), keeping the brand orange and status colours; the dark `theme-color` follows. Also: trust badges sit on their own line in cards, checklist labels keep their width with a readable 2px checkbox border, every Ops button uses the default size, the Reports queue names the entity and links to it, and the journeys list is grouped Active / Upcoming / Draft / Completed / Put away with date ranges | The design review found warm-brown surfaces (bottom navigation, NOW card, Ops sidebar) on the preset's cool near-black in the same screen. Copying the preset's values into the older tokens, rather than pointing them at `var(--background)`, keeps `tokens.test.ts` able to verify every pair's contrast from the hex. Ops sets a 14px root, so the rem-based `xs`/`sm` button sizes rendered 21–24px tall | Point the old tokens at preset variables (the contrast tests would lose the values they check); migrate every screen to the preset components first (weeks, with the mismatch visible meanwhile) | `packages/ui/src/tokens.css`, `packages/ui/src/components/checklist-row.tsx`, `apps/web/app/[locale]/journeys/page.tsx`, `apps/ops/components/reports-queue.tsx` | PRD §12.1, §12.5, PRD F13, D-025, D-172, D-187 |
