node_modules
.next
.turbo
dist
coverage
test-results
playwright-report
.env
.env.*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env.local
apps/*/.env.local
tests/e2e/.auth/
**/public/sw.js
**/public/sw.js.map
**/public/swe-worker-*.js

# The E2E suite builds here so a run never clobbers a running dev server.
.next-e2e/

# Encrypted database dumps produced by scripts/backup-verify.sh. Never committed —
# an encrypted archive in a repository is one leaked passphrase from being plaintext.
backups/

# The translation review pack (`pnpm i18n:review`). Generated from the catalogues on
# demand and sent to a reviewer; a stale copy in the repo is worse than none.
translation-review/
